### 互联网架构梳理

#### 单机架构
- 一台服务器上面装着应用程序和数据库，当用户量多了后单机扛不住了，应用和数据库抢资源，开始加服务器，**应用服务器**处理请求和业务逻辑，**数据库服务器**专门存放数据，做IO操作。

- 用户再次暴涨后，单台应用服务器处理不过来，数据库也扛不住大量查询，于是引出了**高并发**。

#### 高并发
- 开始增加应用服务器数量，部署一模一样的代码，也就是**集群**。
- 那么用户流量怎么分配到每台服务器呢？设计一个中间层，能将请求均匀的分配到每台应用服务器，这就是**负载均衡（Nginx、HAProxy、LVS、F5等）**。
- 增加应用服务器数量也叫**水平扩展**。如果其中一台服务器挂掉了，可以自动屏蔽流量，其他服务器顶上，保证系统不会崩，这就是**高可用**。

应用服务器抗住请求了，但大量请求穿透到数据库，数据库成了新问题。

- 开始考虑应用查询数据库的原理：应用先发查询请求给数据库，数据库内部先去内存缓存区找数据，找到直接返回，没找到就去磁盘找(从磁盘读比内存慢很多)，最后返回给应用。于是把内存缓冲区单独出来放大，经常访问的热数据放进去，就不用去数据库查询了，这就是**缓存** 。
- 一般设计三层缓存：**浏览器缓存**、**应用服务器本地缓存**、**分布式缓存（Redis）**。用户请求每次先去浏览器缓存找，没有就去本地缓存找，再到分布式缓存，最后再到数据库，于是绝大多数请求在缓存层就被解决了。
- **数据库缓存区**只存原始表数据，而三层缓存是存常用的信息，不只是存数据库里的东西，还存页面、接口、静态资源计算结果等。缓存解决了**热点读**。但写请求和非热点读还是要打数据库（写比读慢得多，写会锁行锁表），于是让数据库分工，主库只负责写，从库只负责读，通过**数据同步**保证主从数据一致，这就是**读写分离**，实际情况是读多写少，所以可以**一主多从**。
- 但是时间久了之后，数据库越来越大，各种表越来越大，单库单表撑不住了，于是需要**拆库拆表**，各种数据库（比如用户库、订单库、商品库）互不干扰，也就是**垂直分库**。一张大表拆成多张小表，比如按UID哈希分成多张表，分散压力，配合数据库中间件对应用透明，也就是**水平分表**，这样数据库从单点变成了分布式。

#### CDN和反向代理
- 当系统用户遍地全国，有人访问快，有人访问慢，于是可以在各个地方铺很多节点，将一些静态资源放到里用户最近的结点。然后用户的访问先通过域名解析获取到最近的IP去取数据，这就是**CDN**。
- 应用服务器直接暴露在公网，太容易被攻击，于是设计一个放在用户和应用中间的中间层，公网请求先到这个中间层，再转发给内网应用服务器，这就是**反向代理**。它隐藏了真实应用服务器，防止被攻击，还能做流量清洗、权限校验。

#### 搜索引擎、NoSQL数据库
- 传统数据库遇到模糊查询、全文搜索、多维统计，于是出现了利用倒排索引让搜索秒级响应的**搜索引擎（ElasticSearch）**。
- 同时设计一个存非结构化、半结构化数据，支持高并发易扩展的数据库，就是**NoSQL数据库（MangoDB）**。

#### 分布式架构
- 当应用越来越大，比如一个应用集成了用户、订单、商品，所有功能代码挤在一起，于是改一个代码可能牵扯到整个应用，想给某一种应用服务加服务器抗流量，只能整个应用一起扩容，浪费资源。于是可以按照业务边界将应用拆开，每个部分做成独立的系统，每个系统单独部署、单独发布、单独扩容，这就是**分布式架构**。
- 怎么解决各个子系统相互之间的通信呢（比如用户下单需要查商品库存，然后生成订单），于是设计了**RPC远程调用**，让跨机器的服务调用像本地调用一样。
- 但是服务越来越多，相互调用的链路越来越复杂，快速找到目标去调用是一个问题，于是设计了**服务注册与发现中心**，管理着所有服务的地址，新增的服务在这里注册。
- 还有一个问题，服务之间相互调用相互等待，一个服务卡死，这条链路都卡死，流量一高就雪崩了，于是有了**消息队列**，就算某个服务突然挂了，消息也不会丢失。遇到流量突增，还能先把请求存起来，等系统慢慢处理，这就是**削峰填谷**

#### 微服务
- 解决分布式架构拆服务不够细，以及技术栈不同的问题，出现了**微服务架构**。
- 但是服务太多，调用关系更复杂了，出现问题不知道是哪部分，于是需要**全链路追踪**，直接定位慢服务。流量太大，可能会连锁崩盘，于是需要**限流、熔断、降级**自动保护系统。

#### Docker容器
- 不用给每台机器配环境，把每个微服务的代码依赖，配置环境全部打包成一个镜像，一次打包，到处运行，解决环境不一致的问题。
- 如果容器太多，需要管理，于是需要**K8s**，它能安排容器自愈和扩缩容，流量高了自动加容器，容器挂了自动重启。 

#### 总结

| 架构 | 解决问题 |
| :----: | :----: |
| 单机架构 | 从零搭建 |
| 应用与数据分离 | 资源争抢 |
| 应用集群 + 负载均衡 | 高并发 |
| 多级缓存 | 查询性能、数据库压力 | 
| 数据库读写分离 | 数据库读写阻塞 | 
| 分库分表 + 分布式数据库 | 海量数据存储 | 
| CDN + 反向代理 | 访问速度、系统安全 | 
| 搜索引擎 + NoSQL | 复杂查询 | 
| 业务拆分 + 分布式 | 业务膨胀、代码维护 | 
| 微服务架构 | 团队协作、极致弹性 | 
| 容器化 + 云平台 | 运维、成本、自动化 | 