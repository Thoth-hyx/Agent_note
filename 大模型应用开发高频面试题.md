# 大模型应用开发高频面试题

> 面向国内互联网公司“大模型应用开发 / AI Agent 开发 / RAG 工程师”岗位整理。  
> 内容综合招聘 JD、公开面经、LangChain / LangGraph 官方文档与常见工程实践，不逐字复制来源材料。  
> 时间口径：截至 2026-05-18。

## 适用岗位

| 岗位方向 | 重点能力 |
|---|---|
| 大模型应用开发工程师 | Prompt、RAG、工具调用、接口服务、工程稳定性 |
| AI Agent 开发工程师 | Agent 架构、Function Calling、LangGraph、多 Agent、状态管理 |
| RAG 工程师 | 文档解析、切分、向量检索、重排、评估、知识库运维 |
| LLM 后端工程师 | API 封装、流式响应、并发、限流、缓存、可观测性 |
| 大模型产品工程师 | 场景拆解、效果评估、bad case 迭代、成本控制 |

## 使用方法

1. 第一遍只看“参考回答”，建立知识框架。
2. 第二遍重点练“面试追问”，把每题扩展成 2-3 分钟回答。
3. 第三遍把“项目化回答提示”替换成自己的项目经历。
4. 面试前优先复习高频题：RAG、Agent、LangGraph、工程化评估。

## 复习优先级

| 优先级 | 模块 | 原因 |
|---|---|---|
| P0 | RAG、Agent、Function Calling、LangGraph | 国内大模型应用岗位最常考 |
| P1 | LangChain、Prompt、输出结构化 | 简历项目与代码落地常问 |
| P2 | 评估、性能、安全、上线迭代 | 中高级岗位和二面常问 |
| P3 | 多 Agent、MCP、GraphRAG、多模态 RAG | 进阶加分项 |

---

## 一、LLM 应用基础与 Prompt

### Q01：大模型应用开发和传统后端开发有什么区别？

**高频程度：高**

**参考回答：**
- 传统后端更强调确定性逻辑、数据库事务和接口契约；大模型应用引入了概率性输出，需要做约束、评估和兜底。
- 大模型应用的核心链路通常是“用户输入 -> Prompt -> 模型推理 -> 工具 / 检索 / 后处理 -> 返回结果”。
- 传统系统主要处理结构化数据，大模型应用经常处理自然语言、文档、多模态内容和半结构化结果。
- 大模型应用需要额外关注幻觉、上下文窗口、Token 成本、延迟、模型漂移、提示词注入等问题。
- 工程上要把模型调用当成不稳定外部依赖，需要超时、重试、降级、缓存、审计和监控。

**面试追问：**
- 如何把不确定的模型输出变成稳定接口？
- 模型接口异常或输出不符合格式怎么办？
- 大模型应用的测试和传统单元测试有什么不同？

**项目化回答提示：**  
可以说自己把 LLM 封装成一个后端服务能力层，通过 Prompt 模板、JSON Schema、输出解析器、重试修复、日志追踪和人工反馈闭环来降低不确定性。

### Q02：Prompt 工程的核心目标是什么？

**高频程度：高**

**参考回答：**
- Prompt 的目标不是“写得长”，而是稳定地把任务、上下文、约束、输出格式和边界条件传达给模型。
- 一个好 Prompt 通常包含角色、任务目标、输入变量、约束规则、示例、输出格式和失败处理要求。
- 对应用开发来说，Prompt 要可版本化、可测试、可回滚，而不是散落在代码里的字符串。
- Prompt 优化应基于 bad case 和评估集，而不是只靠主观感觉。
- 对复杂任务，应把大 Prompt 拆成意图识别、检索、生成、校验等多个小步骤。

**面试追问：**
- Zero-shot、Few-shot、CoT 分别适合什么场景？
- 如何判断 Prompt 优化真的有效？
- Prompt 太长会有什么问题？

**项目化回答提示：**  
强调自己维护过 Prompt 模板库，为不同任务设置版本号和评估样例，通过命中率、格式正确率、人工满意度等指标迭代。

### Q03：System Prompt、User Prompt 和 Tool Prompt 分别起什么作用？

**高频程度：高**

**参考回答：**
- System Prompt 用于定义模型身份、行为边界、安全规则和全局约束，优先级通常高于用户输入。
- User Prompt 是用户当前请求，包含任务目标、输入内容和个性化需求。
- Tool Prompt 或工具描述用于告诉模型工具能做什么、参数是什么、什么时候调用。
- 多轮对话中，还会加入历史消息、检索上下文和中间工具结果。
- 工程上要避免把敏感规则、密钥、内部链路暴露给用户可见内容。

**面试追问：**
- 用户指令和系统指令冲突时如何处理？
- 工具描述写得不好会导致什么问题？
- 如何防止用户通过 Prompt Injection 覆盖系统规则？

**项目化回答提示：**  
可以说系统中把安全规则放在 system 层，业务输入放在 user 层，工具 schema 单独维护，并在调用前后做权限校验。

### Q04：什么是上下文工程？和 Prompt 工程有什么区别？

**高频程度：高**

**参考回答：**
- Prompt 工程偏向“如何表达任务”，上下文工程偏向“给模型喂什么信息、以什么顺序喂、喂多少”。
- 上下文工程包括历史对话裁剪、RAG 检索片段选择、工具结果压缩、用户画像注入和系统规则排序。
- 好的上下文工程能减少幻觉、降低 Token 成本、提高回答相关性。
- 常见策略包括窗口记忆、摘要记忆、query rewrite、rerank、去重、上下文压缩。
- 对长任务，应该把上下文作为可管理状态，而不是无限拼接。

**面试追问：**
- 长上下文模型是否能替代 RAG？
- 检索结果太多时如何选择上下文？
- 历史对话太长如何压缩？

**项目化回答提示：**  
结合客服、知识库或 Agent 项目，说明你如何把“最近对话 + 重要长期记忆 + 检索文档 + 工具结果”组合成最终上下文。

### Q05：如何让大模型稳定输出 JSON？

**高频程度：高**

**参考回答：**
- 首选模型原生结构化输出、Function Calling 或 JSON Schema 约束，而不是只在 Prompt 里说“请输出 JSON”。
- 使用 Pydantic / dataclass / schema 校验模型输出，失败后触发修复或重试。
- Prompt 中给出字段含义、类型、必填项和示例，避免字段歧义。
- 对复杂 JSON，拆成多步生成，先生成结构化中间结果，再组装最终结果。
- 生产环境要处理多余文本、字段缺失、类型错误、非法枚举、数组过长等情况。

**面试追问：**
- JSON 解析失败后怎么自动修复？
- Function Calling 和普通 JSON 输出有什么区别？
- 输出 schema 升级如何兼容旧版本？

**项目化回答提示：**  
可以描述“模型输出 -> schema 校验 -> 自动修复 Prompt -> 失败降级人工审核”的链路，并说明格式正确率如何统计。

### Q06：Temperature、Top-p、max_tokens 分别影响什么？

**高频程度：高**

**参考回答：**
- Temperature 控制采样随机性，越高越发散，越低越稳定。
- Top-p 控制候选 token 的累计概率范围，常用于限制采样空间。
- max_tokens 控制最大输出长度，过小会截断，过大增加成本和延迟。
- 面试或生产场景中，信息抽取、分类、工具调用通常使用低温度；创意写作可适当提高。
- 参数调优应结合任务类型、评估集、延迟和成本，而不是固定套用。

**面试追问：**
- 为什么工具调用通常用低 temperature？
- 模型回答太啰嗦怎么控制？
- 同一个 Prompt 为什么多次输出不同？

**项目化回答提示：**  
说明你按任务分层配置参数，例如分类和 JSON 抽取用 `temperature=0`，生成类任务用 0.3-0.7。

### Q07：如何处理模型幻觉？

**高频程度：高**

**参考回答：**
- 幻觉来源包括模型知识过时、上下文不足、检索错误、Prompt 诱导、模型过度补全。
- RAG 场景要要求模型“基于给定上下文回答，不足则说明不知道”。
- 加入引用、证据片段、答案校验器和事实一致性评估可以降低幻觉。
- 对高风险场景，要引入规则校验、人工审核、权限边界和拒答策略。
- 幻觉不能完全消除，只能通过检索、约束、评估和兜底降低风险。

**面试追问：**
- RAG 后仍然幻觉怎么办？
- 如何评估答案是否忠实于上下文？
- 高风险业务能不能完全交给模型？

**项目化回答提示：**  
结合知识库问答，说你做了引用出处、未命中拒答、低置信度转人工、bad case 回流优化。

### Q08：什么是流式响应？为什么大模型应用常用流式输出？

**高频程度：中**

**参考回答：**
- 流式响应是模型边生成边返回 token 或文本片段，前端可以实时展示。
- 它不能减少总推理时间，但能显著降低用户感知首字延迟。
- 常见实现包括 SSE、WebSocket、HTTP chunked。
- 工程上要处理断连、取消生成、部分结果落库、敏感词流式拦截等问题。
- 对工具调用型 Agent，要区分“推理中间状态流”和“最终答案流”。

**面试追问：**
- SSE 和 WebSocket 怎么选？
- 流式输出如何做内容安全审核？
- 用户中途取消时服务端如何停止模型调用？

**项目化回答提示：**  
可以说你用 SSE 返回 token，同时把 trace、工具调用状态、最终答案分事件类型推给前端。

### Q09：为什么要做模型路由？如何设计？

**高频程度：中**

**参考回答：**
- 不同模型在成本、速度、上下文长度、推理能力、多模态能力上差异明显。
- 模型路由可以按任务类型、用户等级、输入长度、复杂度、失败重试、成本预算选择模型。
- 简单任务用小模型，复杂推理或代码任务用强模型，长文档任务用长上下文模型。
- 路由策略可以从规则开始，后续加入分类器、打分器或 bandit 策略。
- 必须保留统一接口、日志和效果评估，避免模型切换导致线上行为不可控。

**面试追问：**
- 如何判断一个请求是简单还是复杂？
- 模型降级时如何保证质量？
- 多模型结果不一致怎么办？

**项目化回答提示：**  
描述“规则路由 + 失败兜底 + 质量采样评估”的方案，例如低价值请求用便宜模型，高风险请求用强模型并加校验。

---

## 二、LangChain 高频题

### Q10：LangChain 的核心价值是什么？

**高频程度：高**

**参考回答：**
- LangChain 是大模型应用开发框架，提供模型调用、Prompt、输出解析、工具、检索、Agent 等抽象。
- 它解决的是“把 LLM 接入真实应用”的工程组合问题，而不是训练模型本身。
- 核心价值是统一不同模型接口、降低组件组合成本、方便快速构建 RAG 和 Agent 原型。
- 新版更强调 `Runnable`、LCEL、LangSmith、LangGraph 等生态能力。
- 生产环境不能盲目堆框架，需要理解每个组件的边界和可替换性。

**面试追问：**
- LangChain 和直接调用模型 API 有什么区别？
- LangChain 是否适合所有生产项目？
- LangChain、LlamaIndex、Dify 怎么选？

**项目化回答提示：**  
可以说你用 LangChain 快速搭建原型，但核心业务链路会抽象接口，避免强绑定框架。

### Q11：LangChain 里 ChatModel、Prompt、OutputParser、Retriever、Tool 分别是什么？

**高频程度：高**

**参考回答：**
- ChatModel 负责和大模型对话，例如 OpenAI、Qwen、DeepSeek、Claude 适配。
- Prompt 负责模板化组织输入，让变量、角色和约束可复用。
- OutputParser 负责把模型输出解析成字符串、JSON 或 Pydantic 对象。
- Retriever 负责从向量库、搜索引擎或数据库中检索相关上下文。
- Tool 负责封装外部能力，让 Agent 可以调用 API、数据库、文件系统等。

**面试追问：**
- Retriever 和 VectorStore 有什么区别？
- OutputParser 解析失败怎么办？
- Tool 的描述为什么重要？

**项目化回答提示：**  
把你的项目链路说成“Prompt 组织任务，Retriever 查知识，ChatModel 生成，Parser 校验结构，Tool 执行业务动作”。

### Q12：LCEL 是什么？为什么常用 `prompt | llm | parser`？

**高频程度：高**

**参考回答：**
- LCEL 是 LangChain Expression Language，用统一的 Runnable 协议组合链路。
- `prompt | llm | parser` 表示输入先进入 Prompt，再调用模型，最后解析输出。
- 它让链路结构更清晰，支持 invoke、batch、stream、async 等统一调用方式。
- LCEL 便于把子链复用、组合、并行和调试。
- 对复杂流程，LCEL 适合处理局部链路，整体编排可以交给 LangGraph。

**面试追问：**
- LCEL 和旧版 Chain 有什么区别？
- 如何做并行 Runnable？
- LCEL 如何接入 LangGraph 节点？

**项目化回答提示：**  
说明你把“意图识别链、答案生成链、结果校验链”分别封装成 LCEL 子链，再由工作流调度。

### Q13：LangChain 的 Memory 机制怎么理解？

**高频程度：高**

**参考回答：**
- LLM 本身无状态，Memory 的本质是把历史消息保存起来，并在下一轮调用时重新注入上下文。
- 短期记忆通常保存最近几轮对话，长期记忆可用向量库保存重要事实。
- 生产环境要按用户、会话、租户隔离记忆，不能用全局变量共享。
- 需要控制 Token 长度，常用窗口、摘要、重要性打分和语义检索。
- 新版实践更倾向 `RunnableWithMessageHistory` 或自行管理消息历史。

**面试追问：**
- 多用户并发时 Memory 如何隔离？
- 历史对话太长怎么办？
- 长期记忆如何避免旧信息干扰新任务？

**项目化回答提示：**  
可以说你按 `user_id + session_id` 存储对话，最近消息直接拼接，长期信息通过向量检索召回。

### Q14：LangChain 的 Agent 是如何工作的？

**高频程度：高**

**参考回答：**
- Agent 让模型不只是生成文本，而是根据目标选择工具、传参、观察结果并继续推理。
- 典型流程是“思考 -> 选择工具 -> 执行工具 -> 观察结果 -> 决定下一步”。
- 早期 Agent 常见 ReAct 文本格式，现在更多依赖模型原生 tool calling。
- Agent 需要工具 schema、执行器、循环控制、错误处理和最终答案生成。
- 生产中要限制最大步数、工具权限、超时时间和敏感操作审批。

**面试追问：**
- ReAct 和 Function Calling 有什么区别？
- Agent 为什么容易陷入循环？
- 如何限制 Agent 乱调用工具？

**项目化回答提示：**  
说你做过工具白名单、参数校验、最大迭代次数、失败反馈和人工确认节点。

### Q15：LangChain Tool 设计有哪些注意点？

**高频程度：高**

**参考回答：**
- 工具名称要短且语义明确，描述要说明适用场景和不适用场景。
- 参数 schema 要清晰，尽量使用枚举、必填字段、类型约束。
- 工具返回值要结构化，避免返回大段无关文本。
- 工具内部要有权限校验、超时、重试、幂等和异常包装。
- 对危险工具，如发邮件、下单、删除数据，要加入人工确认。

**面试追问：**
- 多个工具功能相近时模型怎么选？
- 工具调用参数错了怎么办？
- 工具结果很长如何返回给模型？

**项目化回答提示：**  
可以举例订单查询、知识库检索、工单创建等工具，说明 schema 和权限边界如何设计。

### Q16：LangChain 中 Retriever 和 RAG Chain 怎么组合？

**高频程度：高**

**参考回答：**
- Retriever 根据用户问题召回相关文档片段。
- RAG Chain 将问题、检索片段和 Prompt 组合后交给模型生成答案。
- 常见链路是 query rewrite -> retrieve -> rerank -> context compression -> answer generation。
- LangChain 可用 Runnable 把检索器和生成链组合起来。
- 需要在 Prompt 中要求模型基于上下文回答，并返回引用来源。

**面试追问：**
- 检索结果为空怎么办？
- 如何避免检索片段太长？
- RAG Chain 如何支持多轮对话？

**项目化回答提示：**  
结合知识库项目，说你先改写用户问题，再混合检索，重排后拼接 top chunks 生成答案。

### Q17：OutputParser 在生产中有什么坑？

**高频程度：中**

**参考回答：**
- 模型可能输出多余解释文本，导致 JSON 解析失败。
- 字段可能缺失、类型错误、枚举非法、嵌套结构不完整。
- Parser 只能校验格式，不保证事实正确。
- 自动修复会增加延迟和成本，不能无限重试。
- 最好结合模型原生结构化输出、schema 校验和业务规则校验。

**面试追问：**
- PydanticOutputParser 和 JsonOutputParser 怎么选？
- Parser 失败是重试模型还是走兜底？
- 如何统计格式失败率？

**项目化回答提示：**  
说你做了 `parse -> validate -> repair -> fallback` 四阶段，并把失败样例进入 Prompt 优化集。

### Q18：LangSmith 的作用是什么？

**高频程度：中**

**参考回答：**
- LangSmith 用于 LLM 应用的 trace、调试、评估和数据集管理。
- 它可以记录每次模型调用的输入、输出、耗时、Token、工具调用和中间链路。
- 对 RAG 和 Agent，trace 能帮助定位是检索错、工具错、Prompt 错还是模型错。
- 它也支持基于数据集的回归评估，避免 Prompt 修改后线上质量退化。
- 即使不用 LangSmith，也应自建类似可观测能力。

**面试追问：**
- 如何追踪一次 Agent 的完整执行过程？
- 如何做 Prompt 改动前后的 A/B 评估？
- 线上 bad case 如何回流？

**项目化回答提示：**  
可以说你记录 request_id、prompt_version、retrieved_docs、tool_calls、latency、cost 和用户反馈。

### Q19：LangChain 和 LlamaIndex 怎么选？

**高频程度：中**

**参考回答：**
- LangChain 更偏通用 LLM 应用编排，组件覆盖 Prompt、Tool、Agent、RAG、模型适配。
- LlamaIndex 更偏数据连接、索引构建、复杂检索和知识库问答。
- 如果核心是文档索引、GraphRAG、多源数据检索，LlamaIndex 很有优势。
- 如果核心是工具调用、Agent 编排、应用链路组合，LangChain 更通用。
- 实际项目也可以混用，例如 LlamaIndex 做索引，LangChain / LangGraph 做应用编排。

**面试追问：**
- Dify 和 LangChain 的区别是什么？
- 为什么生产项目不一定直接依赖框架？
- 如何避免框架锁定？

**项目化回答提示：**  
说你会按“数据层、编排层、服务层”解耦，底层框架可替换，业务接口保持稳定。

### Q20：LangChain 在生产落地时有哪些风险？

**高频程度：中**

**参考回答：**
- 抽象层较多，版本升级可能带来 API 变动和调试成本。
- 如果不理解底层逻辑，容易把简单问题做复杂。
- 复杂 Agent 链路中，错误定位和状态管理会变难。
- 生产项目需要补充权限、监控、限流、缓存、评估和审计，框架本身不能全包。
- 关键链路最好封装自己的接口，避免业务代码散落在框架调用里。

**面试追问：**
- 你会在哪些地方不用 LangChain？
- 框架升级导致接口变化怎么办？
- 如何做框架无关的架构设计？

**项目化回答提示：**  
强调“原型用框架提速，生产做接口隔离”，模型、向量库、工具执行器都通过自定义适配层接入。

---

## 三、LangGraph 高频题

### Q21：LangGraph 解决了什么问题？

**高频程度：高**

**参考回答：**
- LangGraph 用状态图来编排复杂 LLM 应用，解决线性 Chain 难以表达分支、循环和状态的问题。
- 它适合 Agent、多步骤工作流、人机协作、可恢复执行和多 Agent 协作。
- 核心抽象是 State、Node、Edge、条件边、Checkpoint。
- 相比普通 LangChain Agent，LangGraph 更强调可控流程和显式状态。
- 生产中可以用它把不可控 Agent 拆成可观察、可中断、可恢复的图。

**面试追问：**
- LangChain 和 LangGraph 是替代关系吗？
- 什么场景不需要 LangGraph？
- LangGraph 为什么更适合复杂 Agent？

**项目化回答提示：**  
说你把“意图识别、检索、工具调用、审核、生成、人工确认”拆成节点，用状态图控制执行路径。

### Q22：StateGraph 中 State、Node、Edge 分别是什么？

**高频程度：高**

**参考回答：**
- State 是全局共享状态，保存用户问题、中间结果、消息、工具结果、重试次数等。
- Node 是执行单元，通常是普通函数或 Runnable，读取 State 并返回局部更新。
- Edge 定义节点之间的流转关系，可以是固定边或条件边。
- 节点一般不返回完整状态，而是返回要更新的字段。
- 通过显式 State，LangGraph 可以支持调试、断点恢复和人机协作。

**面试追问：**
- State 用 TypedDict 还是 Pydantic？
- 多个节点更新同一个字段怎么办？
- Node 里能不能直接改全局变量？

**项目化回答提示：**  
可以举例 State 包含 `messages`、`intent`、`retrieved_docs`、`tool_result`、`final_answer`、`retry_count`。

### Q23：LangGraph 条件边如何工作？

**高频程度：高**

**参考回答：**
- 条件边通过路由函数读取当前 State，并返回下一步节点名或结束标记。
- 它常用于意图分流、是否调用工具、是否继续循环、是否转人工。
- 路由函数应尽量确定、可测试，避免完全依赖自由文本判断。
- 条件边要设计默认分支，防止未知状态导致执行失败。
- 循环场景要加入最大步数或最大重试次数。

**面试追问：**
- 如何防止条件边进入死循环？
- 路由判断应该用规则还是模型？
- 条件边返回非法节点怎么办？

**项目化回答提示：**  
说你用规则优先处理明确意图，模糊场景交给分类模型，并设置默认兜底节点。

### Q24：LangGraph 如何实现循环与反思？

**高频程度：高**

**参考回答：**
- 循环通过条件边返回前序节点实现，例如“生成 -> 评估 -> 不通过则重写”。
- 反思节点可以检查答案是否完整、是否引用证据、是否满足格式。
- 循环必须记录 `retry_count`，超过阈值后降级或转人工。
- 评估标准要具体，例如格式正确、引用覆盖、答案相关、无敏感内容。
- 循环会增加延迟和成本，需要按任务风险决定是否启用。

**面试追问：**
- Reflection 会不会让模型越改越差？
- 最大重试次数怎么定？
- 反思节点用同一个模型还是小模型？

**项目化回答提示：**  
描述你做过“答案生成 -> LLM Judge / 规则校验 -> 不合格重写 -> 仍失败转人工”的闭环。

### Q25：Checkpoint 在 LangGraph 中有什么作用？

**高频程度：高**

**参考回答：**
- Checkpoint 用于保存图执行过程中的 State，使任务可暂停、恢复、回放。
- 它是长流程 Agent、人机审批、断点续跑和多轮会话的重要基础。
- 每个会话通常通过 `thread_id` 区分。
- 内存型 checkpointer 适合开发调试，生产应使用持久化存储。
- Checkpoint 也能帮助调试每一步状态变化，定位问题节点。

**面试追问：**
- Checkpoint 和 Memory 有什么区别？
- 生产环境 checkpointer 存哪里？
- 如何处理状态 schema 版本升级？

**项目化回答提示：**  
可以说你把工作流状态持久化到数据库，用户审批后用同一个会话 ID 恢复执行。

### Q26：LangGraph 的 interrupt 机制适合什么场景？

**高频程度：高**

**参考回答：**
- interrupt 用于在节点前或节点后暂停图执行，等待人工输入或确认。
- 适合发邮件、下单、转账、删除数据、发布内容等高风险操作。
- `interrupt_before` 适合操作执行前审批，`interrupt_after` 适合生成结果后审核。
- 中断必须和 checkpoint 配合，否则无法可靠恢复。
- 人工输入也应该写回 State，作为后续节点的上下文。

**面试追问：**
- 人工审批超时怎么办？
- 中断恢复后如何保证幂等？
- 哪些操作必须人工确认？

**项目化回答提示：**  
描述“Agent 生成工单处理方案，执行前暂停，运营确认后再调用业务 API”的设计。

### Q27：LangGraph 如何做多 Agent？

**高频程度：高**

**参考回答：**
- 可以把每个 Agent 建成一个节点，也可以把每个 Agent 建成一个子图。
- 常见模式有 supervisor 调度、固定 sequence、handoff、swarm、peer-to-peer。
- Supervisor 模式由主管节点决定下一位 Agent，适合任务动态分配。
- Sequence 模式适合固定流程，如写作、审稿、润色。
- 多 Agent 要控制消息协议、共享状态、终止条件和冲突解决。

**面试追问：**
- 多 Agent 一定比单 Agent 好吗？
- Supervisor 失误怎么办？
- Agent 之间如何传递结果？

**项目化回答提示：**  
可以说你会先用单 Agent + 工具，只有任务天然多角色、可并行或需互审时才上多 Agent。

### Q28：LangGraph 和工作流引擎有什么区别？

**高频程度：中**

**参考回答：**
- 传统工作流引擎强调确定性任务编排，LangGraph 面向 LLM 参与的状态化流程。
- LangGraph 的节点可以是模型调用、工具调用、检索、人工审批、子图。
- 它支持基于模型或状态的动态路由，适合不完全确定的任务。
- 传统 BPMN 更适合强规则业务流程，LangGraph 更适合 Agent 和 LLM 工作流。
- 生产中两者可以结合：业务审批走工作流，智能决策走 LangGraph。

**面试追问：**
- LangGraph 能替代 Airflow 吗？
- 为什么不用普通 if-else 写流程？
- 图编排如何可视化？

**项目化回答提示：**  
说你会把 LLM 相关的动态决策放 LangGraph，强事务业务仍由后端服务和数据库保证。

### Q29：LangGraph 中如何设计 State schema？

**高频程度：中**

**参考回答：**
- State 应只保存跨节点需要共享的数据，不要把所有临时变量都塞进去。
- 字段命名要清楚，区分用户输入、检索结果、工具结果、最终答案、错误信息。
- 对列表消息字段要考虑 reducer，例如追加而不是覆盖。
- 高风险字段要包含来源、置信度、时间戳，方便追踪。
- schema 要考虑版本演进，避免 checkpoint 中旧状态无法恢复。

**面试追问：**
- State 过大有什么问题？
- 如何处理并行节点写入冲突？
- Reducer 是什么？

**项目化回答提示：**  
可以说你的 State 会分为 request、context、intermediate、output、control 五类字段。

### Q30：LangGraph 预置 ReAct Agent 和自定义 StateGraph 怎么选？

**高频程度：中**

**参考回答：**
- 预置 ReAct Agent 适合快速构建“模型 + 工具”的标准 Agent。
- 自定义 StateGraph 适合有业务分支、审核、复杂状态和人机协作的场景。
- 如果只是工具调用问答，可以先用 prebuilt。
- 如果需要可控流程、审批、失败恢复、多节点评估，应使用自定义图。
- 工程上可以先用 prebuilt 验证，再逐步拆成显式节点。

**面试追问：**
- ReAct Agent 的终止条件是什么？
- 自定义图会不会开发成本太高？
- 如何把已有 Chain 接进节点？

**项目化回答提示：**  
说你 POC 阶段用预置 Agent，生产阶段把意图识别、工具执行和风控审核拆成自定义节点。

### Q31：如何调试 LangGraph 流程？

**高频程度：中**

**参考回答：**
- 为每个节点记录输入 State 摘要、输出更新、耗时、模型参数和异常。
- 使用 checkpoint 回放某个线程的执行过程。
- 对条件边记录路由原因和下一节点。
- 对模型节点保存 prompt_version、上下文片段和输出。
- 建立小规模测试数据集，覆盖不同路径、失败路径和循环路径。

**面试追问：**
- 如何定位是哪个节点导致结果错误？
- 如何测试条件边？
- 如何观察 Agent 的工具调用链？

**项目化回答提示：**  
可以说你给每次执行分配 trace_id，节点级日志进入可观测平台，bad case 可回放复现。

---

## 四、RAG 高频题

### Q32：请完整介绍 RAG 的基本流程。

**高频程度：高**

**参考回答：**
- 离线阶段：文档加载、清洗、切分、embedding、写入向量库或检索索引。
- 在线阶段：用户问题改写、query embedding、召回、重排、上下文拼接、LLM 生成。
- 生成阶段要要求模型基于上下文回答，并输出引用或证据。
- 评估阶段要分别评估检索质量和生成质量。
- RAG 的目标是让模型利用外部知识，降低幻觉并支持私有知识更新。

**面试追问：**
- RAG 为什么能缓解幻觉？
- 离线索引和在线检索分别有哪些优化点？
- RAG 失败通常失败在哪里？

**项目化回答提示：**  
按“文档入库链路 + 在线问答链路 + 评估迭代链路”讲，最容易显得工程完整。

### Q33：文档切分 chunk size 怎么选？

**高频程度：高**

**参考回答：**
- chunk size 要平衡语义完整性和检索精度，太小会丢上下文，太大噪声多且占 Token。
- 常见策略是按标题、段落、句子、语义边界切分，而不是机械按字符切。
- 技术文档、合同、论文可保留层级标题作为 metadata。
- chunk overlap 可以缓解边界信息丢失，但过大会造成重复召回。
- 最终应通过 Recall@K、答案质量和人工 bad case 调整。

**面试追问：**
- chunk overlap 多大合适？
- 表格和代码怎么切？
- PDF 解析后的页眉页脚怎么处理？

**项目化回答提示：**  
说你按文档类型制定切分策略：Markdown 按标题，FAQ 按问答对，PDF 先清洗页眉页脚再按段落切。

### Q34：Embedding 模型如何选择？

**高频程度：高**

**参考回答：**
- 选择 embedding 要看语言、领域、维度、速度、成本、上下文长度和是否支持稀疏 / 多向量。
- 中文业务优先测试中文语义检索表现，不能只看英文 benchmark。
- 通用模型适合冷启动，专业领域可考虑微调或构造领域词典增强。
- 维度越高不一定越好，还要看向量库性能和存储成本。
- 需要用自己的查询-文档评估集比较 Recall@K、MRR、NDCG。

**面试追问：**
- Query 和 Document 是否用同一个 embedding？
- 领域术语召回差怎么办？
- embedding 模型升级如何重建索引？

**项目化回答提示：**  
说你用业务真实问题构造评测集，对比多个 embedding，在效果、延迟、成本之间选型。

### Q35：向量检索、关键词检索和混合检索有什么区别？

**高频程度：高**

**参考回答：**
- 向量检索擅长语义相似，能处理同义表达，但对精确术语、编号、时间、代码不一定稳定。
- 关键词检索如 BM25 擅长精确匹配，适合专有名词、错误码、法规条款。
- 混合检索结合稠密向量和稀疏关键词，通常比单一路线更稳。
- 混合结果需要归一化、融合排序或交给 reranker。
- 企业知识库常用“BM25 + 向量召回 + rerank”的链路。

**面试追问：**
- BM25 为什么在 RAG 里仍然重要？
- 两路召回分数如何融合？
- 什么时候只用关键词检索？

**项目化回答提示：**  
举例用户查“ERR_1024”或产品型号时，关键词检索更可靠；查含义相近问题时，向量更可靠。

### Q36：Reranker 的作用是什么？

**高频程度：高**

**参考回答：**
- Reranker 对初召回结果进行精排，提高 top 几个上下文的相关性。
- 初召回通常追求召回率，rerank 追求排序准确性。
- Cross-encoder reranker 效果通常好于纯向量相似度，但延迟更高。
- 可以先召回 top 50，再 rerank 到 top 5-10 给模型。
- Reranker 对长文档、多跳问题、语义相近干扰项特别有价值。

**面试追问：**
- reranker 为什么比 embedding 慢？
- top_k 设置多少合适？
- reranker 后还需要去重吗？

**项目化回答提示：**  
说你用召回扩大候选集，用 rerank 提升上下文质量，并通过命中率和答案满意度验证收益。

### Q37：Query Rewrite 在 RAG 中有什么作用？

**高频程度：高**

**参考回答：**
- Query Rewrite 把用户口语化、上下文依赖或多轮追问改写成适合检索的完整问题。
- 它能补全省略信息、统一术语、拆分复杂问题、生成多路查询。
- 多轮场景中尤其重要，因为用户常说“那这个怎么配置？”。
- 改写不能改变用户原意，需要保留原问题用于最终回答。
- 可以用规则、小模型或 LLM 实现，并评估改写后的检索提升。

**面试追问：**
- Query Rewrite 会不会引入错误？
- 多查询检索如何合并结果？
- 问题很短时是否一定要改写？

**项目化回答提示：**  
描述“历史对话 + 当前问题 -> standalone question -> hybrid retrieve”的做法。

### Q38：RAG 检索结果为空怎么办？

**高频程度：高**

**参考回答：**
- 首先区分是真无答案、索引缺失、解析失败、切分不合理还是检索策略问题。
- 在线策略可返回“不知道”，并提示用户补充信息或扩大搜索范围。
- 可以降级到关键词检索、互联网搜索、人工客服或创建待补充知识。
- 记录未命中 query，用于后续补充知识库和优化切分。
- 不应让模型凭空编造答案。

**面试追问：**
- 如何判断是低置信度？
- 空召回是否可以直接让 LLM 回答？
- 未命中日志如何回流知识库？

**项目化回答提示：**  
说你设置相似度阈值和 rerank 分数阈值，低于阈值拒答或转人工，并进入知识运营后台。

### Q39：RAG 召回了错误文档怎么办？

**高频程度：高**

**参考回答：**
- 错误召回可能来自 query 表达差、embedding 不适配、chunk 噪声、metadata 过滤缺失。
- 可通过 query rewrite、混合检索、metadata filter、rerank、去重和上下文压缩优化。
- 对权限、产品线、版本号等场景，metadata 过滤比语义检索更关键。
- 要建立检索评估集，单独评估 Recall@K、MRR、NDCG。
- bad case 应标注“正确文档是什么”，用于调参或微调 embedding/reranker。

**面试追问：**
- 召回结果相关但答非所问怎么办？
- metadata 如何设计？
- 是否需要微调 embedding？

**项目化回答提示：**  
强调你会把“检索错误”和“生成错误”分开定位，而不是只看最终答案。

### Q40：RAG 答案如何加引用？

**高频程度：高**

**参考回答：**
- 入库时要为每个 chunk 保存 doc_id、标题、页码、URL、段落号等 metadata。
- 生成时把上下文编号，并要求模型回答时引用对应编号。
- 最终返回时把编号映射成文档来源、页码或链接。
- 引用只能说明答案依据，不代表答案一定正确，还要检查引用是否真的支持结论。
- 对合规场景，要保留引用片段和版本号，方便审计。

**面试追问：**
- 模型乱引用怎么办？
- 引用粒度是 chunk 还是文档？
- 文档更新后历史引用如何处理？

**项目化回答提示：**  
说你把 chunk_id、source_url、page、version 存入 metadata，答案侧返回可点击出处。

### Q41：如何评估 RAG 系统？

**高频程度：高**

**参考回答：**
- 分层评估：检索评估、生成评估、端到端业务评估。
- 检索指标包括 Recall@K、Precision@K、MRR、NDCG、Hit Rate。
- 生成指标包括 Faithfulness、Answer Relevance、Correctness、引用准确率。
- 业务指标包括解决率、转人工率、用户满意度、平均延迟、单次成本。
- 评估集要来自真实问题和 bad case，而不是只用合成样例。

**面试追问：**
- Faithfulness 怎么评估？
- 没有标准答案怎么办？
- RAGAS、LangSmith、TruLens 分别能做什么？

**项目化回答提示：**  
讲你如何构建“问题、标准答案、正确文档、难度标签”的评估集，并定期回归。

### Q42：RAG 中 Faithfulness 为什么重要？

**高频程度：高**

**参考回答：**
- Faithfulness 衡量答案是否被检索上下文支持，是 RAG 区别于纯生成的核心指标。
- 如果答案正确但不来自上下文，在企业知识库中仍可能不可审计。
- 低 Faithfulness 表示模型可能在编造、过度推理或引用无关内容。
- 可以用 LLM Judge、NLI 模型或规则检查答案句子是否被证据支持。
- 对法律、金融、医疗、政策类场景，Faithfulness 比流畅度更重要。

**面试追问：**
- 答案部分来自上下文、部分来自常识怎么处理？
- 如何做句子级证据对齐？
- 只看最终准确率够不够？

**项目化回答提示：**  
说你会要求答案每个关键结论都能映射到证据片段，不能映射则删除或标注不确定。

### Q43：什么是 Advanced RAG？

**高频程度：高**

**参考回答：**
- Advanced RAG 是在基础检索生成上加入查询优化、混合检索、重排、上下文压缩、反思和评估等增强模块。
- 常见增强包括 query rewrite、multi-query、HyDE、metadata filter、rerank、self-RAG。
- 它解决 naive RAG 召回不准、上下文噪声大、生成不忠实的问题。
- Advanced RAG 不等于模块越多越好，要按 bad case 定位加模块。
- 每新增模块都要评估收益、延迟和成本。

**面试追问：**
- HyDE 是什么？
- Multi-query 会增加什么成本？
- 如何判断该加 rerank 还是改切分？

**项目化回答提示：**  
可以说你从 naive RAG 起步，发现错召回后逐步加 query rewrite、混合召回、rerank 和引用校验。

### Q44：什么是 GraphRAG？解决什么问题？

**高频程度：进阶**

**参考回答：**
- GraphRAG 将文本中的实体和关系抽取成图结构，再结合图检索和摘要生成答案。
- 它适合全局性问题、多跳推理、实体关系密集的知识库。
- 相比传统 chunk 检索，GraphRAG 更能保留实体关系和社区结构。
- 难点是实体抽取质量、实体对齐、图更新成本、社区摘要和查询路由。
- 不适合所有场景，普通 FAQ 或短文档知识库未必需要。

**面试追问：**
- GraphRAG 和知识图谱问答有什么关系？
- 图谱如何增量更新？
- GraphRAG 成本为什么高？

**项目化回答提示：**  
如果没做过，可回答“了解并评估过，当前项目先用混合检索；当出现多跳关系问题时再引入图谱增强”。

### Q45：Agentic RAG 是什么？

**高频程度：进阶**

**参考回答：**
- Agentic RAG 让 Agent 自主决定是否检索、检索哪些源、是否重查、是否调用工具。
- 它适合问题复杂、数据源多、检索策略不固定的场景。
- 普通 RAG 是固定链路，Agentic RAG 是动态规划和执行。
- 优点是灵活，缺点是成本、延迟和不可控性上升。
- 需要限制最大步骤、工具权限、检索范围和终止条件。

**面试追问：**
- Agentic RAG 和 Advanced RAG 区别是什么？
- 什么时候不该用 Agentic RAG？
- 如何评估 Agentic RAG？

**项目化回答提示：**  
举例“用户问题可能需要查文档、查数据库、查实时接口”，由 Agent 根据意图选择检索源。

### Q46：多轮对话 RAG 如何处理历史上下文？

**高频程度：高**

**参考回答：**
- 多轮问题常依赖前文，因此要先把当前问题改写成独立问题。
- 历史对话不能全部拼接，应保留最近对话、摘要和关键槽位。
- 检索时通常用改写后的问题，回答时可结合原始问题和历史上下文。
- 会话状态要按用户和 session 隔离。
- 长期用户偏好可作为个性化记忆，但要注意隐私和过期。

**面试追问：**
- 用户说“这个怎么配置”怎么检索？
- 历史对话里有错误信息怎么办？
- 多轮 RAG 如何评估？

**项目化回答提示：**  
说你用“history-aware query rewrite”，并保存关键实体如产品、版本、地区、角色。

### Q47：RAG 中如何处理权限和数据隔离？

**高频程度：高**

**参考回答：**
- 入库时为文档和 chunk 保存租户、部门、角色、权限标签。
- 检索时必须先做权限过滤，再做语义召回或在召回阶段加入 filter。
- 不能只依赖 Prompt 告诉模型“不要回答无权限内容”。
- 返回引用时也要检查来源是否对用户可见。
- 日志和评估数据要脱敏，避免二次泄露。

**面试追问：**
- 向量库如何做权限过滤？
- 用户越权问到敏感文档怎么办？
- 多租户知识库如何设计？

**项目化回答提示：**  
强调权限在检索层和业务层强制执行，模型只看到用户有权访问的上下文。

### Q48：长文档问答如何优化？

**高频程度：中**

**参考回答：**
- 长文档要先解析结构，保留标题、章节、页码和表格信息。
- 可采用层级索引：先检索章节摘要，再检索具体片段。
- 对全文总结类问题，可以使用 map-reduce、refine 或 GraphRAG 思路。
- 对局部事实问题，用精细 chunk + metadata filter + rerank。
- 长上下文模型可以辅助，但仍需控制成本和证据定位。

**面试追问：**
- 长上下文模型能否直接塞全文？
- 总结类问题和事实类问题检索策略有什么不同？
- 表格如何进入 RAG？

**项目化回答提示：**  
说你按文档目录构建父子 chunk，父 chunk 做摘要，子 chunk 做证据引用。

### Q49：RAG 知识库如何持续更新？

**高频程度：高**

**参考回答：**
- 建立文档入库流水线，支持新增、更新、删除和版本管理。
- 文档更新后要重新解析、切分、embedding，并标记旧 chunk 失效。
- 对高频知识可做增量索引，对大规模变更可离线重建。
- 需要记录文档来源、更新时间、版本号、处理状态和失败原因。
- 线上要避免用户检索到新旧冲突内容。

**面试追问：**
- 删除文档后向量库如何同步删除？
- embedding 模型升级怎么办？
- 如何处理同一知识多个版本？

**项目化回答提示：**  
描述“文档变更事件 -> 入库任务队列 -> 状态机处理 -> 索引切换 -> 失败告警”的工程链路。

---

## 五、Agent / Multi-Agent / Function Calling / MCP

### Q50：如何定义一个 LLM Agent？

**高频程度：高**

**参考回答：**
- LLM Agent 是以大模型为核心，具备感知输入、规划步骤、调用工具、观察结果和完成目标能力的系统。
- 它通常包含模型、Prompt、工具、记忆、规划器、执行器、状态和评估反馈。
- 与普通聊天机器人不同，Agent 可以主动选择行动，而不是只生成回复。
- 真实生产 Agent 往往不是完全自主，而是“可控自主”。
- 安全边界、工具权限、终止条件和可观测性是落地关键。

**面试追问：**
- Agent 和 Workflow 有什么区别？
- Agent 是否一定需要记忆？
- 什么时候不应该用 Agent？

**项目化回答提示：**  
用一句话概括你的项目：例如“客服 Agent 根据用户意图调用订单、知识库和工单工具，最终给出可追踪答复”。

### Q51：ReAct 模式是什么？

**高频程度：高**

**参考回答：**
- ReAct 是 Reasoning + Acting，让模型交替进行推理和行动。
- 模型先判断下一步要做什么，再调用工具，观察结果后继续决策。
- 它适合需要多步搜索、工具调用和中间观察的任务。
- 早期 ReAct 常用文本格式，现代实现常结合 tool calling。
- 缺点是容易循环、成本高、受模型推理稳定性影响。

**面试追问：**
- ReAct 和 Plan-and-Execute 有什么区别？
- ReAct 如何设置停止条件？
- ReAct 中间推理是否应该展示给用户？

**项目化回答提示：**  
说你使用 ReAct 思路，但在工程上隐藏内部推理，只展示工具状态和最终答案。

### Q52：Plan-and-Execute Agent 是什么？

**高频程度：中**

**参考回答：**
- Plan-and-Execute 先由模型生成完整计划，再逐步执行计划。
- 优点是全局目标更清晰，适合复杂任务拆解。
- 缺点是计划可能过时，执行过程中需要重规划机制。
- 它比 ReAct 更结构化，但动态适应性不一定更好。
- 常用于研究、代码生成、旅行规划、复杂办公自动化。

**面试追问：**
- 什么时候需要重规划？
- 计划如何校验？
- 计划步骤失败怎么办？

**项目化回答提示：**  
结合项目说“先让 Planner 拆任务，再由 Executor 调工具；每步失败后回到 Planner 重新规划”。

### Q53：Function Calling 和 Tool Calling 的本质是什么？

**高频程度：高**

**参考回答：**
- Function Calling 是模型根据工具 schema 生成结构化调用意图和参数。
- 模型本身不执行函数，真正执行由应用后端完成。
- Tool Calling 是更通用的说法，可以包括函数、API、检索器、数据库、代码执行器等。
- 它把自然语言决策和确定性工具执行结合起来。
- 工程重点是 schema 设计、参数校验、权限控制、执行结果反馈。

**面试追问：**
- Function Calling 和直接让模型输出 JSON 有什么区别？
- 模型调用了错误工具怎么办？
- 函数执行失败如何反馈给模型？

**项目化回答提示：**  
说你会将工具调用拆成“模型选择 -> 后端校验 -> 执行 -> 结果摘要 -> 模型生成最终答复”。

### Q54：如何设计工具调用的参数 schema？

**高频程度：高**

**参考回答：**
- 参数名称要与业务语义一致，避免缩写和歧义。
- 类型要明确，尽量使用枚举、范围、正则或必填约束。
- 对高风险参数要二次确认，例如金额、收件人、删除对象。
- schema 说明中要写清何时使用、何时不要使用。
- 参数校验失败后应返回可理解的错误，让模型有机会修正。

**面试追问：**
- 可选参数太多怎么办？
- 如何防止 SQL 注入类工具参数风险？
- 工具 schema 变更如何兼容？

**项目化回答提示：**  
举例订单查询工具只允许传 `order_id` 或 `phone_last4`，并在后端做权限校验。

### Q55：Agent 工具调用失败怎么办？

**高频程度：高**

**参考回答：**
- 失败类型包括参数错误、权限不足、超时、第三方 API 异常、空结果、结果冲突。
- 参数错误可反馈给模型修正；系统异常可重试或降级；权限问题应拒绝。
- 设置最大重试次数，避免 Agent 无限尝试。
- 工具返回错误要结构化，包含错误码、可恢复性和建议动作。
- 关键失败要记录 trace，进入 bad case 分析。

**面试追问：**
- 什么错误可以重试，什么错误不能重试？
- 工具空结果时是否继续问用户？
- 重试会不会造成重复下单？

**项目化回答提示：**  
说你区分 retryable 和 non-retryable 错误，对写操作加幂等 key 和人工确认。

### Q56：Agent 如何避免无限循环？

**高频程度：高**

**参考回答：**
- 设置最大步数、最大工具调用次数、最大重试次数和总超时时间。
- 检测重复调用同一工具、重复问题、状态无变化等循环信号。
- 给模型明确终止条件，例如信息足够则回答，不足则澄清或拒答。
- 对循环失败要降级到人工、固定回答或失败说明。
- LangGraph 中可用 State 记录计数并通过条件边结束。

**面试追问：**
- 如何判断 Agent 没有取得进展？
- 最大步数设多少合适？
- 循环中断后如何给用户解释？

**项目化回答提示：**  
可以说你记录 `step_count`、`last_tool`、`last_observation_hash`，重复时直接走兜底节点。

### Q57：Agent 的 Memory 如何设计？

**高频程度：高**

**参考回答：**
- 短期记忆保存当前会话上下文，长期记忆保存用户偏好、关键事实和历史任务结果。
- 短期记忆可用消息窗口或摘要，长期记忆可用向量库或结构化数据库。
- 记忆写入要有筛选，不是所有对话都值得长期保存。
- 记忆读取要按相关性、时效性、权限和置信度过滤。
- 支持遗忘、更新、用户删除和隐私合规。

**面试追问：**
- 长期记忆如何避免过期？
- 用户不希望被记住怎么办？
- 记忆和 RAG 有什么区别？

**项目化回答提示：**  
说你把会话历史、用户画像、业务记录分开存储，调用时按任务需要注入。

### Q58：Multi-Agent 有哪些典型架构？

**高频程度：高**

**参考回答：**
- Supervisor：主管 Agent 负责分配任务和汇总结果。
- Sequence：多个 Agent 按固定顺序处理，如写作、审稿、润色。
- Debate / Critic：多个 Agent 给出方案，评审 Agent 选择或融合。
- Peer-to-peer：Agent 之间通过消息总线或共享状态协作。
- Swarm / Handoff：Agent 根据职责把任务交接给更合适的 Agent。

**面试追问：**
- 多 Agent 相比单 Agent 的收益是什么？
- 多 Agent 的成本和延迟如何控制？
- Agent 之间意见冲突怎么办？

**项目化回答提示：**  
建议回答“不为了多 Agent 而多 Agent”，只有在职责明显不同、需要互审或并行时使用。

### Q59：Supervisor Agent 如何设计？

**高频程度：高**

**参考回答：**
- Supervisor 负责理解目标、选择下一个 Agent、判断是否结束和汇总最终结果。
- 子 Agent 要职责单一，输入输出格式统一。
- Supervisor 路由应尽量结构化，避免自然语言随意判断。
- 必须设置最大轮次、默认兜底和异常处理。
- 可引入审计日志记录每次调度原因。

**面试追问：**
- Supervisor 自己判断错了怎么办？
- 如何防止一直调同一个 Agent？
- 子 Agent 结果不一致如何融合？

**项目化回答提示：**  
举例“需求分析 Agent、检索 Agent、代码 Agent、评审 Agent”，Supervisor 根据任务状态调度。

### Q60：Agent 和 Workflow 如何取舍？

**高频程度：高**

**参考回答：**
- Workflow 适合路径明确、规则稳定、结果可预测的业务流程。
- Agent 适合目标明确但路径不固定，需要模型自主决策的任务。
- 生产中常用混合模式：关键业务流程用 Workflow，局部智能决策用 Agent。
- Agent 灵活但不稳定，Workflow 稳定但不够自适应。
- 对高风险动作，应让 Workflow 控制权限和审批，而不是完全交给 Agent。

**面试追问：**
- 客服系统更适合 Agent 还是 Workflow？
- 如何把 Agent 嵌入现有业务流程？
- Agent 出错是否会影响主流程？

**项目化回答提示：**  
说你会把可确定的订单状态查询写成工具，把意图判断和回复组织交给 Agent。

### Q61：MCP 是什么？解决什么问题？

**高频程度：高**

**参考回答：**
- MCP 是 Model Context Protocol，用标准协议把外部工具、资源和上下文暴露给模型应用。
- 它解决的是 Agent 调用外部系统时的工具发现、协议统一和集成成本问题。
- MCP Server 提供工具和资源，MCP Client 负责连接和调用。
- 它更像“Agent 连接工具的标准接口”，不是模型训练方法。
- 适合文件系统、数据库、浏览器、企业内部系统等工具接入。

**面试追问：**
- MCP 和普通 REST API 有什么区别？
- MCP 和 A2A 有什么区别？
- MCP Server 如何做权限控制？

**项目化回答提示：**  
可以说你会把数据库查询、文档读取、报表生成封装成 MCP Server，让 Agent 通过统一协议调用。

### Q62：MCP 和 A2A 有什么区别？

**高频程度：进阶**

**参考回答：**
- MCP 更关注 Agent / 模型应用如何调用工具和外部资源。
- A2A 更关注 Agent 与 Agent 之间如何互操作和协作。
- 简单理解：MCP 是 Agent-to-Tool，A2A 是 Agent-to-Agent。
- MCP 的核心对象是工具、资源、上下文；A2A 的核心对象是 Agent 能力描述和任务协作。
- 两者可以互补，一个 Agent 通过 A2A 接收任务，再通过 MCP 调工具。

**面试追问：**
- 什么时候需要 A2A？
- MCP 能不能实现多 Agent 通信？
- 企业内部 Agent 协作是否一定要用 A2A？

**项目化回答提示：**  
如果没用过 A2A，可以坦诚说项目当前主要是 MCP / 工具协议，A2A 仍在评估。

### Q63：如何评估一个 Agent？

**高频程度：高**

**参考回答：**
- 任务成功率是核心指标，即 Agent 是否完成用户目标。
- 工具调用质量包括工具选择准确率、参数正确率、调用次数和失败恢复率。
- 过程指标包括步数、延迟、成本、循环率、转人工率。
- 结果指标包括答案正确性、忠实度、安全性、用户满意度。
- 评估要覆盖正常任务、异常任务、对抗输入和权限边界。

**面试追问：**
- Agent 成功率怎么定义？
- 如何评估中间步骤？
- 没有标准答案时怎么办？

**项目化回答提示：**  
说你把任务拆成“意图识别、工具调用、结果生成、用户反馈”四层指标，而不是只看最终答案。

### Q64：如何处理 Agent 的安全风险？

**高频程度：高**

**参考回答：**
- 风险包括越权调用、数据泄露、Prompt Injection、工具滥用、错误执行高风险动作。
- 权限必须在系统层控制，不能只靠模型自觉。
- 工具调用要有白名单、参数校验、敏感操作审批和审计日志。
- 检索上下文要做权限过滤，用户输入要做注入检测和指令隔离。
- 高风险行业要设置拒答、人工审核和输出合规检查。

**面试追问：**
- Prompt Injection 怎么防？
- Agent 读到恶意网页指令怎么办？
- 如何防止工具越权？

**项目化回答提示：**  
强调“模型不能直接接触权限凭证，所有工具调用都走后端鉴权和审计”。

### Q65：Agent 项目如何做 bad case 迭代？

**高频程度：高**

**参考回答：**
- 记录完整 trace，包括用户输入、Prompt、检索结果、工具调用、模型输出和反馈。
- 对 bad case 分类：意图错、检索错、工具错、生成错、安全错、产品预期错。
- 按类型分别优化 Prompt、知识库、工具 schema、路由策略或业务规则。
- 形成回归评估集，防止修一个坏三个。
- 对高频 bad case 优先修，对低频高风险 case 设置兜底。

**面试追问：**
- bad case 如何标注？
- 是否做在线学习？
- Prompt 改动如何验证没有副作用？

**项目化回答提示：**  
说你建立 bad case 看板，每周按影响面和修复成本排序，并进入自动回归测试。

---

## 六、工程化、评估、性能、安全与上线迭代

### Q66：大模型应用上线前要做哪些工程准备？

**高频程度：高**

**参考回答：**
- 接口层：超时、重试、幂等、限流、鉴权、参数校验。
- 模型层：模型路由、降级、成本预算、上下文长度控制。
- 数据层：知识库版本、权限隔离、脱敏和审计。
- 观测层：trace、日志、指标、告警、用户反馈。
- 评估层：离线评估集、回归测试、灰度发布和人工验收。

**面试追问：**
- 如何做灰度发布？
- 模型服务不可用怎么办？
- 如何统计单次请求成本？

**项目化回答提示：**  
回答时按“稳定性、效果、安全、成本、可观测”五个维度展开。

### Q67：如何设计 LLM 应用的日志和 trace？

**高频程度：高**

**参考回答：**
- 每次请求要有 request_id / trace_id，串起模型、检索、工具和后处理。
- 记录 prompt_version、model_name、temperature、token、latency、cost。
- RAG 要记录 query rewrite、召回文档、rerank 分数和引用。
- Agent 要记录每步工具调用、参数、结果、错误和路由原因。
- 日志要脱敏，避免保存密钥、隐私和未授权文档内容。

**面试追问：**
- 线上用户投诉回答错了怎么排查？
- 是否保存完整 Prompt？
- 日志如何兼顾调试和隐私？

**项目化回答提示：**  
说你通过 trace 回放定位问题来源，并把标注后的 bad case 加入评估集。

### Q68：如何控制大模型应用成本？

**高频程度：高**

**参考回答：**
- 控制输入 Token：上下文压缩、去重、只传必要字段。
- 控制输出 Token：明确格式、长度限制、分段生成。
- 模型路由：简单任务走小模型，复杂任务走强模型。
- 缓存：对相同问题、检索结果、embedding 和工具结果做缓存。
- 监控：按用户、租户、功能统计 Token 和费用，设置预算告警。

**面试追问：**
- 缓存会不会返回过期答案？
- 如何在质量和成本之间权衡？
- RAG 哪些环节最耗成本？

**项目化回答提示：**  
可以说你按任务分级定模型，并对 embedding、检索和标准 FAQ 答案做缓存。

### Q69：如何降低大模型应用延迟？

**高频程度：高**

**参考回答：**
- 使用流式响应降低首字延迟。
- 减少上下文长度和不必要的模型调用。
- 并行执行独立步骤，例如多路检索、多个工具查询。
- 使用缓存、异步任务、预计算 embedding 和 warm-up。
- 对复杂 Agent 设置快速路径，简单问题不要走完整多步流程。

**面试追问：**
- 首字延迟和总延迟分别怎么优化？
- rerank 会增加延迟，为什么还用？
- 多 Agent 如何控制延迟？

**项目化回答提示：**  
说你通过“意图识别快速路径 + 并行检索 + 流式输出 + 缓存”把体验做稳。

### Q70：如何做大模型应用的自动化测试？

**高频程度：高**

**参考回答：**
- 传统单测覆盖工具函数、解析器、权限和路由规则。
- LLM 评估集覆盖典型问题、边界问题、对抗问题和 bad case。
- 对结构化输出检查 schema，对 RAG 检查引用和忠实度。
- 对 Agent 检查工具调用序列、参数和最终任务成功率。
- 每次 Prompt、模型、检索策略变更后做回归评估。

**面试追问：**
- 模型输出不稳定，测试怎么写？
- LLM Judge 可靠吗？
- 如何设置上线阈值？

**项目化回答提示：**  
说你把评估结果看成质量门禁，例如成功率、格式正确率、拒答正确率低于阈值不发布。

### Q71：LLM Judge 有什么优缺点？

**高频程度：中**

**参考回答：**
- LLM Judge 可以自动评估相关性、忠实度、完整性、语气和安全性。
- 优点是灵活、覆盖开放式问题，比人工便宜。
- 缺点是可能有偏差、不稳定、被提示词影响，且成本不为零。
- 应通过少量人工标注校准 Judge，并设计明确评分 rubric。
- 高风险指标不能完全依赖 Judge，要结合规则和人工审核。

**面试追问：**
- 如何让 Judge 更稳定？
- Judge 模型和被评估模型能否相同？
- 如何处理 Judge 和人工意见不一致？

**项目化回答提示：**  
说你会用 Judge 做初筛和趋势监控，关键发布仍抽样人工复核。

### Q72：如何处理 Prompt Injection？

**高频程度：高**

**参考回答：**
- Prompt Injection 是用户或外部文档试图覆盖系统指令、泄露信息或诱导工具滥用。
- 防护要分层：输入检测、上下文隔离、权限控制、工具白名单、输出审查。
- 对 RAG，要把检索文档视为不可信内容，不能让文档中的指令覆盖系统规则。
- 工具调用前必须做业务权限和参数校验。
- 敏感系统提示词和密钥不应进入模型可见上下文。

**面试追问：**
- 文档里写“忽略之前指令”怎么办？
- 如何检测间接提示注入？
- Prompt Injection 能完全防住吗？

**项目化回答提示：**  
强调“模型只能建议，系统负责执行权限判断”，并说明检索内容作为 data 而不是 instruction。

### Q73：如何设计内容安全和合规审核？

**高频程度：高**

**参考回答：**
- 输入侧检查违法、暴力、隐私、越权和注入风险。
- 输出侧检查敏感信息、违规内容、错误承诺和高风险建议。
- 检索侧检查用户是否有权访问上下文。
- 工具侧检查操作权限、参数范围和人工审批。
- 不同风险等级使用不同策略：直接拒答、澄清、转人工、脱敏回答。

**面试追问：**
- 内容审核放在模型前还是模型后？
- 如何处理误杀？
- 企业内部知识是否也需要审核？

**项目化回答提示：**  
说你按“输入、检索、工具、输出”四个关口做安全，而不是只在最后过滤。

### Q74：大模型应用如何做缓存？

**高频程度：中**

**参考回答：**
- 缓存对象包括 embedding、检索结果、rerank 结果、模型回答、工具查询结果。
- 适合缓存确定性强、更新慢、访问频繁的内容。
- 对个性化、权限敏感、实时数据要谨慎缓存。
- 缓存 key 应包含模型版本、Prompt 版本、用户权限、知识库版本等关键信息。
- 缓存需要 TTL、失效机制和命中率监控。

**面试追问：**
- RAG 答案缓存如何避免文档更新后过期？
- 多租户缓存如何隔离？
- 流式回答能缓存吗？

**项目化回答提示：**  
说你对 FAQ 和 embedding 做强缓存，对权限相关回答只缓存检索中间结果或短 TTL。

### Q75：如何设计大模型应用的降级策略？

**高频程度：高**

**参考回答：**
- 模型不可用时，可切换备用模型、小模型或规则模板。
- 检索失败时，可降级关键词检索、返回未找到或转人工。
- 工具失败时，可重试、排队、降级只读查询或提示稍后处理。
- 质量低置信度时，可拒答、澄清或人工审核。
- 降级要可观测，避免用户无感知地收到错误答案。

**面试追问：**
- 备用模型输出风格不同怎么办？
- 降级是否会影响业务 SLA？
- 如何测试降级链路？

**项目化回答提示：**  
说你定义了“强模型 -> 备用模型 -> 固定模板 -> 人工”的多级兜底，并有告警。

### Q76：如何做模型、Prompt、知识库版本管理？

**高频程度：高**

**参考回答：**
- 每次请求记录模型版本、Prompt 版本、知识库版本和工具版本。
- Prompt 应像代码一样管理，支持灰度、回滚和评估。
- 知识库更新要有文档版本、chunk 版本和索引版本。
- 模型升级要做离线评估、灰度对比和线上监控。
- 历史回答要能追溯当时使用的版本，方便审计。

**面试追问：**
- Prompt 改动如何回滚？
- embedding 模型升级为何要重建索引？
- 知识库新旧版本如何平滑切换？

**项目化回答提示：**  
可以说你在日志里记录 `model_id`、`prompt_version`、`kb_version`，上线走灰度和回归评估。

### Q77：如果让你从 0 到 1 设计一个企业知识库 Agent，你会怎么做？

**高频程度：高**

**参考回答：**
- 明确场景和边界：问答、摘要、工单、数据查询，哪些能答，哪些必须拒答或转人工。
- 搭建 RAG：文档解析、切分、embedding、混合检索、rerank、引用返回。
- 设计 Agent：意图识别、工具选择、检索源路由、澄清、答案生成和人工确认。
- 做工程化：权限隔离、日志 trace、评估集、监控、限流、成本控制。
- 做迭代闭环：用户反馈、bad case 标注、知识库运营、Prompt 和检索策略回归。

**面试追问：**
- 第一版最小可用系统怎么做？
- 如何证明系统有效？
- 如何支持多部门、多权限、多数据源？

**项目化回答提示：**  
用 STAR 结构讲：业务目标、技术方案、关键难点、指标提升，例如解决率、检索命中率、转人工率下降。

---

## 一页速查表

| 主题 | 面试关键词 | 推荐回答抓手 |
|---|---|---|
| Prompt | 角色、任务、约束、示例、输出格式、版本化 | 不靠玄学，靠评估集迭代 |
| LangChain | ChatModel、Prompt、Parser、Retriever、Tool、LCEL | 快速组合，但生产要接口隔离 |
| LangGraph | State、Node、Edge、条件边、Checkpoint、Interrupt | 复杂 Agent 用显式状态图控制 |
| RAG | 切分、Embedding、混合检索、Rerank、引用、评估 | 分开定位检索错和生成错 |
| Agent | ReAct、工具调用、记忆、规划、终止条件 | 可控自主，不是完全放飞 |
| Function Calling | Schema、参数校验、后端执行、错误反馈 | 模型只产生调用意图，系统负责执行 |
| Multi-Agent | Supervisor、Sequence、Critic、Handoff | 只有职责天然不同才上多 Agent |
| MCP | Agent-to-Tool、工具发现、统一协议 | 适合接企业工具和资源 |
| 评估 | Recall@K、MRR、Faithfulness、成功率 | 指标分层，不只看最终回答 |
| 工程化 | trace、限流、缓存、降级、安全、版本 | 上线关键是稳定性和可观测 |

## 常见项目包装话术

### 1. RAG 项目回答模板

我做的是一个企业知识库问答系统，核心链路是文档解析、语义切分、向量化、混合检索、rerank、上下文拼接和答案生成。  
难点主要有三个：第一是 PDF / Markdown / 表格解析后的结构保留；第二是检索不准导致的幻觉；第三是权限隔离。  
我通过 metadata filter、BM25 + 向量混合召回、reranker、引用返回和低置信度拒答来提升效果。上线后用 Recall@K、Faithfulness、用户满意度和转人工率做评估。

### 2. Agent 项目回答模板

我做的 Agent 不是单纯聊天，而是能根据用户意图调用工具完成任务。  
架构上分为意图识别、上下文构造、工具选择、参数校验、工具执行、结果总结和安全审核。  
为了防止不可控，我设置了工具白名单、最大步数、超时、幂等 key、敏感操作人工确认和完整 trace。  
bad case 会按意图错误、工具错误、生成错误、安全错误分类回流。

### 3. LangGraph 项目回答模板

我使用 LangGraph 的 StateGraph 把 Agent 流程拆成显式节点，例如解析问题、检索、调用工具、生成答案、质量审核和人工确认。  
State 保存中间结果和控制字段，条件边负责路由，checkpoint 支持中断恢复。  
相比纯 ReAct，这种方式更可控，也更容易调试和做权限审核。

### 4. 工程化回答模板

上线时我不会只关注模型效果，还会关注稳定性、成本、安全和可观测性。  
每次请求都有 trace_id，记录模型版本、Prompt 版本、知识库版本、检索结果、工具调用、延迟和 token 成本。  
同时有超时、重试、限流、缓存、降级和内容安全审核。改 Prompt 或模型前会跑离线评估集，灰度后再全量。

## 资料来源

- 招聘 JD 与岗位要求：
  - [Remote China AI 全栈开发](https://remote-china.com/jobs/1153)
  - [BeBee 大模型应用开发工程师](https://bebee.com/cn/jobs/ss-cn-4ekryl)
  - [BeBee Agent 开发工程师](https://bebee.com/cn/jobs/agent--techmap_cn_82246019)
  - [智联 AI Agent 开发工程师](https://www.zhaopin.com/jobdetail/CCL1503189680J40843725805.htm)
  - [AI 应用/RAG 工程师](https://jobs.tiangong.edu.cn/correcruit/content/id/54911.html)
- 官方文档：
  - [LangGraph overview](https://docs.langchain.com/oss/python/langgraph)
  - [LangGraph Graph API](https://docs.langchain.com/oss/python/langgraph/graph-api)
  - [LangGraph Agentic RAG](https://docs.langchain.com/oss/python/langgraph/agentic-rag)
  - [LangGraph Workflows and agents](https://docs.langchain.com/oss/python/langgraph/workflows-agents)
  - [LangGraph Persistence](https://docs.langchain.com/oss/javascript/langgraph/persistence)
- 公开面试资料与社区题库：
  - [百度开发者中心：从 RAG 到 Agent 面经](https://developer.baidu.com/article/detail.html?id=5573507)
  - [小林 Coding：RAG 面试题](https://xiaolinnote.com/ai/rag/rag_info.html)
  - [火山引擎开发者社区：LangChain 高频题](https://developer.volcengine.com/articles/7582491181021659172)
  - [GitHub AgentGuide：Agent 面试题](https://github.com/adongwanai/AgentGuide/blob/c62afb715686993538761848f0da4c773567e7af/docs/04-interview/03-agent-questions.md)
  - [GitHub Multi-Agent-AI-Platform：RAG/LLM Interview QA](https://github.com/lxhmx/Multi-Agent-AI-Platform/blob/6aea5206b2ca00f03877ae3263f214627a2ff7fd/RAG_LLM_Interview_QA.md)

