面试官大概率会围绕 **LLM 应用链路、流式语音、多轮记忆、RAG、鉴权和工程化缺口** 来追问。你可以先这样概括项目：

> 这是一个 AI 虚拟角色聊天平台。用户可以注册登录、创建角色，配置头像、背景、人设和音色。聊天时后端会把系统提示词、角色人设、长期记忆和最近对话拼成上下文，通过 LangGraph 调用大模型，并支持工具调用和知识库检索。前端支持文本输入和语音输入，语音通过 VAD 自动切分后接 ASR，回复同时以 SSE 流式返回文本和 TTS 音频，实现边生成边播放。

关键代码在 [chat.py](D:/IDE/Pycharm/project/AIFriends/backend/web/views/friend/message/chat/chat.py:28)、[graph.py](D:/IDE/Pycharm/project/AIFriends/backend/web/views/friend/message/chat/graph.py:21)、[memory/update.py](D:/IDE/Pycharm/project/AIFriends/backend/web/views/friend/message/memory/update.py:23)、[InputField.vue](D:/IDE/Pycharm/project/AIFriends/frontend/src/components/character/chat_field/input_field/InputField.vue:22)。

**高频问题**
1. 这个项目的核心亮点是什么？  
答：我不是只做了一个调用大模型的聊天框，而是做了一个完整角色对话系统，包括角色创建、人设注入、短期上下文、长期记忆、RAG 工具调用、语音输入、流式 TTS 播放、JWT 登录鉴权和前后端完整闭环。

1. 一条消息从用户发送到 AI 回复的完整链路是什么？  
答：前端发送文本或语音转写后的文本，后端校验 friend 是否属于当前用户，然后拼接系统提示词、角色 profile、长期 memory 和最近 10 条历史消息，交给 LangGraph 执行。模型流式输出时，一边把文本 chunk 通过 SSE 返回前端，一边把文本 chunk 发给 TTS WebSocket，TTS 返回的音频也通过 SSE 传给前端播放。最后保存 Message，并触发长期记忆更新。

1. 你怎么保证角色人设稳定？  
答：主要靠三层上下文：系统提示词定义通用回复规则，Character.profile 定义角色性格和背景，Friend.memory 保存这个用户和该角色之间的长期关系记忆，再补充最近对话做短期上下文。这样角色不会只依赖当前一轮输入。

1. 长期记忆怎么实现？  
答：Friend 表里有 memory 字段。每次对话结束后，我会把原始 memory 和最近对话交给一个 MemoryGraph，让模型重新总结成新的长期记忆，再写回 friend.memory。这个方案优点是简单、token 成本可控；缺点是当前是同步更新，生产环境我会改成异步任务，并做重要性判断，不是每轮都更新。

1. 为什么不直接把所有历史消息都塞给模型？  
答：会带来 token 成本、延迟和上下文窗口问题，而且长历史里有很多低价值信息。所以我用“最近消息 + 摘要记忆”的组合：最近消息保证对话连续性，摘要记忆保留长期事实和关系变化。

1. RAG 是怎么做的？  
答：离线用 TextLoader 加载文档，用 RecursiveCharacterTextSplitter 按 500 字符、50 overlap 切块，然后用自定义 Embeddings 调 embedding 模型生成 1024 维向量，存到 LanceDB。在线聊天时 LangGraph 工具 `search_knowledge_base` 根据 query 做 top3 相似度检索，把相关片段作为上下文返回给模型。

1. 为什么用 LangGraph，而不是直接调用 LLM？  
答：直接调用适合简单问答，但这个项目需要工具调用和后续扩展。LangGraph 可以把模型节点、工具节点和条件边组织成可扩展的 agent 流程。现在已有时间工具和知识库检索工具，后续可以继续加日程、搜索、角色状态等工具。

1. 工具调用怎么避免乱调？  
答：我通过 tool 的描述限制调用场景，比如知识库工具只在用户询问特定知识库内容时调用。更生产化的做法是加路由分类、工具权限控制、检索结果置信度判断，以及让模型在回答时只基于检索上下文作答。

1. 为什么用 SSE 做流式输出？  
答：聊天回复主要是服务端向客户端单向推送，SSE 比 WebSocket 简单，和 HTTP 鉴权、代理兼容性也更好。当前语音 ASR/TTS 与第三方服务之间需要双向流，所以后端对模型/TTS 使用 WebSocket，前端接收结果用 SSE。

1.  TTS 怎么做到边生成边播放？  
答：后端在模型输出 chunk 时立即把文本发给 TTS WebSocket，同时把文本 chunk 放进队列返回前端；TTS 返回二进制音频后转 base64，通过同一条 SSE 发给前端。前端用 MediaSource 和 SourceBuffer 维护音频队列，收到 mp3 chunk 就 appendBuffer，所以可以边收边播。

1.  语音输入怎么做？  
答：前端使用 `@ricky0123/vad-web` 做语音活动检测，检测到说话结束后把 Float32 音频转成 16-bit PCM，上传到后端。后端通过实时 ASR WebSocket 分块发送 PCM，并接收最终转写文本，再走普通聊天链路。这样可以避免用户手动按住录音，也减少无效音频上传。

1.  登录鉴权怎么设计？  
答：后端用 SimpleJWT。access token 给前端放内存，refresh token 放 HttpOnly cookie，减少被 JS 读取的风险。前端 axios 拦截器会自动带 access token，遇到 401 时调用 refresh 接口，并用 `isRefreshing + refreshSubscribers` 避免多个请求同时刷新 token。

1.  如何保证用户只能访问自己的会话？  
答：关键查询都会带上当前登录用户过滤，比如聊天时校验 `Friend.objects.filter(pk=friend_id, me__user=request.user)`，历史消息查询也通过 `friend__me__user=request.user` 过滤。Friend 是用户和 Character 的关系表，所以同一个角色对不同用户有独立记忆。

1.  数据表为什么这样设计？  
答：UserProfile 存用户资料，Character 存角色人设、头像、背景和音色，Friend 表示某个用户与某个角色建立的关系并保存长期 memory，Message 保存每轮输入、输出、prompt 快照和 token 用量，SystemPrompt 存可配置的系统提示词。这样角色资产和用户私有对话状态是分离的。

1.  怎么做成本控制？  
答：目前记录了 input_tokens、output_tokens、total_tokens，可以用于统计成本。上下文上限制最近消息数量，长期记忆用摘要压缩。进一步可以加用户级限流、每日 token quota、模型分级、缓存常见检索结果，以及把 TTS/ASR 计费也纳入日志。

1.  这个项目最大的技术难点是什么？  
答：我会说是“流式文本和流式语音的协同”。模型输出、TTS WebSocket、SSE 推送、前端 MSE 播放都不是一次性请求，需要处理异步队列、chunk 顺序、播放中断和错误恢复。

1.  如果用户点击停止生成，你怎么处理？  
答：前端现在用 processId 忽略过期响应，并停止音频播放，这是前端层面的停止。更完整的方案是给 fetchEventSource 加 AbortController，并让后端感知客户端断开后取消 LLM/TTS 任务，释放 WebSocket 和线程资源。

1.  生产环境要怎么改？  
答：我会把 SQLite 换成 PostgreSQL，媒体文件放对象存储，Django Streaming 相关接口迁到 ASGI 或专门的异步服务，长期记忆更新放 Celery/RQ 队列，增加 Redis 缓存、限流、日志追踪、模型调用超时重试，以及 API key、SECRET_KEY、DEBUG、CORS 等配置环境化。

1.  安全上有哪些风险？  
答：个人项目阶段主要验证功能闭环。生产要处理密钥不能硬编码、DEBUG 必须关闭、CORS 和 ALLOWED_HOSTS 收紧、上传文件类型和大小校验、接口限流、CSRF 策略、Prompt Injection 防护、RAG 文档可信边界，以及用户内容安全审核。

1.  如何评价这个 AI 应用效果？  
答：可以从四类指标看：首 token 延迟、完整回复延迟、ASR/TTS 延迟和失败率；角色一致性、长期记忆准确率、RAG 命中率；token 成本和调用成功率；最后用一组固定测试问题做回归评测，防止改 prompt 或模型后质量下降。

**容易被追问的代码缺口**
你要提前准备诚实回答：`memory/update.py` 里本意可能是取最近 10 条，但现在写成了 `order_by('-id'[:10])`，实际没有切片；聊天结果 `output` 只存 500 字，长回复会被截断；每轮都同步更新 memory 会增加延迟；`DEBUG=True` 和硬编码 SECRET_KEY 不能用于生产。  
面试时不要慌，可以说：“这是个人项目阶段遗留的问题，我复盘后会把它们作为生产化改造项：修正历史截取、异步更新记忆、完整保存消息、配置环境化并补充测试。”

简历上建议把它写成：**“基于 Django + Vue3 + LangGraph 构建多模态 AI 角色聊天平台，支持角色人设注入、长期记忆、RAG 工具调用、SSE 流式回复、VAD/ASR 语音输入和流式 TTS 播放。”**

---
---

## 一、LLM→TTS 双向流式架构

> 你写了"采用WebSocket全双工通信，文本生成与语音合成并行执行"

**基础问题：**

1. **你的架构中，前端和后端之间的通信方式是什么？后端和TTS服务之间的通信方式又是什么？** 
   - 看了你的代码，前端实际用的是 **SSE**（`text/event-stream` + `fetchEventSource`），后端和TTS服务之间用的是 **WebSocket**。你简历上写"WebSocket全双工通信"，面试官可能会质疑这个表述是否准确——因为对前端用户来说并不是WebSocket。

2. **为什么不直接用WebSocket而用SSE？SSE和WebSocket各有什么优缺点？你在什么场景下会选择WebSocket？**
   - 期望你能回答：SSE是单向服务端推送，适合这种"服务端不断发数据给客户端"的场景；WebSocket是双向的，适合客户端也需要频繁发消息的场景（如聊天室）。你的项目中客户端只需要发送一次消息，然后等待流式回复，所以SSE就够了。

3. **你说"响应延迟降低"，降低了多少？有没有做过量化对比？**
   - 这是一个很实际的追问。如果没做过benchmark，建议你准备一个大致的说法，比如"因为文本和语音并行生成，用户在LLM输出第一个token后几乎立即听到语音，而非等全部文本生成完再合成"。

**深入问题：**

4. **你的 ```[tts_sender](file:///d:/IDE/Pycharm/project/AIFriends/backend/web/views/friend/message/chat/chat.py#L79-L107) 和 [tts_receiver](file:///d:/IDE/Pycharm/project/AIFriends/backend/web/views/friend/message/chat/chat.py#L109-L118)``` 是通过 `asyncio.gather` 并行执行的。如果TTS合成速度比LLM生成速度快，会出现什么问题？你如何处理背压（backpressure）？**
   - 你当前代码中 TTS sender 直接 `await ws.send()` 发送每个 chunk，如果 TTS 服务处理不过来怎么办？

5. **你用 `Queue` 在子线程和 SSE generator 之间传递数据（[event_stream](file:///d:/IDE/Pycharm/project/AIFriends/backend/web/views/friend/message/chat/chat.py#L165-L203) 方法）。为什么用 `threading.Thread` + `Queue` 而不是直接在 async 上下文中操作？Django 的 `StreamingHttpResponse` 对异步生成器支持如何？**
   - 这个问题考察你对 Django 异步模型的理解。Django 的 `StreamingHttpResponse` 在 ASGI 模式下可以接受 async generator，但你用的是同步 view + 线程的方式。

6. **前端如何实现音频流式播放的？** 
   - 你的前端用了 **MediaSource Extensions (MSE)**，通过 `MediaSource` + `SourceBuffer` 实现 mp3 chunk 边收边播。面试官可能会问：为什么不用 `AudioContext`（Web Audio API）？MSE 和 Web Audio API 各有什么适用场景？

7. **如果用户在语音播放过程中发送了新消息，你怎么处理？**
   - 看你的 [handleStop](file:///d:/IDE/Pycharm/project/AIFriends/frontend/src/components/character/chat_field/input_field/InputField.vue#L126-L130) 方法，你通过 `++processId` 来忽略旧的流式回调，用 `stopAudio()` 停止播放。面试官可能追问：服务端那边的 LLM 调用和 TTS WebSocket 连接是否也中断了？**你并没有在服务端做中断处理**。

---

## 二、LangGraph Agent 状态机

> 你写了"基于LangGraph构建可扩展Agent状态机，集成时间查询、知识库RAG检索等工具，支持条件分支与循环执行"

**基础问题：**

8. **请画一下你的Agent状态机的状态转移图。**
   - 你的 [graph.py](file:///d:/IDE/Pycharm/project/AIFriends/backend/web/views/friend/message/chat/graph.py#L54-L86) 实际结构是：
   ```
   START → agent → [should_continue] → tools → agent (循环)
                                   → END
   ```

9. **`should_continue` 函数的判断逻辑是什么？`last_message.tool_calls` 是什么？LLM是如何决定要调用工具的？**
   - 考察你对 function calling / tool use 机制的理解。

10. **你说"支持条件分支与循环执行"，能举一个循环执行的具体例子吗？**
    - 你的代码中确实有循环：agent → tools → agent，如果 agent 再次决定调用工具就会继续循环。面试官可能会问：有没有设置最大循环次数来防止无限循环？**你的代码中没有设置 `recursion_limit`**。

**深入问题：**

11. **LangGraph 和直接用 LangChain 的 `AgentExecutor` 有什么区别？你为什么选择 LangGraph？**
    - 这考察你对技术选型的思考。LangGraph 的优势在于状态机的可视化、可控的流程、支持 human-in-the-loop 等。

12. **你的 AgentState 只有一个 `messages` 字段。如果要扩展（比如加入用户意图分类、上下文信息），你会怎么设计 state？**

13. **你目前只有两个工具（`get_time` 和 `search_knowledge_base`）。如果要加入更多工具（比如查天气、发邮件），你的架构需要做哪些调整？如何避免工具太多导致 LLM 选错工具？**

14. **`search_knowledge_base` 工具中，你每次调用都会重新 `lancedb.connect()` 和创建 `CustomEmbeddings()` 实例。这有什么性能问题？如何优化？**
    - 看 [graph.py:L28-L36](file:///d:/IDE/Pycharm/project/AIFriends/backend/web/views/friend/message/chat/graph.py#L28-L36)，每次 tool 调用都会重新连接数据库和初始化 embedding client。

---

## 三、LanceDB 向量数据库 + RAG

> 你写了"LanceDB向量数据库存储文档嵌入，实现Top-K相似度检索"

**基础问题：**

15. **为什么选择 LanceDB 而不是 FAISS、ChromaDB、Milvus 等？它们各有什么优缺点？**
    - LanceDB 的优势是嵌入式（无需单独部署服务）、基于 Lance 格式（列式存储，支持版本控制）。

16. **你的 embedding 模型用的是什么？维度是多少？chunk_size 和 chunk_overlap 是怎么确定的？**
    - 你用的是 `text-embedding-v4`，维度 1024，chunk_size=500, overlap=50。面试官会问这些超参数是怎么调的。

17. **`similarity_search(query, k=3)` 中的 k 为什么选 3？有没有做过不同 k 值的效果对比？**

**深入问题：**

18. **RAG 的召回质量如何评估？你有没有遇到过"检索到了但答案不对"或者"该检索到但没检索到"的情况？怎么解决？**
    - 这是 RAG 系统最核心的问题。可以聊：query rewriting、reranking、hybrid search 等。

19. **你的知识库文档是什么？是静态的还是会动态更新的？如果文档更新了，向量索引怎么更新？**
    - 你的 [insert_document.py](file:///d:/IDE/Pycharm/project/AIFriends/backend/web/documents/utils/insert_document.py#L1-L25) 用的是 `mode='overwrite'`，全量覆盖。面试官可能会问增量更新怎么做。

20. **你的 [CustomEmbeddings](file:///d:/IDE/Pycharm/project/AIFriends/backend/web/documents/utils/custom_embeddings.py#L1-L31) 类中 `embed_documents` 用 batch_size=10 逐批调用 API。如果文档量很大（百万级），这个方案可行吗？如何优化？**

---

## 四、长期/短期双层记忆架构

> 你写了"对话后自动总结更新记忆"

**基础问题：**

21. **请解释一下你的"长期记忆"和"短期记忆"分别是什么？它们是怎么配合使用的？**
    - **长期记忆**：`Friend.memory` 字段，是 LLM 对历史对话的总结文本（最大5000字符）。
    - **短期记忆**：最近10条消息的原始记录（[add_recent_messages](file:///d:/IDE/Pycharm/project/AIFriends/backend/web/views/friend/message/chat/chat.py#L42-L50)）。

22. **每次对话后都更新记忆（`update_memory`），这个"总结"是用 LLM 做的吗？用的什么 prompt？**
    - 你的 [update.py](file:///d:/IDE/Pycharm/project/AIFriends/backend/web/views/friend/message/memory/update.py#L1-L37) 中用 SystemPrompt（title='记忆'）+ 原始记忆 + 最近对话作为输入，让 LLM 重新总结。

23. **每次对话都调用一次记忆更新，这个过程是同步阻塞的吗？会不会影响用户等待时间？**
    - 看 [chat.py:L202-L203](file:///d:/IDE/Pycharm/project/AIFriends/backend/web/views/friend/message/chat/chat.py#L202-L203)：`update_memory(friend)` 是在 `event_stream` generator 的最后同步调用的，SSE 数据已经全部发送完了才更新。面试官可能会问：如果更新失败怎么办？会不会影响下一次对话？

**深入问题：**

24. **长期记忆的 5000 字符限制怎么处理？如果对话历史很长，总结后的内容超过 5000 字符会怎样？**
    - 你的 `Friend.memory` 是 `max_length=5000`，如果 LLM 返回的总结超过这个长度会被截断。

25. **你提到"每1次对话后自动总结"——我看到 [chat.py:L203](file:///d:/IDE/Pycharm/project/AIFriends/backend/web/views/friend/message/chat/chat.py#L203) 写的是 `% 1 == 0`，这意味着每条消息都会触发记忆更新。这样做成本是否太高？你考虑过什么优化策略？**
    - 比如：每 N 条消息更新一次、异步更新、用更小的模型做总结等。

26. **多个角色的记忆是隔离的吗？同一用户和不同角色的对话记忆是否互相影响？**
    - 你的 `memory` 字段在 `Friend` 模型上，是按用户-角色对隔离的，这点设计合理。

27. **你有没有想过用向量数据库来存储记忆（而不是纯文本），实现更精准的记忆检索？这和现在的方案比有什么优劣？**

---

## 五、JWT 双Token + SSE 401 自动刷新

> 你写了"SSE连接支持401自动刷新Token并重试，用户无感知"

**基础问题：**

28. **access_token 和 refresh_token 分别存在哪里？为什么要这样设计？**
    - access_token 存在 Pinia store（内存/前端状态），refresh_token 存在 httponly cookie。面试官会问：为什么不把 access_token 也存 cookie？

29. **access_token 有效期是多久？refresh_token 呢？为什么这样设置？**
    - 你设的是 access_token 2小时，refresh_token 7天。

30. **`ROTATE_REFRESH_TOKENS: True` 和 `BLACKLIST_AFTER_ROTATION: True` 是什么意思？为什么要开启？**
    - 每次刷新 token 时，旧的 refresh_token 会被加入黑名单，防止重放攻击。

**深入问题：**

31. **你的 [streamApi.js](file:///d:/IDE/Pycharm/project/AIFriends/frontend/src/js/http/streamApi.js#L34-L51) 中，SSE 遇到 401 时的处理逻辑是：先调 `api.post` 刷新 token，然后抛出 `"TOKEN_REFRESHED"` 错误，在 `onerror` 中捕获并递归调用 `startFetch()`。为什么不直接在 `onopen` 中刷新后重新发起 SSE 连接，而要通过抛错误这种"迂回"的方式？**
    - 这是因为 `fetchEventSource` 的 `onopen` 中无法直接"重新发起"请求，需要通过 error → retry 的机制。面试官会考察你是否理解这个设计的原因。

32. **你的 [api.js](file:///d:/IDE/Pycharm/project/AIFriends/frontend/src/js/http/api.js#L43-L92) 中用 `isRefreshing` + `refreshSubscribers` 队列处理并发请求的 token 刷新。请解释这个模式的工作原理。如果同时有 10 个请求返回 401，会发生什么？**
    - 这是经典的"token 刷新锁"模式：第一个 401 触发刷新，后续 401 请求被加入等待队列，刷新完成后统一重试。

33. **这个方案有没有竞态条件（race condition）？比如在 SSE 流式传输过程中 token 过期了，中途收到 401 怎么办？**
    - 你的 SSE 是长连接，如果 2 小时的 access_token 在传输过程中过期... 实际上一般对话不会持续 2 小时，但面试官可能会问这个边界情况。

---

## 六、综合/系统设计问题

34. **如果要支持多人同时在线聊天，你的架构有什么瓶颈？如何扩展？**
    - Django 同步 view + 线程的方案在高并发下的问题；是否需要换成 ASGI + asyncio 全异步。

35. **你的数据库用的是 SQLite。如果要迁移到 MySQL/PostgreSQL，需要改哪些地方？**
    - 考察你对 ORM 和数据库抽象层的理解。

36. **你的项目中有没有做错误处理和重试机制？如果 TTS 服务挂了怎么办？如果 LLM API 超时怎么办？**
    - 看你的代码中 `try/except` 基本都是空的或者很粗略。

37. **你有没有做过性能测试？瓶颈在哪里？一秒钟能处理多少个并发聊天请求？**

38. **如果让你重新设计这个系统，你会做哪些不同的技术决策？**

---

## 总结建议

作为面试官，我会特别关注以下几点：

| 维度 | 你的项目亮点 | 面试官可能深挖的方向 |
|------|------------|-------------------|
| 流式架构 | SSE + WebSocket 双通道 | 为什么不是全 WebSocket？服务端中断处理？ |
| Agent | LangGraph 状态机 | 和 AgentExecutor 区别？无限循环保护？ |
| RAG | LanceDB + 自定义 Embeddings | k 值选择？召回质量评估？增量更新？ |
| 记忆 | 长短双层记忆 | 总结质量？更新频率优化？记忆上限处理？ |
| 认证 | 双 Token + SSE 刷新 | 竞态条件？并发刷新锁？ |
| 工程实践 | 端到端可运行 | 错误处理？性能？可扩展性？ |

**核心提醒**：你的简历写的是"WebSocket全双工通信"，但实际前端用的是 **SSE**。面试时需要准确描述——后端和 TTS 服务之间是 WebSocket，但前端和后端之间是 SSE。如果你被追问到这一点，坦诚说"前端到后端用的是SSE因为场景只需要单向推送，后端到TTS服务用的是WebSocket因为TTS协议要求双向流式传输"，会比硬撑更专业。

---
---

## 一、LLM→TTS 双向流式架构

### Q1：前端和后端、后端和TTS服务之间的通信方式分别是什么？

**参考回答：**

> 前端和后端之间用的是 **SSE（Server-Sent Events）**，后端和 TTS 服务之间用的是 **WebSocket**。
> 
> 选择不同协议的原因是通信模式不同：
> - 前端→后端：客户端只需要发送一次请求，之后由服务端单向推送文本和音频数据。SSE 天然适合这种"服务端单向推送"的场景，而且基于 HTTP，无需额外的协议升级，兼容性更好。
> - 后端→TTS 服务：TTS 服务需要双向流式通信——我需要持续向它发送文本 chunk，同时它实时返回音频 bytes。这必须用 WebSocket 的全双工能力。
> 
> 所以简历上"WebSocket"指的是后端和 TTS 服务之间的通信链路。整体架构是：**前端 ←SSE→ 后端 ←WebSocket→ TTS服务**，两条链路并行工作。

**面试官可能追问：** 那你为什么不前后端也用 WebSocket？

> 因为 SSE 足够了。WebSocket 需要协议升级、连接管理更复杂，而我这里客户端不需要在流式过程中向服务端发消息（停止聊天是通过中断请求实现的），SSE 更轻量、更简单，且基于 HTTP 天然支持现有的认证机制（JWT header）。

---

### Q2：为什么不直接用 WebSocket？SSE 和 WebSocket 各有什么优缺点？

**参考回答：**

> | 特性 | SSE | WebSocket |
> |------|-----|-----------|
> | 通信方向 | 单向（服务端→客户端） | 双向 |
> | 协议 | HTTP/HTTPS | 独立协议（ws://） |
> | 自动重连 | 浏览器原生支持 | 需手动实现 |
> | 认证 | 复用 HTTP Header | 需在握手阶段传递 |
> | 数据格式 | 文本（UTF-8） | 文本+二进制 |
> | 适用场景 | 通知、流式推送 | 聊天室、游戏、协同编辑 |
>
> 在我的项目中，聊天场景的数据流是单向的（服务端推文本+音频给客户端），所以 SSE 完全够用。如果未来需要实现实时的"正在输入"状态同步、或者多人协同聊天，那就会切换到 WebSocket。

---

### Q3：你说"响应延迟降低"，降低了多少？

**参考回答：**

> 我没有做过精确的 benchmark，但架构上的收益是清晰的：
> 
> **传统串行方案**：LLM 生成全部文本 → 发送给 TTS → TTS 合成全部语音 → 返回给前端播放。假设 LLM 生成需要 3 秒，TTS 合成需要 2 秒，总延迟 = 5 秒。
> 
> **我的并行方案**：LLM 每生成一个 token 立即推送给 TTS 和前端。用户在 LLM 输出第一个 token 后（约 200-500ms）就能看到文字，同时 TTS 开始合成。首字节感知延迟约 500ms（LLM 首 token 时间）+ 几百ms（TTS 首个音频 chunk），总计约 1 秒以内用户就能看到文字并听到声音。
> 
> 关键优化点在于**文本流和音频流并行传输**，且 TTS 采用流式合成（不是等全部文本到齐才开始），所以总延迟接近 LLM 生成时间，而非两者之和。

---

### Q4：如果 TTS 合成速度比 LLM 生成速度快，会出现什么问题？如何处理背压？

**参考回答：**

> 在我的架构中，TTS 合成速度快于 LLM 生成速度其实不是问题，反而是理想情况——因为 TTS 需要等待 LLM 的文本 chunk 才能开始合成，它天然就是被 LLM 的输出速率"节流"的。
> 
> 真正需要关注的背压场景是反过来的：**如果 LLM 生成很快而 TTS 合成慢**。我当前的代码中，`tts_sender` 是直接 `await ws.send()` 发送每个 chunk 给 TTS。如果 TTS 处理不过来，WebSocket 的发送缓冲区会自然地形成背压——`send` 会阻塞等待，这实际上起到了限流作用。
> 
> 不过更健壮的做法是加入显式的队列控制。在当前项目规模下，由于 TTS 服务（字节的 CosyVoice）处理能力足够强，这个问题不显著。如果要做到生产级，我会加一个发送队列和消费速率控制。

---

### Q5：为什么用 `threading.Thread` + `Queue` 而不是直接在 async 上下文中操作？

**参考回答（这个需要诚实回答）：**

> 这是一个技术债务。Django 的 `StreamingHttpResponse` 在设计上是同步的——它接受一个同步的 generator。而我的 LLM 调用（通过 LangChain）和 TTS 调用（通过 `websockets`）都是异步的。
> 
> 我当前的方案是：在子线程中运行 `asyncio.run()` 来执行异步的 TTS 任务，然后通过线程安全的 `Queue` 把数据传递给同步的 SSE generator。这是一种"桥接"方案。
> 
> 更好的方案是使用 Django 的 ASGI 模式 + 异步 generator，直接用 `async def` 写 generator，这样就不需要线程切换的开销了。或者考虑用 FastAPI 这样的原生异步框架来替代 Django 处理这部分逻辑。

---

### Q6：前端如何实现音频流式播放？

**参考回答：**

> 我使用的是 **MediaSource Extensions（MSE）** 方案：
> 
> 1. 每次用户发送消息时，创建一个新的 `MediaSource` 实例并绑定到 `<audio>` 元素
> 2. 当 `sourceopen` 事件触发后，创建一个 `audio/mpeg` 类型的 `SourceBuffer`
> 3. 每次从 SSE 收到 base64 编码的音频 chunk，先解码为 `Uint8Array`，然后推入队列
> 4. 通过 `processQueue()` 控制写入节奏——只有当 `SourceBuffer` 不在 updating 状态时才写入下一个 chunk
> 5. 音频 chunk 写入后，`<audio>` 元素会自动继续播放，实现了"边收边播"
> 
> 我没有选 Web Audio API（`AudioContext`），原因是 MSE 对 MP3 格式原生支持好，解码由浏览器内部完成，性能更好；而 Web Audio API 需要手动解码 PCM 数据再拼接，延迟更高且实现更复杂。MSE 的缺点是不能精确控制到 sample 级别，但对于聊天语音播放场景来说足够了。

---

### Q7：如果用户在语音播放过程中发送了新消息，怎么处理？

**参考回答：**

> 前端侧：我用了一个 `processId` 计数器来做**请求版本控制**。每次发送新消息或停止时，`processId++`。SSE 回调中会检查 `curId !== processId`，如果不匹配就丢弃这个 chunk。同时调用 `stopAudio()` 暂停播放、清空队列、释放 MediaSource。
>
> 但这里确实有一个不足：**服务端的 LLM 调用和 TTS WebSocket 连接并不会因为用户中断而主动关闭**。前端中断 SSE 连接后，后端的 generator 会在下一次 yield 时发现连接已断开并抛出异常，从而终止。
>
> 更完善的方案应该是：前端发送一个取消请求到后端，后端主动中断 LLM 的 streaming 和 TTS 连接，释放服务端资源。这在当前版本中还没实现，是一个待优化的点。

---

## 二、LangGraph Agent 状态机

### Q8：请描述你的 Agent 状态机的状态转移图

**参考回答：**

> 我的 Agent 状态机有三个核心组件：
>
> ```
> ┌──────┐    ┌──────┐    should_continue?
> │ START │───→│ agent │─────────────┬──→ tools ──→ agent (循环)
> └──────┘    └──────┘              │
>                                   └──→ END
> ```
>
> 具体来说：
> - **START → agent**：入口边，将用户的输入（经过 system prompt 和历史消息组装后）传入 LLM
> - **agent 节点**：调用 LLM（DeepSeek V3.2），LLM 可以选择直接回复文本，也可以选择调用工具（通过 function calling）
> - **should_continue 条件边**：检查 LLM 返回的最后一条消息是否包含 `tool_calls`。如果有，走 `tools` 路径；如果没有，走 `END`
> - **tools 节点**：LangGraph 内置的 `ToolNode`，自动执行 LLM 选择的工具并将结果作为 `ToolMessage` 追加到状态中
> - **tools → agent**：工具执行结果返回给 agent，LLM 可以根据工具返回结果决定下一步——可能直接回复，也可能再次调用工具（形成循环）

---

### Q9：`should_continue` 的判断逻辑是什么？LLM 如何决定调用工具？

**参考回答：**

> `should_continue` 检查的是 `state['messages'][-1].tool_calls`。当 LLM（DeepSeek V3.2）通过 OpenAI 兼容的 function calling 协议被调用时，如果它判断需要使用工具，会在返回的 `AIMessage` 中填充 `tool_calls` 字段，包含要调用的函数名和参数。如果没有 tool_calls，说明 LLM 认为可以直接回答。
>
> LLM 的工具选择完全由模型自身的推理能力决定——我在定义工具时，每个 `@tool` 装饰器下方的 docstring 就是工具描述，LangChain 会把它转换成 OpenAI 的 `tools` 参数格式传给模型。例如 `search_knowledge_base` 的描述是"当用户查询阿里云百炼平台时的相关信息时，调用此函数"，LLM 会根据这个描述和用户的问题来决定是否调用。

---

### Q10：请举一个循环执行的具体例子

**参考回答：**

> 比如用户问："现在几点了？阿里云百炼平台的 RAG 服务怎么收费？"
>
> 第一轮：agent 节点的 LLM 判断需要先查时间，返回 `tool_calls: [get_time()]`
> → should_continue 判断有 tool_calls，走 tools 路径
> → ToolNode 执行 `get_time()`，返回当前时间，以 ToolMessage 追加到 messages
> → 回到 agent 节点
>
> 第二轮：LLM 看到时间信息，但还需要查百炼平台的收费信息，返回 `tool_calls: [search_knowledge_base("阿里云百炼RAG服务收费")]`
> → 再次走 tools 路径
> → ToolNode 执行 RAG 检索，返回相关文档片段
> → 回到 agent 节点
>
> 第三轮：LLM 现在有了时间和收费信息，直接生成综合回答，没有 tool_calls
> → should_continue 判断无 tool_calls，走 END
>
> 这就是一个完整的 agent → tools → agent → tools → agent → END 的循环过程。

**面试官可能追问：** 有没有设置最大循环次数？

> LangGraph 默认的 `recursion_limit` 是 25。在当前版本中我使用了默认值。但对于聊天场景，如果出现异常的循环（比如 LLM 反复调用工具但始终不生成最终回答），25 次循环会消耗大量 token。生产环境中我应该设置一个更小的值（比如 5-10），并在达到限制时返回一个兜底回答。

---

### Q11：LangGraph 和 LangChain 的 AgentExecutor 有什么区别？为什么选择 LangGraph？

**参考回答：**

> | 特性 | AgentExecutor | LangGraph |
> |------|--------------|-----------|
> | 流程控制 | 隐式循环，黑盒 | 显式状态机，完全可控 |
> | 状态管理 | 不可访问中间状态 | 自定义 State，可追踪每一步 |
> | 条件分支 | 不支持 | 原生支持 `conditional_edges` |
> | 人机交互 | 不支持 | 支持 `interrupt` / `human_in_the_loop` |
> | 可视化 | 不支持 | 支持生成 Mermaid 图 |
> | 持久化 | 不支持 | 支持 checkpoint |
>
> 选择 LangGraph 的原因：
> 1. **可控性强**：我可以精确控制每个节点做什么、条件边怎么判断、状态怎么传递。AgentExecutor 对开发者来说是黑盒，出了问题很难调试。
> 2. **可扩展性**：未来如果要加入更复杂的流程（比如意图分类 → 不同意图走不同子图、或者需要人工审核的环节），LangGraph 可以轻松实现。
> 3. **生态趋势**：LangChain 官方也在主推 LangGraph，AgentExecutor 逐步进入维护模式。

---

### Q12：如果要扩展 AgentState，你会怎么设计？

**参考回答：**

> 当前的 `AgentState` 只有 `messages`，足够简单场景使用。如果要扩展，我会这样设计：
>
> ```python
> class AgentState(TypedDict):
>     messages: Annotated[Sequence[BaseMessage], add_messages]
>     user_intent: str              # 用户意图分类（如"闲聊"、"查知识库"、"设闹钟"）
>     context: dict                 # 额外上下文（如用户位置、时间、偏好）
>     tool_results: list            # 工具调用结果缓存
>     memory_context: str           # 注入的长期记忆摘要
> ```
>
> 这样可以在不同的节点中读写不同字段，实现更精细的流程控制。比如先做意图分类，然后根据意图选择不同的子图路径。

---

### Q13：如果加入更多工具，需要做什么调整？如何避免选错工具？

**参考回答：**

> 加入更多工具本身很简单——在 `tools` 列表中添加新的 `@tool` 函数即可，LangGraph 的 ToolNode 会自动路由到正确的工具。
>
> 但工具多了之后的核心挑战是**工具选择准确性**。我的策略：
> 1. **工具描述要精确**：每个工具的 docstring 就是 LLM 的"说明书"，必须清晰描述何时该用、何时不该用、输入输出格式
> 2. **分层工具**：如果工具超过 10 个，可以考虑先用一个"路由 LLM"做意图分类，然后只把相关工具 bind 给主 LLM，减少选择空间
> 3. **工具分类 + 子图**：不同类型工具走不同的子图，比如"信息查询类"工具一个子图，"操作执行类"工具另一个子图

---

### Q14：`search_knowledge_base` 每次调用都重新连接数据库和初始化 embedding，有什么性能问题？

**参考回答（诚实回答）：**

> 这确实是一个性能问题。在当前的 [graph.py](file:///d:/IDE/Pycharm/project/AIFriends/backend/web/views/friend/message/chat/graph.py#L28-L36) 中，每次工具调用都会执行：
> ```python
> db = lancedb.connect('./web/documents/lancedb_storage')
> embeddings = CustomEmbeddings()
> vector_db = LanceDB(...)
> ```
> 
> 其中 `CustomEmbeddings()` 每次会创建一个新的 OpenAI 客户端实例。虽然 LanceDB 是嵌入式数据库，`connect` 本身开销不大，但 embedding client 的重复创建是不必要的。
>
> 更好的方案是：
> 1. 在 `ChatGraph.create_app()` 中初始化一次，通过闭包或类属性传递给工具函数
> 2. 或者用模块级单例缓存 embedding client
> 3. 如果数据量大，还可以在启动时预加载向量索引到内存
>
> 在当前项目规模下（知识库文档量小），性能影响不显著，但这确实是需要优化的点。

---

## 三、LanceDB 向量数据库 + RAG

### Q15：为什么选择 LanceDB？

**参考回答：**

> 我选择 LanceDB 主要基于以下考虑：
>
> 1. **嵌入式，零运维**：LanceDB 不需要独立部署数据库服务，直接以文件形式存储在本地，`lancedb.connect('./path')` 即可使用。对我的项目规模来说，这大幅降低了部署复杂度
> 2. **基于 Lance 列式格式**：底层存储格式高效，支持版本控制和零拷贝查询，读取性能好
> 3. **与 LangChain 生态集成好**：有 `langchain-community` 的官方集成，API 简洁
> 4. **对比其他方案**：
>    - FAISS：纯内存索引，不支持持久化（需要手动序列化），没有元数据过滤
>    - ChromaDB：支持持久化但性能不如 LanceDB
>    - Milvus/Qdrant：功能强大但需要独立部署服务，对我这个项目来说过重
>
> 如果项目发展到需要分布式部署、亿级向量检索的阶段，我会考虑迁移到 Milvus 或 Qdrant。

---

### Q16：embedding 模型、chunk_size、chunk_overlap 是怎么确定的？

**参考回答：**

> - **Embedding 模型**：`text-embedding-v4`，这是阿里云百炼平台提供的最新文本嵌入模型，1024 维。选择它是因为我在项目中统一使用百炼平台的 API（LLM、TTS、ASR、Embedding 都走百炼），统一 API key 和管理。
> - **chunk_size=500**：我的知识库文档主要是百炼平台的技术文档，段落长度适中。500 字符大致对应一个完整的知识点或 API 说明，既不会因为太小丢失上下文，也不会因为太大引入过多噪声。
> - **chunk_overlap=50**：大约 10% 的重叠率，保证切分边界处的语义连续性。比如一个段落的最后一句话和下一段的第一句话可能有关联，重叠可以保留这种关联。
>
> 这些参数我没有做过系统的超参数调优。如果要优化，我会用一些典型的测试 query，对比不同参数组合下的召回准确率。

---

### Q17：k 为什么选 3？

**参考回答：**

> k=3 是一个经验性的选择，基于以下权衡：
> - **k 太小（如 1）**：可能遗漏相关信息，信息不充分
> - **k 太大（如 10）**：引入过多噪声片段，稀释关键信息，而且会占用大量的 context window
> - **k=3**：在信息充分性和噪声控制之间取得平衡。对于我的知识库文档来说，一个问题的相关信息通常集中在 1-2 个文档片段中，3 个片段足够覆盖，同时给一些冗余保障
>
> 理想情况下应该根据具体的 query 集合做评估，计算不同 k 下的 Recall@K 指标来选择最优值。

---

### Q18：RAG 召回质量如何评估？遇到过什么问题？

**参考回答：**

> 在开发过程中我确实遇到过两类典型问题：
>
> 1. **"检索到了但答案不对"**：有些 query 表述和文档原文差异大，embedding 相似度匹配不够准确。比如用户用口语化的方式提问，但文档用的是技术术语。
>    - 解决思路：在 LLM 侧做 query rewriting，先把用户问题转化为更精确的检索 query
>
> 2. **"该检索到但没检索到"**：当用户问题涉及跨段落信息时，单个 chunk 可能不包含完整答案。
>    - 解决思路：增大 k 值 + 使用 `chunk_overlap` 保证语义连续性 + 考虑引入 reranking 模型对召回结果重新排序
>
> 目前我主要靠人工测试来评估——用一些典型问题验证回答质量。更系统的方法是构建一个评测集（question + expected answer），自动化计算 BLEU/ROUGE 或用 LLM-as-judge 评分。

---

### Q19：文档更新了，向量索引怎么更新？

**参考回答：**

> 当前我的方案比较粗暴——[insert_document.py](file:///d:/IDE/Pycharm/project/AIFriends/backend/web/documents/utils/insert_document.py#L1-L25) 中使用 `mode='overwrite'`，全量重新构建索引。
>
> 这在文档量小的时候没问题，但如果文档量大或需要频繁更新，应该做**增量更新**：
> 1. LanceDB 的表支持 `add_documents()` 方法，可以只追加新的文档向量
> 2. 需要维护一个文档 ID → 向量 ID 的映射，更新时先删除旧向量再插入新向量
> 3. 如果文档内容修改了，需要对修改的 chunk 重新计算 embedding 并替换
>
> 在当前项目阶段，知识库文档是相对静态的（百炼平台的技术文档），全量重建可以接受。

---

### Q20：batch_size=10 的方案在百万级文档下可行吗？

**参考回答：**

> 不可行。当前方案是每次调用百炼平台的 embedding API，batch_size=10 逐批请求。百万级文档意味着 10 万次 API 调用，耗时和成本都无法接受。
>
> 优化方案：
> 1. **增大 batch_size**：大部分 embedding API 支持更大的 batch（如 100-256），减少网络往返次数
> 2. **离线批量处理**：用离线任务（如 Celery）批量处理文档，而不是在线实时计算
> 3. **本地 embedding 模型**：部署开源的 embedding 模型（如 BGE、GTE），避免 API 调用的延迟和成本
> 4. **GPU 加速**：本地模型可以用 GPU 并行计算，速度远超 API 调用
>
> 在我的项目中，知识库只有少量文档，API 调用方案完全够用。

---

## 四、长期/短期双层记忆架构

### Q21：长期记忆和短期记忆分别是什么？怎么配合？

**参考回答：**

> **长期记忆**：存储在 `Friend.memory` 字段中，是一个文本摘要（最大 5000 字符）。它是 LLM 对历史对话的压缩总结，包含用户偏好、重要信息、对话风格等。每次新对话后由 LLM 自动更新。
>
> **短期记忆**：最近 10 条对话的原始记录（`Message` 表中的 `user_message` 和 `output`），在 [add_recent_messages](file:///d:/IDE/Pycharm/project/AIFriends/backend/web/views/friend/message/chat/chat.py#L42-L50) 中加载。
>
> **配合方式**：在构造 LLM 输入时，按以下顺序组装 context：
> 1. **System Prompt**：角色设定 + 回复规则
> 2. **长期记忆**：作为 system prompt 的一部分注入（"【长期记忆】xxx"）
> 3. **最近对话**：按时间顺序排列，提供近期上下文
> 4. **当前用户消息**
>
> 这样 LLM 既能"记得"历史重要信息（通过长期记忆），又能"看到"最近的对话细节（通过短期记忆），实现连贯的个性化交互。

---

### Q22：记忆更新用的什么 prompt？

**参考回答：**

> 记忆更新使用的是数据库中 `SystemPrompt` 表里 `title='记忆'` 的 prompt 条目。通过 `SystemPrompt.objects.filter(title='记忆').order_by('order_number')` 加载，支持多条 prompt 按顺序拼接。
>
> 发给 LLM 的输入结构是：
> - **SystemMessage**：记忆总结的指令（如"请根据以下对话更新角色记忆，保留重要信息，删除过时信息"）
> - **HumanMessage**：包含原始记忆 + 最近 10 条对话记录
>
> LLM 返回更新后的记忆文本，直接覆盖写入 `Friend.memory`。
>
> 这种设计的好处是 prompt 可以通过 Django Admin 后台动态调整，不需要改代码。

---

### Q23：记忆更新是同步阻塞的吗？

**参考回答：**

> 是的，当前是同步阻塞的。看 [chat.py:L165-L203](file:///d:/IDE/Pycharm/project/AIFriends/backend/web/views/friend/message/chat/chat.py#L165-L203) 中的 `event_stream` generator：
>
> ```python
> while True:
>     msg = mq.get()
>     if not msg:
>         break
>     # ... yield SSE 数据
>
> yield 'data: [DONE]\n\n'
> # 保存消息到数据库
> Message.objects.create(...)
> # 触发记忆更新
> update_memory(friend)
> ```
>
> 记忆更新在 SSE 数据全部发送完毕之后执行。此时用户已经收到了完整的回复和语音，所以**用户不会感知到阻塞**——他的体验流程是：发消息 → 收到回复 → （后台更新记忆）。
>
> 但如果 `update_memory` 执行时间很长（比如 LLM API 响应慢），会导致这个请求的连接一直保持。更好的方案是用 **Celery 异步任务**来执行记忆更新，SSE 结束后立即返回，记忆更新在后台 worker 中完成。

---

### Q24：长期记忆超过 5000 字符会怎样？

**参考回答（诚实回答）：**

> 当前代码中 `Friend.memory` 的 `max_length=5000` 是 Django model 层面的约束。如果 LLM 返回的总结超过 5000 字符，`friend.save()` 时会触发数据库的截断或者报错（取决于数据库类型和配置）。
>
> 这是一个需要改进的点。更好的策略：
> 1. 在 memory 更新 prompt 中明确要求 LLM 控制输出长度
> 2. 在代码层面做截断保护：如果返回内容超过限制，先截断再保存
> 3. 或者采用**滑动窗口 + 归档**策略：当记忆太长时，将旧部分归档到向量数据库，只保留最近的重要信息在 memory 字段中，实现"热/冷"分离

---

### Q25：每条消息都触发记忆更新，成本是否太高？

**参考回答：**

> 你说得对。`% 1 == 0` 等于每次都触发，这确实成本较高。每条消息都会额外调用一次 LLM API 来做记忆总结。
>
> 实际上我在开发时写成 `% 1` 是为了方便测试。生产环境中应该调整为：
> - **每 N 条消息更新一次**：比如 `% 5 == 0`，每 5 条对话更新一次记忆
> - **基于时间窗口**：比如每 30 分钟更新一次
> - **基于重要性**：用一个轻量的分类器判断当前对话是否包含值得记忆的信息，只在"重要对话"后更新
> - **异步执行**：用消息队列（如 Celery）异步执行记忆更新，不阻塞主流程
>
> 同时，记忆更新使用的是生成回复的同一个 LLM（DeepSeek V3.2），如果成本敏感，可以用更便宜的模型（如小参数模型）来做记忆总结。

---

### Q26：多个角色的记忆是隔离的吗？

**参考回答：**

> 是的，完全隔离。记忆存储在 `Friend` 模型的 `memory` 字段中，而 `Friend` 表的主键是 `(me, character)` 的组合——一个用户和一个角色构成一个独立的好友关系。
>
> 同一个用户和角色 A 的对话记忆与和角色 B 的对话记忆是完全独立的。这是合理的，因为不同角色有不同的性格设定，记忆不应该互相污染。
>
> `Message` 表也是通过 `friend` 外键关联的，查询历史消息时只查询当前 `friend_id` 下的记录。

---

### Q27：用向量数据库存储记忆有什么优劣？

**参考回答：**

> **当前纯文本方案**：
> - 优点：实现简单，全量记忆注入到 system prompt 中，LLM 能完整"看到"所有记忆
> - 缺点：记忆长度有限（受 context window 和 max_length 限制），信息密度随时间降低
>
> **向量数据库方案**：
> - 优点：可以存储大量记忆片段，根据当前对话内容检索最相关的记忆，实现"按需召回"
> - 缺点：实现复杂，召回质量受 embedding 模型和检索策略影响，可能遗漏关键记忆
>
> 如果要做向量记忆，架构会变成：
> 1. 每次对话后，将关键信息作为独立的记忆条目写入向量数据库（带时间戳、话题标签等元数据）
> 2. 新对话时，先用当前消息检索最相关的 Top-K 条记忆
> 3. 将检索到的记忆 + 最近对话 + system prompt 一起传给 LLM
>
> 这本质上是把 RAG 的思路应用到记忆管理上。当角色需要积累大量长期记忆时（比如一个陪伴型 AI 要记住几个月的对话），向量方案会更合适。

---

## 五、JWT 双Token + SSE 401 自动刷新

### Q28：access_token 和 refresh_token 分别存在哪里？

**参考回答：**

> - **access_token**：存在前端的 Pinia store 中（内存状态），过期时间 2 小时。每次请求通过 `Authorization: Bearer <token>` header 发送。
> - **refresh_token**：存在 `httponly` cookie 中，过期时间 7 天。JavaScript 无法直接读取，防止 XSS 攻击窃取。
>
> 这样设计的安全考虑：
> 1. access_token 虽然存在 JS 可访问的地方，但它有效期短（2 小时），即使泄露，攻击窗口有限
> 2. refresh_token 存在 httponly cookie 中，JS 完全无法访问，大幅降低 XSS 窃取的风险
> 3. cookie 设置了 `samesite='Lax'` 防止 CSRF，`secure=True` 确保只在 HTTPS 下传输

---

### Q29、Q30：Token 有效期和 ROTATE/BLACKLIST 策略

**参考回答：**

> - access_token 有效期 2 小时：平衡用户体验和安全性。太短会导致频繁刷新，太长会增加泄露风险。
> - refresh_token 有效期 7 天：用户一周内不需要重新登录。
>
> `ROTATE_REFRESH_TOKENS: True`：每次用 refresh_token 刷新 access_token 时，**同时签发一个新的 refresh_token**，旧的 refresh_token 立即失效。这叫"token 轮换"，防止 refresh_token 被长期滥用。
>
> `BLACKLIST_AFTER_ROTATION: True`：旧 refresh_token 使用后会被加入黑名单（存在数据库中），即使攻击者拿到了旧 token 也无法再使用。这有效防止了**refresh_token 重放攻击**。
>
> 工作流程：
> ```
> 登录 → AT(2h) + RT(7天)
> 2h后 AT过期 → 用RT刷新 → 新AT(2h) + 新RT(7天)，旧RT作废
> 如果有人同时用旧RT尝试刷新 → 旧RT在黑名单中 → 拒绝并可能触发安全警报
> ```

---

### Q31：为什么 SSE 遇到 401 要通过"抛错误再重试"的方式？

**参考回答：**

> 这是 `@microsoft/fetch-event-source` 这个库的 API 设计决定的。
>
> 核心问题是：在 `onopen` 回调中，我们已经拿到了 HTTP 响应，此时无法"重新发起"一个新的 SSE 连接。`fetchEventSource` 的重试机制是通过 `onerror` 实现的——当 `onerror` 返回一个值时，库会自动重试。
>
> 所以我的策略是：
> 1. 在 `onopen` 中检测到 401
> 2. 调用 `api.post('/api/user/account/refresh_token/')` 刷新 token（这里复用了 axios 拦截器的逻辑）
> 3. 刷新成功后，抛出一个特殊错误 `"TOKEN_REFRESHED"`
> 4. 在 `onerror` 中捕获这个特殊错误，**不抛出**（让库认为可重试）
> 5. `startFetch()` 递归调用，用新的 token 重新发起 SSE 连接
>
> 虽然看起来"迂回"，但这是在该库的 API 约束下最干净的方案。如果我用原生的 `fetch` + `ReadableStream`，就可以在检测到 401 后直接关闭旧连接、刷新 token、重新创建新连接。

---

### Q32：并发请求的 token 刷新锁机制

**参考回答：**

> 这是一个经典的**并发控制模式**，防止多个请求同时触发 token 刷新：
>
> ```javascript
> let isRefreshing = false      // 是否正在刷新
> let refreshSubscribers = []   // 等待刷新完成的请求队列
> ```
>
> 工作流程（10 个请求同时返回 401）：
> 1. **请求 1** 返回 401，进入 interceptor。检查 `isRefreshing === false`，于是设为 `true`，发起刷新请求
> 2. **请求 2-10** 同时返回 401，进入 interceptor。检查 `isRefreshing === true`（正在刷新），于是把回调函数注册到 `refreshSubscribers` 队列中，**不发起刷新请求**
> 3. 请求 1 的刷新完成 → `onRefreshed(newToken)` → 遍历 `refreshSubscribers`，用新 token 重新发送请求 2-10
> 4. 如果刷新失败 → `onRefreshFailed()` → 所有等待中的请求都被 reject，同时调用 `logout()`
>
> 这确保了**同一时刻最多只有一次刷新请求**，避免了竞态条件。

---

### Q33：SSE 长连接中 token 过期的竞态条件

**参考回答：**

> 这是一个边界情况。在我的设置中，access_token 有效期是 2 小时，而一个 SSE 流式对话通常在几秒到几十秒内完成。正常情况下不会在传输过程中过期。
>
> 但如果极端情况下发生了（比如用户发了一条消息，LLM 因为某种原因生成极慢，超过了 2 小时），理论上：
> - SSE 连接已经建立，token 验证只在连接建立时做一次
> - 连接建立后，后续的数据推送不会再验证 token
> - 所以**已经建立的 SSE 连接不会因为 token 过期而中断**
>
> 真正的风险在于：如果此时前端尝试发起新的请求（比如加载历史消息），会遇到 401。这由常规的 token 刷新机制处理。
>
> 如果需要更严格的安全策略，可以在 SSE 数据中定期发送心跳包，并在心跳中验证 token 有效性。

---

## 六、综合/系统设计问题

### Q34：多人同时在线聊天的瓶颈和扩展方案

**参考回答：**

> 当前架构的主要瓶颈：
>
> 1. **Django 同步 view + 线程模型**：每个聊天请求都会占用一个线程直到 SSE 结束。如果同时 1000 个用户聊天，需要 1000 个线程，这在 Django 的线程模型下是不可持续的。
>    - **优化**：迁移到 ASGI + async view，用协程替代线程，单机可以处理数万并发连接
>
> 2. **LLM API 的并发限制**：每个请求都要调用百炼平台的 LLM API，受限于 API 的 RPM（每分钟请求数）限制。
>    - **优化**：请求队列 + 限流；如果量大可以考虑自部署开源模型
>
> 3. **SQLite 单文件数据库**：不支持高并发写入。
>    - **优化**：迁移到 PostgreSQL，支持连接池和并发读写
>
> 4. **单点部署**：所有服务都在一台机器上。
>    - **优化**：前后端分离部署，后端用 Gunicorn + Nginx 多 worker；如果需要水平扩展，需要解决 SSE 连接的 session affinity 问题（或用 Redis 做消息广播）

---

### Q35：从 SQLite 迁移到 MySQL/PostgreSQL 需要改什么？

**参考回答：**

> 改动非常小，主要在 Django 的 settings.py 中：
>
> 1. 修改 `DATABASES` 配置，换成 MySQL/PostgreSQL 的连接参数
> 2. 安装对应的 Python 驱动（`psycopg2` 或 `mysqlclient`）
> 3. 运行 `python manage.py migrate` 创建表结构
>
> 因为我使用的是 Django ORM，所有模型定义和查询代码完全不需要修改。ORM 层屏蔽了数据库差异。
>
> 唯一需要注意的是：SQLite 对一些高级特性（如 JSON 字段的索引、全文搜索）支持较弱，迁移到 PostgreSQL 后可以利用这些能力来优化查询。

---

### Q36：错误处理和重试机制

**参考回答（诚实回答）：**

> 当前的错误处理确实不够完善，这是我项目的一个短板：
>
> - **TTS 服务故障**：如果 TTS WebSocket 连接失败，`run_tts_tasks` 中的 `async with websockets.connect()` 会抛出异常，被 `work` 方法的 `finally` 捕获，往队列中放入 `None` 终止 SSE。但没有重试机制。
> - **LLM API 超时**：LangChain 的 `ChatOpenAI` 没有显式设置超时和重试。如果 API 不响应，请求会一直挂起。
> - **工具调用失败**：如果 `search_knowledge_base` 中 LanceDB 连接失败，会直接抛异常。
>
> 生产级的改进方案：
> 1. LLM 调用：配置 `request_timeout` 和 `max_retries` 参数
> 2. TTS 服务：加入重连逻辑和超时控制
> 3. 全局异常处理：在 SSE generator 中加 try-except，遇到错误发送一个友好的错误消息而不是直接断开
> 4. 降级策略：如果 TTS 失败，仍然返回文本；如果 LLM 超时，返回一个兜底回答

---

### Q37：性能测试和瓶颈分析

**参考回答：**

> 我没有做过正式的压测，但基于架构分析，瓶颈排序如下：
>
> 1. **LLM API 延迟**：这是最大的瓶颈。DeepSeek V3.2 的首 token 延迟约 200-500ms，完整的回复可能需要几秒。这是外部依赖，优化空间有限（可以换更快的模型或自部署）
> 2. **TTS API 延迟**：CosyVoice 流式合成的首音频 chunk 延迟约 300-500ms，与 LLM 并行执行所以不额外增加延迟
> 3. **Django 线程模型**：在高并发下，线程数量是瓶颈。单机大约能支撑 200-500 并发 SSE 连接
> 4. **数据库**：SQLite 在并发写入时会锁表。记忆更新和消息保存都是写操作
>
> 如果要优化：
> - Django → ASGI（提升并发能力到数万）
> - SQLite → PostgreSQL（解决并发写入）
> - 加入 Redis 缓存（减少数据库查询）
> - LLM 调用加请求队列和限流

---

### Q38：如果重新设计，你会做哪些不同的技术决策？

**参考回答：**

> 1. **后端框架**：考虑用 FastAPI 替代 Django。FastAPI 原生支持异步、WebSocket、SSE，不需要用线程桥接的方式绕弯路。Django 的优势在 ORM 和 Admin，但对实时通信场景来说过重。
>
> 2. **记忆更新**：用 Celery 异步任务替代同步调用，避免阻塞 SSE 连接。
>
> 3. **错误处理**：从一开始就设计完善的错误处理和降级策略，而不是事后补。
>
> 4. **向量数据库**：如果知识库规模增长，会考虑 Qdrant 或 Milvus，支持分布式部署和更好的元数据过滤。
>
> 5. **前端音频播放**：如果需要更细粒度的控制（比如音频可视化、混音），考虑用 Web Audio API 替代 MSE。
>
> 6. **部署架构**：用 Docker 容器化，方便迁移和扩展；加入 Nginx 做反向代理和 SSE 连接的超时配置。
>
> 7. **可观测性**：加入 logging、metrics、tracing，方便排查线上问题。

---

## 总结

以上回答遵循以下原则，你面试时可以参考：

| 原则 | 说明 |
|------|------|
| **基于代码说事实** | 每个回答都基于你实际的代码实现，不说没做过的事 |
| **承认不足并给出改进方案** | 面试官看重的是你的思考深度，坦诚说"这里还不够完善，如果要做我会..."比硬撑更专业 |
| **展示技术广度** | 回答时适当对比多种技术方案，说明你为什么选这个而不是那个 |
| **有层次的回答** | 先说结论 → 再说原理 → 最后说改进方向 |