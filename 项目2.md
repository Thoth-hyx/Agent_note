# 📁 Graph_RAG完整目录结构与代码文件功能分析

## 一、完整目录结构

```
graph_rag/                                    # 项目根目录
├── .env                                      # 环境变量（敏感配置，不提交Git）
├── .env.example                              # 环境变量示例模板
├── .gitignore                                # Git忽略规则
├── LICENSE                                   # 开源许可证
├── README.md                                 # 项目说明文档
├── view.png                                  # 项目界面截图
│
├── config.py                                 # 全局配置管理
├── main.py                                   # 后端主程序入口
├── requirements.txt                          # Python依赖清单
│
├── Dockerfile.backend                        # 后端Docker镜像构建文件
├── docker-compose.yml                        # 生产环境服务编排
├── docker-compose.dev.yml                    # 开发环境服务编排
│
├── start.bat                                 # Windows一键启动脚本
├── start.sh                                  # Linux/macOS一键启动脚本
├── stop.bat                                  # Windows停止脚本
├── stop.sh                                   # Linux/macOS停止脚本
│
├── nginx/                                    # Nginx反向代理配置
│   └── nginx.conf                            # Nginx主配置文件
│
├── rag_modules/                              # RAG核心模块包
│   ├── __init__.py                           # 模块包导出声明
│   ├── graph_data_preparation.py             # 图数据库数据准备模块
│   ├── graph_indexing.py                     # 图索引（实体/关系KV）模块
│   ├── milvus_index_construction.py          # Milvus向量索引构建模块
│   ├── hybrid_retrieval.py                   # 混合检索模块
│   ├── graph_rag_retrieval.py                # 图RAG检索模块
│   ├── intelligent_query_router.py           # 智能查询路由器
│   ├── generation_integration.py             # LLM答案生成模块
│   ├── session_cache_manager.py              # 会话缓存管理模块
│   ├── web_service_handler.py                # Flask Web API服务模块
│   └── recipe_recommendation.py              # 菜谱推荐管理模块
│
├── data/                                     # 数据目录
│   ├── docker-compose.yml                    # 单独的Neo4j数据导入编排
│   ├── recipes_with_images.json              # 预生成的菜谱索引（含图片路径）
│   │
│   ├── cypher/                               # Neo4j知识图谱数据
│   │   ├── neo4j_import.cypher               # Cypher导入脚本（Schema+数据导入）
│   │   ├── nodes.csv                         # 节点CSV数据
│   │   └── relationships.csv                 # 关系CSV数据
│   │
│   ├── dishes/                               # 菜谱Markdown源文件库
│   │   ├── template/                         # 菜谱模板
│   │   │   └── 示例菜/                       # 模板示例
│   │   ├── aquatic/                          # 水产类菜品（约20+道）
│   │   │   ├── 小龙虾/
│   │   │   ├── 清蒸鲈鱼/
│   │   │   ├── 葱烧海参/
│   │   │   ├── 红烧鱼.md
│   │   │   └── ...
│   │   ├── meat_dish/                        # 肉类菜品（约30+道）
│   │   │   ├── 宫保鸡丁/
│   │   │   ├── 回锅肉/
│   │   │   ├── 徽派红烧肉/
│   │   │   ├── 可乐鸡翅.md
│   │   │   └── ...
│   │   ├── vegetable_dish/                   # 素菜类（约10+道）
│   │   │   ├── 鸡蛋羹/
│   │   │   ├── 酸辣土豆丝.md
│   │   │   └── ...
│   │   ├── soup/                             # 汤类（约6+道）
│   │   ├── staple/                           # 主食类（约13+道）
│   │   ├── breakfast/                        # 早餐类（约22+道）
│   │   ├── dessert/                          # 甜品类（约17+道）
│   │   ├── drink/                            # 饮品类（约17+道）
│   │   └── condiment/                        # 调味料类（约9+种）
│   │
│   └── tips/                                 # 烹饪技巧知识库
│       ├── 厨房准备.md
│       ├── 如何选择现在吃什么.md
│       ├── 食材相克与禁忌.md
│       ├── learn/                            # 基础烹饪技巧
│       │   ├── 学习炒与煎.md
│       │   ├── 学习煮.md
│       │   ├── 学习蒸.md
│       │   ├── 学习焯水.md
│       │   ├── 去腥.md
│       │   ├── 空气炸锅.md
│       │   └── ...
│       └── advanced/                         # 高级烹饪技巧
│           ├── 油温判断技巧.md
│           ├── 糖色的炒制.md
│           ├── 辅料技巧.md
│           └── 高级专业术语.md
│
└── frontend/                                 # Next.js前端项目
    ├── Dockerfile                            # 前端Docker镜像（多阶段构建）
    ├── package.json                          # Node.js依赖清单
    ├── package-lock.json                     # 依赖锁定文件
    ├── next.config.js                        # Next.js配置
    ├── tsconfig.json                         # TypeScript配置
    ├── tailwind.config.js                    # Tailwind CSS主题配置
    ├── postcss.config.js                     # PostCSS配置
    ├── next-env.d.ts                         # Next.js类型声明
    ├── .eslintrc.json                        # ESLint代码规范配置
    │
    ├── public/                               # 静态公共资源
    │   └── og.png                            # Open Graph社交分享图片
    │
    └── src/                                  # 前端源码
        ├── app/                              # Next.js App Router页面
        │   ├── globals.css                   # 全局CSS样式
        │   ├── layout.tsx                    # 根布局（元数据、字体、HTML结构）
        │   ├── page.tsx                      # 首页（推荐卡片+快捷提问）
        │   ├── chat/
        │   │   └── page.tsx                  # 聊天页（对话界面+会话管理）
        │   └── recipe/
        │       └── [id]/
        │           └── page.tsx              # 菜谱详情页（食材+步骤+营养）
        │
        ├── components/                       # React组件库
        │   ├── ui/                           # 通用UI组件
        │   │   ├── index.ts                  # 统一导出
        │   │   ├── Button.tsx                # 按钮组件（多变体、动画）
        │   │   ├── Card.tsx                  # 卡片组件（毛玻璃效果）
        │   │   ├── Input.tsx                 # 输入框组件
        │   │   ├── Modal.tsx                 # 模态对话框
        │   │   ├── Toast.tsx                 # 消息提示组件
        │   │   └── LoadingSpinner.tsx        # 加载动画组件
        │   ├── chat/                         # 聊天相关组件
        │   │   ├── index.ts                  # 统一导出
        │   │   ├── ChatMessage.tsx           # 聊天消息气泡（Markdown渲染）
        │   │   └── ChatInput.tsx             # 聊天输入框（自动扩展+快捷键）
        │   └── recipe/                       # 菜谱相关组件
        │       ├── index.ts                  # 统一导出
        │       ├── RecipeCard.tsx            # 菜谱卡片组件
        │       ├── IngredientList.tsx        # 食材清单组件（可调份量）
        │       └── CookingSteps.tsx          # 烹饪步骤组件（分步展示）
        │
        ├── hooks/                            # 自定义React Hooks
        │   ├── index.ts                      # 统一导出
        │   ├── useChat.ts                    # 聊天Hook（流式发送、中断、复制）
        │   └── useRecipes.ts                 # 菜谱Hook（搜索、详情、收藏、评分）
        │
        ├── store/                            # 状态管理
        │   ├── index.ts                      # 统一导出
        │   └── useAppStore.ts                # Zustand全局Store（持久化）
        │
        ├── lib/                              # 工具库
        │   └── api.ts                        # Axios HTTP客户端（API封装+SSE流式）
        │
        └── types/                            # TypeScript类型定义
            └── index.ts                      # 全局类型（Recipe、Chat、User、API等）
```

---

## 二、后端代码文件详细分析

### 1. [config.py](file:///d:/IDE/Pycharm/project/graph_rag/config.py) — 全局配置管理

| 属性 | 说明 |
|------|------|
| **功能** | 定义 `GraphRAGConfig` 数据类，集中管理所有系统配置参数 |
| **核心逻辑** | 使用 `@dataclass` 装饰器声明配置字段，通过 `os.getenv()` 从环境变量读取值并提供默认值 |
| **关键配置** | Neo4j连接（URI/用户/密码）、Milvus连接（Host/Port/Collection）、嵌入模型（BGE-small-zh-v1.5, 512维）、LLM模型、检索参数（top_k=5, chunk_size=500）、生成参数（temperature=0.1, max_tokens=2048） |
| **依赖关系** | 被 `main.py` 导入，传入所有 RAG 模块；依赖 `os` 和 `dataclasses` |
| **架构角色** | 全局配置中心，所有模块通过它获取参数 |

### 2. [main.py](file:///d:/IDE/Pycharm/project/graph_rag/main.py) — 后端主程序入口（397行）

| 属性 | 说明 |
|------|------|
| **功能** | 定义 `AdvancedGraphRAGSystem` 系统主类，负责初始化、编排和启动所有模块 |
| **核心逻辑** | 按顺序初始化 9 个子系统：①数据准备 ②向量索引 ③生成模块 ④传统混合检索 ⑤图RAG检索 ⑥查询路由器 ⑦会话缓存 ⑧菜谱推荐 ⑨Web服务 |
| **关键流程** | `initialize_system()` → `build_knowledge_base()` → `run_web_service()`。知识库构建：先检查Milvus是否已有集合 → 若有则加载，若无则从Neo4j加载图数据 → 构建文档 → 分块 → 向量化插入Milvus |
| **问答流程** | `ask_question_with_routing()` → 智能路由分析 → 选择检索策略（传统/图RAG/组合） → 检索相关文档 → LLM生成答案 |
| **依赖关系** | 导入所有 `rag_modules` 模块 + `config.py`；启动 Flask Web 服务监听 `0.0.0.0:8000` |
| **架构角色** | 系统编排器（Orchestrator），连接所有模块的核心中枢 |

### 3. [rag_modules/\_\_init\_\_.py](file:///d:/IDE/Pycharm/project/graph_rag/rag_modules/__init__.py) — 模块包声明

| 属性 | 说明 |
|------|------|
| **功能** | 导出 4 个核心模块类：`GraphDataPreparationModule`、`MilvusIndexConstructionModule`、`HybridRetrievalModule`、`GenerationIntegrationModule` |
| **架构角色** | 包级公共API暴露 |

### 4. [rag_modules/graph_data_preparation.py](file:///d:/IDE/Pycharm/project/graph_rag/rag_modules/graph_data_preparation.py) — 图数据库数据准备（458行）

| 属性 | 说明 |
|------|------|
| **功能** | 连接 Neo4j，读取图数据（Recipe/Ingredient/CookingStep 节点及关系），转换为 LangChain `Document` 对象并分块 |
| **核心逻辑** | ① `load_graph_data()` — Cypher查询加载三类节点，通过 `BELONGS_TO_CATEGORY` 关系获取分类 ② `build_recipe_documents()` — 对每个Recipe查询关联的 `REQUIRES`（食材）和 `CONTAINS_STEP`（步骤），组装成结构化Markdown文档 ③ `chunk_documents()` — 按章节标题（`##`）或固定长度分块（chunk_size=500, overlap=50） |
| **关键算法** | 分块策略优先按 Markdown 二级标题切分，不足则按长度强制分块并带重叠窗口 |
| **依赖关系** | `neo4j` 驱动 + `langchain_core.documents.Document`；被 `main.py` 调用，结果供 `milvus_index_construction` 和 `hybrid_retrieval` 使用 |
| **数据结构** | `GraphNode`（node_id/labels/name/properties）、`GraphRelation`（start/end/type/properties） |
| **架构角色** | 数据访问层（DAL），知识图谱 → 文档的桥梁 |

### 5. [rag_modules/graph_indexing.py](file:///d:/IDE/Pycharm/project/graph_rag/rag_modules/graph_indexing.py) — 图索引键值对模块（387行）

| 属性 | 说明 |
|------|------|
| **功能** | 为图实体和关系创建键值对（KV）索引结构，支持快速检索和去重 |
| **核心逻辑** | ① `create_entity_key_values()` — 为 Recipe/Ingredient/CookingStep 各创建 KV 对，K=实体名称, V=详细描述 ② `create_relation_key_values()` — 为关系创建多键索引（如 `REQUIRES` 关系的键包括 `食材搭配`、`烹饪原料`、`{菜名}_食材`） ③ `deduplicate_entities_and_relations()` — 基于名称去重实体，基于 source-target-type 签名去重关系 |
| **关键算法** | `_generate_relation_index_keys()` — 根据关系类型生成全局主题键；可选 LLM 增强生成更多索引键 |
| **依赖关系** | `langchain_core.documents`；被 `hybrid_retrieval.py` 使用 |
| **数据结构** | `EntityKeyValue`（entity_name/index_keys/value_content/entity_type/metadata）、`RelationKeyValue`（relation_id/index_keys/value_content/relation_type/source/target/metadata） |
| **架构角色** | 索引层，LightRAG 风格的 KV 索引实现 |

### 6. [rag_modules/milvus_index_construction.py](file:///d:/IDE/Pycharm/project/graph_rag/rag_modules/milvus_index_construction.py) — Milvus 向量索引构建（503行）

| 属性 | 说明 |
|------|------|
| **功能** | 管理 Milvus 向量数据库的集合生命周期：创建、索引、插入、查询、删除 |
| **核心逻辑** | ① `_setup_embeddings()` — 加载 `BAAI/bge-small-zh-v1.5` HuggingFace嵌入模型（CPU模式，归一化） ② `build_vector_index()` — 创建集合Schema（12个字段）→ 批量生成向量（batch=100）→ 插入Milvus → 创建HNSW索引（M=16, efConstruction=200）→ 加载到内存 ③ `similarity_search()` — 生成查询向量 → 构建过滤表达式 → COSINE相似度搜索（ef=64） |
| **集合Schema** | `id`(VARCHAR PK) + `vector`(FLOAT_VECTOR 512d) + `text` + `node_id` + `recipe_name` + `node_type` + `category` + `cuisine_type` + `difficulty` + `doc_type` + `chunk_id` + `parent_id` |
| **关键算法** | HNSW（Hierarchical Navigable Small World）近似最近邻索引；COSINE 余弦相似度度量 |
| **依赖关系** | `pymilvus.MilvusClient` + `langchain_huggingface.HuggingFaceEmbeddings` + `numpy` |
| **架构角色** | 向量存储层，稀疏/密集向量检索的基础设施 |

### 7. [rag_modules/hybrid_retrieval.py](file:///d:/IDE/Pycharm/project/graph_rag/rag_modules/hybrid_retrieval.py) — 混合检索模块（628行）

| 属性 | 说明 |
|------|------|
| **功能** | 实现 LightRAG 风格的双层检索范式：实体级 + 主题级，结合图结构、向量、BM25 三种检索 |
| **核心逻辑** | ① `extract_query_keywords()` — LLM 分析查询，提取实体级关键词（具体食材/菜品）和主题级关键词（抽象概念/风格） ② `entity_level_retrieval()` — 图索引KV精确匹配 → Neo4j全文索引补充 → 一跳邻居扩展 ③ `topic_level_retrieval()` — 图索引关系键匹配 → 实体分类匹配 → Neo4j分类/菜系/标签补充 ④ `dual_level_retrieval()` — 合并两层结果，去重排序 ⑤ `vector_search_enhanced()` — Milvus向量检索 + Neo4j邻居信息增强 ⑥ `hybrid_search()` — **Round-robin轮询**合并策略，交替从图检索和向量结果中选取 |
| **关键算法** | Round-robin 合并策略避免单一检索方法偏置；BM25 关键词检索作为稀疏检索补充 |
| **依赖关系** | `neo4j` + `langchain_community.retrievers.BM25Retriever` + `graph_indexing.GraphIndexingModule` + Milvus模块 + 数据模块 |
| **架构角色** | 传统混合检索引擎，RAG 系统的核心检索组件 |

### 8. [rag_modules/graph_rag_retrieval.py](file:///d:/IDE/Pycharm/project/graph_rag/rag_modules/graph_rag_retrieval.py) — 图RAG检索模块（634行）

| 属性 | 说明 |
|------|------|
| **功能** | 真正的图结构知识推理和检索系统，利用 Neo4j 图拓扑进行多跳遍历、子图提取和路径推理 |
| **核心逻辑** | ① `understand_graph_query()` — LLM 分析查询意图，识别5种图查询模式：`ENTITY_RELATION`/`MULTI_HOP`/`SUBGRAPH`/`PATH_FINDING`/`CLUSTERING` ② `multi_hop_traversal()` — 多跳遍历（1-3跳），Neo4j `*1..N` 变长路径查询，路径评分公式：`1/路径长度 + 度数归一化 + 关系类型匹配加分` ③ `extract_knowledge_subgraph()` — 提取中心实体的 N 跳邻居子图，计算图密度指标 ④ `graph_structure_reasoning()` — 识别推理模式 → 构建推理链 → 验证可信度 ⑤ `adaptive_query_planning()` — 根据复杂度分数（<0.3简单/0.3-0.7中等/>0.7复杂）动态调整查询策略 ⑥ `graph_rag_search()` — 整合入口，根据查询类型分发到不同策略 |
| **数据结构** | `QueryType`枚举（5种）、`GraphQuery`、`GraphPath`（nodes/relationships/path_length/relevance_score）、`KnowledgeSubgraph`（central_nodes/connected_nodes/metrics/reasoning_chains） |
| **依赖关系** | `neo4j` + LLM客户端；被 `intelligent_query_router` 调用 |
| **架构角色** | 图RAG检索引擎，体现知识图谱推理优势的核心模块 |

### 9. [rag_modules/intelligent_query_router.py](file:///d:/IDE/Pycharm/project/graph_rag/rag_modules/intelligent_query_router.py) — 智能查询路由器（335行）

| 属性 | 说明 |
|------|------|
| **功能** | 根据查询特征自动选择最优检索策略，是系统的"大脑"决策层 |
| **核心逻辑** | ① `analyze_query()` — LLM 深度分析查询，输出 `QueryAnalysis`：复杂度(0-1)、关系密集度(0-1)、是否需要推理、实体数量、推荐策略、置信度 ② `_rule_based_analysis()` — 降级方案，基于关键词规则（"为什么/如何/关系"→复杂，"配/搭配"→关系密集） ③ `route_query()` — 路由执行：`HYBRID_TRADITIONAL`→传统混合检索 / `GRAPH_RAG`→图RAG检索 / `COMBINED`→**并行执行**两种检索 ④ `_combined_search()` — 使用 `ThreadPoolExecutor` 并行执行两种检索，Round-robin 交替合并结果（图RAG优先） ⑤ `_post_process_results()` — 向结果文档注入路由元数据 |
| **数据结构** | `SearchStrategy`枚举（3种）、`QueryAnalysis`数据类 |
| **依赖关系** | 依赖传统检索模块和图RAG模块；被 `main.py` 的问答流程调用 |
| **架构角色** | 智能路由层，策略决策器 |

### 10. [rag_modules/generation_integration.py](file:///d:/IDE/Pycharm/project/graph_rag/rag_modules/generation_integration.py) — LLM 答案生成（169行）

| 属性 | 说明 |
|------|------|
| **功能** | 封装 LLM 调用，支持普通生成和 SSE 流式生成 |
| **核心逻辑** | ① 初始化 OpenAI 兼容客户端（支持 SiliconFlow/Moonshot/DeepSeek 等多供应商） ② `_build_prompt()` — 构建烹饪助手角色 Prompt，注入检索上下文 ③ `generate_adaptive_answer()` — 普通同步生成 ④ `generate_adaptive_answer_stream()` — Generator 流式生成，带**指数退避重试**（最多3次）和非流式降级 |
| **Prompt 模板** | 角色设定：专业烹饪助手 → 注入检索信息 → 用户问题 → 分类回答指导（列表/步骤/综合） → 对话历史忠实性提醒 |
| **依赖关系** | `openai.OpenAI` + `langchain_core.documents`；环境变量 `OPENAI_API_KEY` + `OPENAI_BASE_URL` |
| **架构角色** | 生成层，RAG 的 "G"（Generation） |

### 11. [rag_modules/session_cache_manager.py](file:///d:/IDE/Pycharm/project/graph_rag/rag_modules/session_cache_manager.py) — 会话缓存管理（205行）

| 属性 | 说明 |
|------|------|
| **功能** | 管理会话级语义缓存和对话上下文，加速重复查询响应 |
| **核心逻辑** | ① `check_semantic_cache()` — 计算查询向量 → 遍历缓存计算余弦相似度 → 超过阈值(0.75)则命中 ② `add_to_semantic_cache()` — 将 query-response 对及其 embedding 存入缓存（每会话最多50条） ③ `add_to_context()` — 维护对话历史（最多10条） ④ `get_context_for_query()` — 取最近3轮对话历史增强当前查询 |
| **关键算法** | 余弦相似度：`dot(a,b) / (||a|| * ||b||)` |
| **数据结构** | 三级字典：`session_caches{session_id: {query: {response, timestamp}}}`、`session_embeddings{session_id: {query: embedding}}`、`session_contexts{session_id: [messages]}` |
| **依赖关系** | `numpy` 向量计算 + HuggingFace嵌入模型（通过构造函数注入） |
| **架构角色** | 缓存层，性能优化组件 |

### 12. [rag_modules/web_service_handler.py](file:///d:/IDE/Pycharm/project/graph_rag/rag_modules/web_service_handler.py) — Flask Web API（331行）

| 属性 | 说明 |
|------|------|
| **功能** | 设置 Flask 路由，处理所有 HTTP API 请求 |
| **API 端点** | `GET /health` — 健康检查 / `POST /api/chat` — 普通聊天 / `POST /api/chat/stream` — SSE流式聊天 / `POST /api/recipes/recommendations` — 随机推荐 / `GET /api/recipes/<id>` — 菜谱详情 / `GET /api/stats` — 系统统计 |
| **核心逻辑** | ① 聊天请求：**并行**执行缓存检查 + 查询预处理（ThreadPoolExecutor）→ 缓存命中则直接返回 → 否则走完整 RAG 流程（路由→检索→生成）→ 结果写入缓存 ② 流式请求：SSE（Server-Sent Events）格式，`data: {json}\n\n` + `data: [DONE]\n\n`，缓存命中时模拟流式（3字符/段, 20ms间隔） ③ 推荐请求：委托给 `RecipeRecommendationManager` |
| **依赖关系** | `flask` + `flask_cors` + `concurrent.futures`；持有 `rag_system` 引用调用所有子模块 |
| **架构角色** | API 网关层，前后端通信桥梁 |

### 13. [rag_modules/recipe_recommendation.py](file:///d:/IDE/Pycharm/project/graph_rag/rag_modules/recipe_recommendation.py) — 菜谱推荐管理（296行）

| 属性 | 说明 |
|------|------|
| **功能** | 管理菜谱推荐逻辑，从预生成索引文件获取随机推荐和菜谱详情 |
| **核心逻辑** | ① `get_random_recipes_with_images()` — 读取 `data/recipes_with_images.json` → 转换为API格式 → `random.sample` 随机选取3个 ② `_process_image_url()` — 处理图片URL：本地相对路径 → GitHub LFS Media URL ③ `get_recipe_by_id()` — 根据ID（`recipe_1`格式）查找 → 读取Markdown文件解析食材/步骤 ④ `_get_fallback_recommendations()` — 硬编码的3道备用菜谱（红烧肉/西红柿鸡蛋/宫保鸡丁） |
| **依赖关系** | `json` + `random` + `os`；读取 `data/recipes_with_images.json` 和 `data/dishes/**/*.md` |
| **架构角色** | 业务逻辑层，菜谱推荐服务 |

---

## 三、前端代码文件详细分析

### 14. [frontend/src/types/index.ts](file:///d:/IDE/Pycharm/project/graph_rag/frontend/src/types/index.ts) — TypeScript 类型定义（211行）

| 属性 | 说明 |
|------|------|
| **功能** | 定义项目所有 TypeScript 接口和类型 |
| **核心类型** | `Recipe`（菜谱：id/name/description/category/difficulty/cookingTime/prepTime/servings/ingredients/steps/tags/nutrition）/ `Ingredient`（食材：id/name/amount/unit/alternatives/checked）/ `CookingStep`（步骤：id/stepNumber/title/description/duration/temperature/tips）/ `NutritionInfo` / `ChatMessage`（id/role/content/timestamp/metadata{recipes,suggestions}）/ `ChatSession` / `UserPreferences`（8个维度：饮食限制/过敏/菜系/辣度/技能等）/ `ApiResponse` / `StreamResponse` / `SearchFilters` / `Timer` / `Toast` / `AppState`（全局状态树结构） |
| **架构角色** | 类型安全层，前后端数据契约 |

### 15. [frontend/src/lib/api.ts](file:///d:/IDE/Pycharm/project/graph_rag/frontend/src/lib/api.ts) — API 客户端（250+行）

| 属性 | 说明 |
|------|------|
| **功能** | 封装所有 HTTP 请求，包括普通请求和 SSE 流式请求 |
| **核心逻辑** | ① Axios 实例配置：baseURL 从 `NEXT_PUBLIC_API_URL` 读取，超时30s ② 请求拦截器：注入 `auth_token` ③ 响应拦截器：统一错误处理，401自动跳转登录 ④ `chatApi.sendMessage()` — **AsyncGenerator** 实现 SSE 流式读取：`fetch` + `ReadableStream` + `TextDecoder`，逐 chunk yield ⑤ `recipeApi` — 搜索/详情/推荐/分类查询 ⑥ `userApi` — 收藏/评分/偏好 |
| **SSE 协议** | 读取 `data: {json}` 行 → 解析 `chunk` 字段 → 遇到 `[DONE]` 结束 |
| **依赖关系** | `axios`；被 `useChat` 和 `useRecipes` hooks 调用 |
| **架构角色** | 数据访问层（前端），后端 API 的客户端封装 |

### 16. [frontend/src/store/useAppStore.ts](file:///d:/IDE/Pycharm/project/graph_rag/frontend/src/store/useAppStore.ts) — Zustand 全局状态管理（260+行）

| 属性 | 说明 |
|------|------|
| **功能** | 管理整个应用的全局状态，使用 Zustand + persist 中间件实现 localStorage 持久化 |
| **状态树** | ① `user` — 偏好(8维度) + 收藏列表 + 评分列表 ② `chat` — 当前会话 + 所有会话列表 + 加载/流式状态 ③ `recipes` — 当前菜谱 + 搜索结果 + 最近浏览 + 加载状态 ④ `cooking` — 烹饪模式（活跃菜谱/当前步骤/计时器列表/完成步骤） ⑤ `ui` — 主题/侧边栏/模态框/Toast消息 |
| **核心方法** | 用户偏好管理 / 聊天会话CRUD / 消息添加更新 / 菜谱搜索收藏 / 烹饪模式控制 / UI状态管理 |
| **依赖关系** | `zustand` + `zustand/middleware`（persist）；被所有页面组件和 hooks 消费 |
| **架构角色** | 前端状态管理中心，替代 Redux 的轻量方案 |

### 17. [frontend/src/hooks/useChat.ts](file:///d:/IDE/Pycharm/project/graph_rag/frontend/src/hooks/useChat.ts) — 聊天 Hook（254行）

| 属性 | 说明 |
|------|------|
| **功能** | 封装聊天交互的所有业务逻辑 |
| **核心方法** | ① `sendMessage()` — 添加用户消息 → 创建助手占位 → SSE流式接收 → 逐chunk更新消息 → 失败时降级到普通请求 ② `stopGeneration()` — 通过 `AbortController` 中断流式请求 ③ `regenerateResponse()` — 重新生成某条助手回复 ④ `copyMessage()` — 复制到剪贴板 ⑤ `provideFeedback()` — 点赞/踩 |
| **依赖关系** | `useAppStore`（状态读写）+ `chatApi`（API调用）；被 `ChatPage` 页面使用 |
| **架构角色** | 业务逻辑 Hook，连接 UI 和数据层 |

### 18. [frontend/src/hooks/useRecipes.ts](file:///d:/IDE/Pycharm/project/graph_rag/frontend/src/hooks/useRecipes.ts) — 菜谱 Hook（263行）

| 属性 | 说明 |
|------|------|
| **功能** | 封装菜谱相关的所有业务逻辑 |
| **核心方法** | `searchRecipes()` / `getRecipeDetails()` / `getRecommendations()` / `getRecipesByCategory()` / `toggleFavorite()` / `submitRating()` / `getUserFavorites()` / `isFavorited()` / `getUserRating()` / `clearSearchResults()` |
| **依赖关系** | `useAppStore` + `recipeApi` + `userApi`；被首页和详情页使用 |
| **架构角色** | 业务逻辑 Hook |

### 19. [frontend/src/app/layout.tsx](file:///d:/IDE/Pycharm/project/graph_rag/frontend/src/app/layout.tsx) — 根布局

| 属性 | 说明 |
|------|------|
| **功能** | Next.js App Router 根布局，定义 HTML 结构、元数据和全局容器 |
| **核心逻辑** | `lang="zh-CN"` + SEO元数据（title/description/keywords）+ Viewport配置 + 三个挂载根（`#root`/`#modal-root`/`#toast-root`）+ 导入 `globals.css` |
| **依赖关系** | Next.js Metadata/Viewport API；被所有页面继承 |

### 20. [frontend/src/app/page.tsx](file:///d:/IDE/Pycharm/project/graph_rag/frontend/src/app/page.tsx) — 首页（280+行）

| 属性 | 说明 |
|------|------|
| **功能** | 应用首页，展示推荐菜谱卡片和快捷提问 |
| **核心逻辑** | ① 加载随机3个推荐菜谱（`getRecommendations`） ② 渐变背景 + 毛玻璃效果侧边栏（搜索/收藏） ③ 6个快捷提问按钮（"今天晚餐吃什么？"等） ④ 点击"开始对话"创建新会话并跳转聊天页 ⑤ 菜谱卡片可点击进入详情页 |
| **UI框架** | Framer Motion动画 + Lucide图标 + Tailwind CSS |
| **依赖关系** | `useAppStore` + `useRecipes` + `RecipeCard` + UI组件 |

### 21. [frontend/src/app/chat/page.tsx](file:///d:/IDE/Pycharm/project/graph_rag/frontend/src/app/chat/page.tsx) — 聊天页（312行）

| 属性 | 说明 |
|------|------|
| **功能** | ChatGPT风格的对话界面，支持多会话管理和流式输出 |
| **核心逻辑** | ① URL参数解析：`?session=xxx&q=xxx` ② 会话初始化：指定会话 → 切换；无参数 → 创建新会话 ③ 侧边栏：会话列表（标题+消息数+时间）+ 新建/删除/切换 ④ 消息区域：`ChatMessage` 组件渲染 + 自动滚动到底部 ⑤ 输入区域：`ChatInput` 组件 + 停止生成按钮 ⑥ 空状态：3个建议问题按钮 |
| **依赖关系** | `useChat` hook + `useAppStore` + `ChatMessage` + `ChatInput` + `LoadingSpinner` |

### 22. [frontend/src/app/recipe/[id]/page.tsx](file:///d:/IDE/Pycharm/project/graph_rag/frontend/src/app/recipe/[id]/page.tsx) — 菜谱详情页（356行）

| 属性 | 说明 |
|------|------|
| **功能** | 菜谱详情展示，包含图片、基本信息、食材清单、烹饪步骤、营养信息 |
| **核心逻辑** | ① 路由参数 `[id]` 获取菜谱ID → `getRecipeDetails()` 加载 ② 头部：图片+难度标签+名称+描述+用时/份量卡片+标签 ③ 标签页切换：食材清单（可调份量）/ 烹饪步骤（分步展示）/ 营养信息（卡路里/蛋白质/碳水/脂肪） ④ 操作：收藏/分享/开始烹饪 |
| **依赖关系** | `useRecipes` + `useAppStore` + `IngredientList` + `CookingSteps` |

### 23. [frontend/src/components/chat/ChatMessage.tsx](file:///d:/IDE/Pycharm/project/graph_rag/frontend/src/components/chat/ChatMessage.tsx) — 聊天消息组件（211行）

| 属性 | 说明 |
|------|------|
| **功能** | 渲染单条聊天消息，支持 Markdown 和流式打字效果 |
| **核心逻辑** | ① 用户消息：蓝色气泡 + 白色文字 ② 助手消息：使用 `ReactMarkdown` 渲染，自定义组件映射（p/ul/ol/li/h1-h3/code/pre/blockquote） ③ 流式指示器：三个跳动的点 + "AI正在思考..." ④ 操作按钮：复制/点赞/踩 ⑤ 扩展信息：相关菜谱推荐卡片 + 建议问题按钮 |
| **依赖关系** | `react-markdown` + `framer-motion` + Lucide图标 |

### 24. [frontend/src/components/chat/ChatInput.tsx](file:///d:/IDE/Pycharm/project/graph_rag/frontend/src/components/chat/ChatInput.tsx) — 聊天输入框（196行）

| 属性 | 说明 |
|------|------|
| **功能** | 聊天输入区域，支持自动扩展、快捷键和语音输入 |
| **核心逻辑** | ① textarea 自动高度调整（`scrollHeight`） ② Enter 发送 + Shift+Enter 换行 ③ 字符计数（1000上限） ④ 附件/表情/语音按钮（语音为UI占位） ⑤ 毛玻璃边框 + 聚焦高亮动画 |
| **依赖关系** | `framer-motion` + Lucide图标 |

### 25-27. UI 组件

| 文件 | 功能 |
|------|------|
| [Button.tsx](file:///d:/IDE/Pycharm/project/graph_rag/frontend/src/components/ui/Button.tsx) | 多变体按钮（primary/secondary/ghost/glass），支持动画和加载状态 |
| [Card.tsx](file:///d:/IDE/Pycharm/project/graph_rag/frontend/src/components/ui/Card.tsx) | 卡片容器，支持毛玻璃（glass）和默认变体 |
| [Input.tsx](file:///d:/IDE/Pycharm/project/graph_rag/frontend/src/components/ui/Input.tsx) | 输入框组件，带标签和错误提示 |
| [Modal.tsx](file:///d:/IDE/Pycharm/project/graph_rag/frontend/src/components/ui/Modal.tsx) | 模态对话框，使用 React Portal 渲染到 `#modal-root` |
| [Toast.tsx](file:///d:/IDE/Pycharm/project/graph_rag/frontend/src/components/ui/Toast.tsx) | 消息提示，支持 success/error/warning/info 类型，自动消失 |
| [LoadingSpinner.tsx](file:///d:/IDE/Pycharm/project/graph_rag/frontend/src/components/ui/LoadingSpinner.tsx) | 加载动画，支持 sm/md/lg 尺寸 |

### 28-29. 菜谱组件

| 文件 | 功能 |
|------|------|
| [RecipeCard.tsx](file:///d:/IDE/Pycharm/project/graph_rag/frontend/src/components/recipe/RecipeCard.tsx) | 菜谱卡片，展示图片/名称/描述/难度/用时/评分/标签，支持收藏和分享 |
| [IngredientList.tsx](file:///d:/IDE/Pycharm/project/graph_rag/frontend/src/components/recipe/IngredientList.tsx) | 食材清单，支持按份量缩放、勾选购物清单模式、替代食材展示 |
| [CookingSteps.tsx](file:///d:/IDE/Pycharm/project/graph_rag/frontend/src/components/recipe/CookingSteps.tsx) | 烹饪步骤，分步展示+进度条+完成标记+技巧提示+计时器集成 |

---

## 四、基础设施配置文件分析

### 30. [docker-compose.yml](file:///d:/IDE/Pycharm/project/graph_rag/docker-compose.yml) — 生产环境编排

| 服务 | 镜像 | 端口 | 说明 |
|------|------|------|------|
| `neo4j` | neo4j:5.11-community | 7474/7687 | 图数据库，配置APOC+GDS插件，健康检查 |
| `milvus-etcd` | etcd:v3.5.5 | - | Milvus元数据存储 |
| `milvus-minio` | minio:2023-03-20 | 9000/9001 | Milvus对象存储 |
| `milvus-standalone` | milvus:v2.3.2 | 19530/9091 | 向量数据库，依赖etcd+minio健康检查 |
| `neo4j-init` | neo4j:5.11-community | - | 一次性数据导入容器，执行Cypher脚本 |
| `backend` | 构建自Dockerfile.backend | 8000 | Python后端，挂载.env和data卷 |
| `frontend` | 构建自frontend/Dockerfile | 3000 | Next.js前端 |
| `nginx` | nginx:alpine | 80 | 反向代理，统一入口 |

### 31. [nginx/nginx.conf](file:///d:/IDE/Pycharm/project/graph_rag/nginx/nginx.conf) — Nginx 反向代理

| 属性 | 说明 |
|------|------|
| **功能** | 统一入口代理，前端（`/` → `:3000`）和后端API（`/api/` → `:8000`）分离 |
| **关键配置** | Gzip压缩 / SSE支持（`proxy_buffering off` + `proxy_cache off`）/ WebSocket升级 / 86400s长连接超时 |

### 32. [data/cypher/neo4j_import.cypher](file:///d:/IDE/Pycharm/project/graph_rag/data/cypher/neo4j_import.cypher) — 知识图谱构建脚本

| 属性 | 说明 |
|------|------|
| **功能** | 在 Neo4j 中创建完整的烹饪知识图谱 Schema 并导入数据 |
| **核心逻辑** | ① 创建唯一约束（Recipe/Ingredient/CookingStep 的 nodeId）② 创建索引（名称/难度/菜系/食材分类/步骤编号）③ 创建全文搜索索引 ④ LOAD CSV 导入层次节点 → Recipe节点 → Ingredient节点 → CookingStep节点 → 创建关系（REQUIRES/CONTAINS_STEP/BELONGS_TO_CATEGORY等） |
| **图Schema** | 节点：Recipe + Ingredient + CookingStep + Category + CookingMethod + CookingTool + DifficultyLevel；关系：REQUIRES + CONTAINS_STEP + BELONGS_TO_CATEGORY + USES_METHOD + USES_TOOL |

---

## 五、整体架构协作关系图

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户浏览器                                │
│  ┌──────────┐  ┌──────────────┐  ┌──────────────────────────┐  │
│  │  首页     │  │  聊天页      │  │  菜谱详情页               │  │
│  │ page.tsx  │  │ chat/page.tsx│  │ recipe/[id]/page.tsx     │  │
│  └─────┬────┘  └──────┬───────┘  └────────────┬─────────────┘  │
│        │              │                        │                │
│  ┌─────┴──────────────┴────────────────────────┴─────────────┐  │
│  │                  Hooks (useChat / useRecipes)              │  │
│  │                     Zustand Store (useAppStore)            │  │
│  │                        lib/api.ts (Axios + SSE)           │  │
│  └────────────────────────────┬──────────────────────────────┘  │
└───────────────────────────────┼─────────────────────────────────┘
                                │ HTTP/SSE
┌───────────────────────────────┼─────────────────────────────────┐
│                           Nginx (:80)                           │
│                   / → frontend:3000                             │
│                   /api/ → backend:8000                          │
└───────────────────────────────┼─────────────────────────────────┘
                                │
┌───────────────────────────────┼─────────────────────────────────┐
│                     main.py (AdvancedGraphRAGSystem)            │
│                               │                                 │
│  ┌────────────────────────────┴──────────────────────────────┐  │
│  │              web_service_handler.py (Flask)                │  │
│  │  /api/chat  /api/chat/stream  /api/recipes  /api/stats    │  │
│  └────────────────────────────┬──────────────────────────────┘  │
│                               │                                 │
│  ┌────────────────────────────┴──────────────────────────────┐  │
│  │           session_cache_manager.py (语义缓存)              │  │
│  └────────────────────────────┬──────────────────────────────┘  │
│                               │                                 │
│  ┌────────────────────────────┴──────────────────────────────┐  │
│  │         intelligent_query_router.py (智能路由)             │  │
│  │            analyze_query → route_query                     │  │
│  └─────────┬──────────────────────────────┬──────────────────┘  │
│            │                              │                     │
│  ┌─────────┴──────────┐     ┌─────────────┴─────────────────┐  │
│  │ hybrid_retrieval.py │     │  graph_rag_retrieval.py       │  │
│  │ (传统混合检索)       │     │  (图RAG检索)                  │  │
│  │ - BM25              │     │  - 多跳遍历                   │  │
│  │ - 向量检索           │     │  - 子图提取                   │  │
│  │ - 图索引KV匹配       │     │  - 路径推理                   │  │
│  │ - Round-robin合并    │     │  - 自适应规划                 │  │
│  └────────┬───────────┘     └─────────────┬─────────────────┘  │
│           │                                │                    │
│  ┌────────┴────────────────┐    ┌──────────┴──────────────────┐ │
│  │ graph_indexing.py       │    │ graph_rag_retrieval          │ │
│  │ (实体/关系KV索引)        │    │ 直接查询Neo4j                │ │
│  └────────┬────────────────┘    └──────────┬──────────────────┘ │
│           │                                │                    │
│  ┌────────┴────────────────────────────────┴──────────────────┐ │
│  │         graph_data_preparation.py (数据准备)                │ │
│  │         milvus_index_construction.py (向量索引)             │ │
│  └─────────────────────────┬──────────────────────────────────┘ │
│                            │                                    │
│  ┌─────────────────────────┴──────────────────────────────────┐ │
│  │           generation_integration.py (LLM生成)              │ │
│  │           recipe_recommendation.py (菜谱推荐)              │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
            │                           │
   ┌────────┴────────┐         ┌────────┴────────┐
   │   Neo4j (:7687) │         │  Milvus (:19530) │
   │   图数据库       │         │  向量数据库       │
   │   + etcd + MinIO │         │                  │
   └─────────────────┘         └──────────────────┘
```

---

## 六、依赖关系总结

**后端核心依赖链：**
```
main.py → config.py → GraphDataPreparationModule(→Neo4j)
                     → MilvusIndexConstructionModule(→Milvus+HuggingFace)
                     → GenerationIntegrationModule(→OpenAI API)
                     → HybridRetrievalModule(→BM25+Milvus+Neo4j+GraphIndexing)
                     → GraphRAGRetrieval(→Neo4j+LLM)
                     → IntelligentQueryRouter(→Hybrid+GraphRAG+LLM)
                     → SessionCacheManager(→HuggingFace+numpy)
                     → WebServiceHandler(→Flask→所有模块)
                     → RecipeRecommendationManager(→JSON+MD文件)
```

**前端核心依赖链：**
```
layout.tsx → pages(page/chat/recipe)
         → hooks(useChat/useRecipes) → store(useAppStore/Zustand)
                                     → lib/api.ts(Axios+SSE)
         → components(ui/chat/recipe) → types/index.ts
```

这个项目完整展示了从 **知识图谱数据准备 → 向量化索引构建 → 双引擎检索（传统混合+图RAG）→ 智能路由 → LLM生成 → 会话缓存 → Web API → 前端展示** 的全链路 Graph RAG 系统架构。

---

好问题！根据这个项目的架构，我给你一个**从零开始构建的完整顺序**。

---

## 构建顺序总览

```
阶段1: 项目初始化
    ↓
阶段2: 后端基础架构
    ↓
阶段3: 数据层（图数据库 + 向量数据库）
    ↓
阶段4: RAG核心模块
    ↓
阶段5: Web服务层
    ↓
阶段6: 前端基础架构
    ↓
阶段7: 前端功能实现
    ↓
阶段8: 前后端联调
```

---

## 阶段1: 项目初始化

### 1.1 后端初始化

```bash
# 创建项目目录
mkdir graph_rag
cd graph_rag

# 创建Python虚拟环境
python -m venv venv
.\venv\Scripts\activate  # Windows

# 创建目录结构
mkdir rag_modules
mkdir data
mkdir data\cypher
mkdir data\dishes
mkdir data\tips

# 安装后端依赖
pip install flask flask-cors
pip install neo4j pymilvus
pip install sentence-transformers
pip install openai
pip install langchain langchain-core
```

### 1.2 前端初始化

```bash
# 创建Next.js项目
npx create-next-app@latest frontend --typescript --tailwind --app

# 进入前端目录
cd frontend

# 安装前端依赖
npm install zustand axios framer-motion
npm install react-markdown react-syntax-highlighter
npm install @types/react-syntax-highlighter
```

### 1.3 最终目录结构

```
graph_rag/
├── config.py              # 后端配置
├── main.py                # 后端入口
├── rag_modules/           # RAG模块目录
│   └── __init__.py
├── data/                  # 数据目录
│   ├── cypher/           # Neo4j脚本
│   ├── dishes/           # 菜谱Markdown
│   └── tips/             # 烹饪技巧
├── frontend/              # Next.js前端
│   ├── src/
│   ├── package.json
│   └── ...
└── requirements.txt       # Python依赖
```

---

## 阶段2: 后端基础架构

### 2.1 配置文件 `config.py`

**第一个要写的文件**，因为所有模块都依赖它。

```python
# config.py - 配置管理
import os
from typing import Dict, Any
from dataclasses import dataclass

@dataclass
class GraphRAGConfig:
    # Neo4j配置
    neo4j_uri: str = os.getenv("NEO4J_URI", "bolt://localhost:7687")
    neo4j_user: str = os.getenv("NEO4J_USER", "neo4j")
    neo4j_password: str = os.getenv("NEO4J_PASSWORD", "your_password")
    
    # Milvus配置
    milvus_host: str = os.getenv("MILVUS_HOST", "localhost")
    milvus_port: int = int(os.getenv("MILVUS_PORT", "19530"))
    milvus_collection_name: str = "cooking_knowledge"
    milvus_dimension: int = 512
    
    # 模型配置
    embedding_model: str = os.getenv("EMBEDDING_MODEL", "BAAI/bge-small-zh-v1.5")
    llm_model: str = os.getenv("LLM_MODEL", "moonshot-v1-8k")
    
    # 检索配置
    top_k: int = 5
    temperature: float = 0.1
    max_tokens: int = 2048
    
    # 文档处理配置
    chunk_size: int = 500
    chunk_overlap: int = 50
    max_graph_depth: int = 2

DEFAULT_CONFIG = GraphRAGConfig()
```

**为什么先写它**：
- 所有模块都需要配置
- 定义了数据库连接参数、模型参数等
- 后续模块初始化时需要 `from config import DEFAULT_CONFIG`

---

## 阶段3: 数据层

### 3.1 图数据准备模块 `rag_modules/graph_data_preparation.py`

**这是数据层的基础**，负责从Neo4j加载数据。

**依赖**：
- `neo4j` 库
- `langchain_core.documents.Document`

**核心功能**：
1. 连接Neo4j数据库
2. 执行Cypher查询获取菜谱、食材、烹饪步骤
3. 构建LangChain Document对象
4. 文档分块（Chunking）

**构建顺序**：
```python
# 1. 先定义数据结构
@dataclass
class GraphNode:
    node_id: str
    labels: list
    name: str
    properties: dict

# 2. 实现数据库连接
class GraphDataPreparation:
    def __init__(self, config):
        self.driver = GraphDatabase.driver(...)
    
    # 3. 实现数据加载
    def load_graph_data(self) -> Dict:
        # Cypher查询
        pass
    
    # 4. 实现文档构建
    def build_recipe_documents(self) -> List[Document]:
        pass
    
    # 5. 实现文档分块
    def chunk_documents(self, chunk_size, chunk_overlap) -> List[Document]:
        pass
```

### 3.2 向量索引构建模块 `rag_modules/milvus_index_construction.py`

**依赖**：
- `pymilvus` 库
- `sentence_transformers` 库（BGE模型）

**核心功能**：
1. 加载Embedding模型（BGE-small-zh-v1.5）
2. 连接Milvus数据库
3. 创建Collection和索引（HNSW）
4. 文本向量化并插入Milvus
5. 向量检索

**构建顺序**：
```python
class MilvusIndexConstruction:
    def __init__(self, config):
        # 1. 初始化Embedding模型
        self.embedding_model = SentenceTransformer(config.embedding_model)
        
        # 2. 连接Milvus
        connections.connect(host=config.milvus_host, port=config.milvus_port)
    
    # 3. 创建集合
    def create_collection(self):
        pass
    
    # 4. 插入数据
    def insert_documents(self, documents):
        pass
    
    # 5. 向量检索
    def search(self, query, top_k) -> List[Document]:
        pass
```

**测试点**：
- 确保能连接Milvus
- 确保Embedding模型能正常加载
- 确保能插入和查询向量

---

## 阶段4: RAG核心模块

### 4.1 图索引模块 `rag_modules/graph_indexing.py`

**作用**：构建LightRAG风格的KV索引（实体+关系）

**依赖**：无外部依赖，纯逻辑处理

**核心功能**：
1. 从文档中提取实体（菜名、食材）
2. 构建实体索引 `{实体名: [相关文档列表]}`
3. 构建关系索引 `{(实体A, 关系, 实体B): [相关文档]}`

### 4.2 混合检索模块 `rag_modules/hybrid_retrieval.py`

**这是检索的核心**，实现双层次检索。

**依赖**：
- 已有的 `graph_indexing.py`
- 已有的 `milvus_index_construction.py`

**核心功能**：
1. 实体级检索（BM25 + 向量 + 图索引）
2. 主题级检索（语义相似度）
3. Round-robin合并策略

**构建顺序**：
```python
class HybridRetrieval:
    def __init__(self, config, index_module, graph_index):
        pass
    
    # 1. 关键词提取（调用LLM）
    def extract_query_keywords(self, query):
        pass
    
    # 2. 实体级检索
    def entity_level_retrieval(self, query, keywords):
        pass
    
    # 3. 主题级检索
    def topic_level_retrieval(self, query, keywords):
        pass
    
    # 4. 结果合并
    def merge_results(self, entity_results, topic_results):
        pass
```

### 4.3 图RAG检索模块 `rag_modules/graph_rag_retrieval.py`

**真正的图RAG**，基于图结构的知识推理。

**依赖**：
- Neo4j驱动
- 已有的 `graph_data_preparation.py`

**核心功能**：
1. 多跳图遍历
2. 子图提取
3. 路径推理
4. 5种查询类型（实体查询、关系查询、路径查询等）

### 4.4 智能查询路由模块 `rag_modules/intelligent_query_router.py`

**依赖**：
- LLM客户端
- 已有的 `hybrid_retrieval.py`
- 已有的 `graph_rag_retrieval.py`

**核心功能**：
1. LLM分析查询意图
2. 选择检索策略（hybrid_traditional / graph_rag / combined）
3. 并行执行组合查询

**构建顺序**：
```python
class IntelligentQueryRouter:
    def __init__(self, config, hybrid_module, graph_rag_module):
        pass
    
    # 1. 查询分析
    def analyze_query(self, query) -> Dict:
        # 调用LLM分析意图
        pass
    
    # 2. 策略选择
    def select_strategy(self, analysis) -> str:
        pass
    
    # 3. 执行检索
    def route_query(self, query) -> List[Document]:
        pass
```

---

## 阶段5: Web服务层

### 5.1 LLM生成模块 `rag_modules/generation_integration.py`

**依赖**：
- `openai` 库

**核心功能**：
1. OpenAI兼容的LLM客户端
2. 流式输出支持
3. 重试机制（指数退避）
4. 烹饪助手Prompt模板

### 5.2 会话缓存模块 `rag_modules/session_cache_manager.py`

**依赖**：
- 无外部依赖

**核心功能**：
1. 语义缓存（余弦相似度，阈值0.75）
2. 会话上下文管理（最多10条消息）
3. 每个session独立缓存（最多50条）

### 5.3 Web服务处理模块 `rag_modules/web_service_handler.py`

**依赖**：
- `flask` 库

**核心功能**：
1. Flask路由定义
2. SSE流式输出
3. 并行处理（ThreadPoolExecutor）

### 5.4 菜谱推荐模块 `rag_modules/recipe_recommendation.py`

**依赖**：
- 无外部依赖

**核心功能**：
1. 随机推荐菜谱
2. Markdown文件解析
3. 图片URL处理

### 5.5 主入口 `main.py`

**这是最后写的后端文件**，负责编排所有模块。

**依赖**：
- 所有 `rag_modules` 模块

**核心功能**：
1. 初始化所有子系统（按顺序）
2. 构建知识库
3. 启动Flask服务

**初始化顺序**（重要！）：
```python
class AdvancedGraphRAGSystem:
    def __init__(self, config):
        # 1. 配置
        self.config = config
        
        # 2. 数据准备（依赖Neo4j）
        self.data_module = GraphDataPreparation(config)
        
        # 3. 向量索引（依赖Milvus + Embedding模型）
        self.index_module = MilvusIndexConstruction(config)
        
        # 4. 图索引（依赖数据模块）
        self.graph_index = GraphIndexing(config)
        
        # 5. 混合检索（依赖索引模块 + 图索引）
        self.hybrid_module = HybridRetrieval(config, self.index_module, self.graph_index)
        
        # 6. 图RAG检索（依赖数据模块）
        self.graph_rag_module = GraphRAGRetrieval(config, self.data_module)
        
        # 7. LLM客户端（依赖OpenAI）
        self.generation_module = GenerationIntegration(config)
        
        # 8. 查询路由（依赖检索模块 + LLM）
        self.router_module = IntelligentQueryRouter(
            config, self.hybrid_module, self.graph_rag_module, self.generation_module
        )
        
        # 9. 会话缓存
        self.cache_module = SessionCacheManager(config)
        
        # 10. Web服务
        self.web_module = WebServiceHandler(config, self)
```

---

## 阶段6: 前端基础架构

### 6.1 类型定义 `frontend/src/types/index.ts`

**第一个要写的前端文件**，定义所有TypeScript接口。

**核心类型**：
```typescript
// 菜谱相关
export interface Recipe { ... }
export interface Ingredient { ... }
export interface CookingStep { ... }

// 聊天相关
export interface ChatMessage { ... }
export interface ChatSession { ... }

// 应用状态
export interface AppState { ... }
export interface UserPreferences { ... }
```

### 6.2 API客户端 `frontend/src/lib/api.ts`

**依赖**：
- `axios` 库

**核心功能**：
1. Axios实例配置（baseURL, 拦截器）
2. HTTP请求封装
3. SSE流式请求（fetch + ReadableStream）

### 6.3 状态管理 `frontend/src/store/useAppStore.ts`

**依赖**：
- `zustand` 库

**核心功能**：
1. 5个状态切片（user/chat/recipes/cooking/ui）
2. 持久化（localStorage）
3. 状态操作方法

---

## 阶段7: 前端功能实现

### 7.1 通用组件 `frontend/src/components/ui/`

**先写基础UI组件**：
- `Button.tsx` - 按钮
- `Card.tsx` - 卡片
- `Input.tsx` - 输入框
- `Modal.tsx` - 弹窗
- `Toast.tsx` - 提示
- `LoadingSpinner.tsx` - 加载动画

### 7.2 聊天组件 `frontend/src/components/chat/`

**依赖**：
- 通用组件
- `react-markdown` 库

**组件**：
- `ChatMessage.tsx` - 消息气泡（支持Markdown渲染）
- `ChatInput.tsx` - 输入框（自动扩展、Enter发送）

### 7.3 菜谱组件 `frontend/src/components/recipe/`

**依赖**：
- 通用组件

**组件**：
- `RecipeCard.tsx` - 菜谱卡片
- `IngredientList.tsx` - 食材列表
- `CookingSteps.tsx` - 烹饪步骤

### 7.4 自定义Hooks `frontend/src/hooks/`

**依赖**：
- API客户端
- 状态管理

**Hooks**：
- `useChat.ts` - 聊天逻辑（发送消息、SSE流、停止生成）
- `useRecipes.ts` - 菜谱逻辑（搜索、详情、推荐、收藏）

### 7.5 页面实现 `frontend/src/app/`

**按路由顺序实现**：

1. **首页** `app/page.tsx`
   - 随机推荐菜谱卡片
   - 快速问题入口
   - 侧边栏导航

2. **聊天页** `app/chat/page.tsx`
   - 会话管理侧边栏
   - 消息列表
   - 输入框
   - SSE流式显示

3. **菜谱详情页** `app/recipe/[id]/page.tsx`
   - 食材列表（可调整份量）
   - 烹饪步骤（Tab切换）
   - 营养信息

---

## 阶段8: 前后端联调

### 8.1 启动后端

```bash
# 终端1：启动后端
cd graph_rag
python main.py
# 后端运行在 http://localhost:8000
```

### 8.2 启动前端

```bash
# 终端2：启动前端
cd graph_rag/frontend
npm run dev
# 前端运行在 http://localhost:3000
```

### 8.3 配置代理（开发环境）

在 `frontend/next.config.js` 中配置API代理：

```javascript
module.exports = {
  async rewrites() {
    return [
      {
        source: '/api/:path*',
        destination: 'http://localhost:8000/api/:path*',
      },
    ]
  },
}
```

### 8.4 测试流程

1. **测试首页**：访问 `http://localhost:3000`，检查随机推荐是否显示
2. **测试聊天**：点击"开始聊天"，发送消息，检查SSE流式响应
3. **测试菜谱**：点击菜谱卡片，检查详情页是否正常显示
4. **测试搜索**：在聊天中询问"红烧肉怎么做"，检查检索结果

---

## 关键依赖关系图

```
config.py
    ↓
graph_data_preparation.py (依赖Neo4j)
    ↓
milvus_index_construction.py (依赖Milvus + Embedding模型)
    ↓
graph_indexing.py (依赖数据模块)
    ↓
hybrid_retrieval.py (依赖索引模块 + 图索引)
    ↓
graph_rag_retrieval.py (依赖数据模块)
    ↓
generation_integration.py (依赖OpenAI API)
    ↓
intelligent_query_router.py (依赖检索模块 + LLM)
    ↓
session_cache_manager.py (无依赖)
    ↓
web_service_handler.py (依赖Flask)
    ↓
main.py (编排所有模块)
```

---

## 常见踩坑点

### 1. Neo4j连接失败
- 确保Neo4j服务已启动
- 检查用户名密码是否正确
- 检查防火墙是否放行7687端口

### 2. Milvus连接失败
- 确保Milvus服务已启动
- 检查端口19530是否可用
- Milvus需要etcd和MinIO作为依赖

### 3. Embedding模型下载慢
- 使用国内镜像源
- 或者提前下载模型到本地

### 4. LLM API调用失败
- 检查API Key是否正确
- 检查模型名称是否正确
- 检查网络是否能访问API服务

### 5. 前端SSE流式显示异常
- 检查后端是否正确返回 `text/event-stream`
- 检查前端是否正确处理 `ReadableStream`
- 检查CORS配置是否允许流式响应

---

## 总结

**核心构建顺序**：

1. **先写配置** (`config.py`)
2. **再写数据层** (Neo4j + Milvus)
3. **然后写RAG核心** (检索 + 生成)
4. **接着写Web服务** (Flask)
5. **最后写前端** (Next.js)

**每个阶段的验证点**：
- 阶段2：能成功连接Neo4j和Milvus
- 阶段3：能从数据库加载数据并构建索引
- 阶段4：能执行检索并返回结果
- 阶段5：能通过API调用RAG系统
- 阶段6-7：前端能正常显示和交互
- 阶段8：前后端能完整联调

这样按顺序构建，每一步都有明确的验证点，遇到问题也容易定位。



---

## 当用户发送问题后，完整流程图

用户在前端输入"红烧肉怎么做？"，按下发送按钮，到看到回答，后端做了以下所有事情：

---

```
┌─────────────────────────────────────────────────────────────────────┐
│                        前端 (Next.js)                                │
│                                                                     │
│  用户输入 "红烧肉怎么做？"                                             │
│  点击发送按钮                                                        │
│       ↓                                                             │
│  fetch('/api/chat/stream', { method: 'POST', body: {message, session_id} })
│  建立 SSE 连接，等待流式数据                                          │
└───────────────────────────┬─────────────────────────────────────────┘
                            │ HTTP POST 请求
                            ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    Web服务层 (Flask)                                  │
│                    web_service_handler.py                            │
│                                                                     │
│  _handle_stream_request()                                           │
│       │                                                             │
│       ├── Step 1: 缓存检查（并行）                                    │
│       │      ├── check_semantic_cache()    → 检查语义缓存             │
│       │      └── get_context_for_query()   → 获取对话上下文            │
│       │                                                             │
│       ├── 缓存命中？ ──→ 直接返回缓存结果（跳过检索）                     │
│       │                                                             │
│       └── 缓存未命中 ──→ 继续执行完整 RAG 流程                         │
│              │                                                      │
│              ↓                                                      │
│       Step 2: 智能路由                                              │
│              query_router.route_query(query, top_k=5)               │
└──────────────────────┬──────────────────────────────────────────────┘
                       │
                       ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    智能查询路由层                                      │
│                    intelligent_query_router.py                       │
│                                                                     │
│  Step 3: 查询分析（调用 LLM）                                        │
│       analyze_query(query)                                          │
│       │                                                             │
│       │  把 "红烧肉怎么做？" 发给 LLM，让 LLM 分析：                    │
│       │  - 查询复杂度: 0.3（简单查找）                                  │
│       │  - 关系密集度: 0.2（单一实体）                                  │
│       │  - 推理需求: false                                           │
│       │  - 推荐策略: hybrid_traditional                               │
│       │                                                             │
│       ↓                                                             │
│  Step 4: 策略路由                                                   │
│       │                                                             │
│       │  ┌─────────────────────────────────────────────────────┐    │
│       │  │                                                     │    │
│       ├──│→ HYBRID_TRADITIONAL（简单查询）                       │    │
│       │  │   → traditional_retrieval.hybrid_search()           │    │
│       │  │   → 同时查向量数据库 + 图索引                          │    │
│       │  │                                                     │    │
│       ├──│→ GRAPH_RAG（复杂查询，如"川菜和湘菜有什么关系"）       │    │
│       │  │   → graph_rag_retrieval.graph_rag_search()          │    │
│       │  │   → 在图数据库中多跳遍历、子图提取                      │    │
│       │  │                                                     │    │
│       └──│→ COMBINED（中等复杂度）                                │    │
│          │   → 同时执行以上两种策略，合并结果                       │    │
│          │                                                     │    │
│          └─────────────────────────────────────────────────────┘    │
└──────────────────────┬──────────────────────────────────────────────┘
                       │
                       ↓
┌─────────────────────────────────────────────────────────────────────┐
│          检索策略 A: 传统混合检索 (hybrid_retrieval.py)                │
│          适用于 "红烧肉怎么做？" 这类简单查询                          │
│                                                                     │
│  hybrid_search() 内部并行执行两条路线：                                │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │                                                             │    │
│  │  路线1: 双层检索 (dual_level_retrieval)                      │    │
│  │       │                                                     │    │
│  │       ├── LLM 提取查询关键词                                  │    │
│  │       │   "红烧肉怎么做？" → entity_keywords: ["红烧肉"]      │    │
│  │       │                      topic_keywords: ["做法", "烹饪"] │    │
│  │       │                                                     │    │
│  │       ├── 实体级检索（关键词精确匹配）                          │    │
│  │       │   → graph_index.get_entities_by_key("红烧肉")        │    │
│  │       │   → 图索引中精确查找包含"红烧肉"的文档                   │    │
│  │       │                                                     │    │
│  │       └── 主题级检索（语义相似度）                              │    │
│  │           → graph_index.get_relations_by_key("做法")         │    │
│  │           → 图索引中查找与"做法"相关的文档                      │    │
│  │                                                             │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │                                                             │    │
│  │  路线2: 向量检索增强 (vector_search_enhanced)                 │    │
│  │       │                                                     │    │
│  │       └── milvus_index.similarity_search(query, k=top_k)    │    │
│  │           │                                                 │    │
│  │           │  "红烧肉怎么做？"                                 │    │
│  │           │       ↓                                         │    │
│  │           │  BGE嵌入模型 → [0.023, -0.156, 0.891, ...]     │    │
│  │           │       ↓                                         │    │
│  │           │  Milvus HNSW 索引搜索                            │    │
│  │           │       ↓                                         │    │
│  │           │  返回 top 5 最相似的文档片段                      │    │
│  │           │                                                 │    │
│  │           │  ┌──────────────────────────────────────────┐   │    │
│  │           │  │ 结果1: [0.92相似度] "红烧肉的做法：五花肉切块"│  │    │
│  │           │  │ 结果2: [0.89相似度] "红烧肉的食材：五花肉500g"│ │    │
│  │           │  │ 结果3: [0.75相似度] "炖肉的技巧和火候控制"   │  │    │
│  │           │  │ 结果4: [0.65相似度] "家常菜做法合集"         │  │    │
│  │           │  │ 结果5: [0.58相似度] "中式烹饪基础技法"       │  │    │
│  │           │  └──────────────────────────────────────────┘   │    │
│  │           │                                                 │    │
│  └───────────┴─────────────────────────────────────────────────┘    │
│                                                                     │
│  Step 5: Round-robin 合并                                           │
│       │                                                             │
│       │  两条路线的结果交替排列，去重：                                │
│       │                                                             │
│       │  合并前：                                                    │
│       │    双层检索结果: [A1, A2, A3, A4, A5]                        │
│       │    向量检索结果: [B1, B2, B3, B4, B5]                        │
│       │                                                             │
│       │  合并后（交替取，去重）：                                      │
│       │    [A1, B1, A2, B2, A3, B3, A4, B4, A5, B5]                │
│       │                                                             │
│       │  取前 top_k=5 个：                                           │
│       │    [A1, B1, A2, B2, A3]                                     │
│       │                                                             │
│       ↓                                                             │
│  返回 5 个 Document 对象给路由层                                       │
└──────────────────────┬──────────────────────────────────────────────┘
                       │
                       ↓
┌─────────────────────────────────────────────────────────────────────┐
│          检索策略 B: 图RAG检索 (graph_rag_retrieval.py)               │
│          适用于 "川菜和湘菜有什么关系" 这类复杂查询                     │
│                                                                     │
│  graph_rag_search() 流程：                                          │
│       │                                                             │
│       ├── Step 3.1: 查询意图理解                                     │
│       │      understand_graph_query(query)                          │
│       │      → 判断查询类型：实体关系 / 多跳遍历 / 子图提取            │
│       │                                                             │
│       ├── Step 3.2: 根据类型执行图操作                                │
│       │      │                                                      │
│       │      ├── MULTI_HOP（多跳遍历）                               │
│       │      │   → 从一个节点出发，沿着关系边跳转 N 步                 │
│       │      │   → 红烧肉 → 需要 → 五花肉 → 属于 → 猪肉 → 常用于 → 川菜│
│       │      │                                                     │
│       │      ├── SUBGRAPH（子图提取）                                │
│       │      │   → 提取某个节点周围的局部子图                         │
│       │      │   → 红烧肉周围的所有节点和关系组成子图                  │
│       │      │                                                     │
│       │      ├── PATH_FINDING（路径发现）                            │
│       │      │   → 找到两个节点之间的最短路径                         │
│       │      │   → 川菜 ←── 最短路径 ──→ 湘菜                        │
│       │      │                                                     │
│       │      └── ENTITY_RELATION（实体关系）                         │
│       │          → 查询某个实体的所有关系                             │
│       │          → 红烧肉 的所有 REQUIRES 关系 → 知道需要哪些食材     │
│       │                                                             │
│       ├── Step 3.3: 图结构推理                                      │
│       │      → 对子图/路径进行推理分析                                │
│       │                                                             │
│       ├── Step 3.4: 图结构相关性排序                                 │
│       │      → 按照图结构的紧密度/路径长度排序结果                    │
│       │                                                             │
│       ↓                                                             │
│  返回 Document 对象列表给路由层                                       │
└──────────────────────┬──────────────────────────────────────────────┘
                       │
                       ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    生成层 (generation_integration.py)                │
│                                                                     │
│  Step 6: 生成回答                                                   │
│  generate_adaptive_answer_stream(query, documents)                  │
│       │                                                             │
│       ├── 构建 Prompt                                               │
│       │   ┌──────────────────────────────────────────────────────┐  │
│       │   │ 作为一位专业的烹饪助手，请基于以下信息回答用户的问题。  │  │
│       │   │                                                      │  │
│       │   │ 检索到的相关信息：                                    │  │
│       │   │ [ENTITY] 红烧肉的做法：五花肉切块，锅中放油...        │  │
│       │   │ [ENTITY] 红烧肉的食材：五花肉500克，生抽3勺...        │  │
│       │   │ [TOPIC] 炖肉的技巧：小火慢炖，中途不要开盖...        │  │
│       │   │                                                      │  │
│       │   │ 用户问题：红烧肉怎么做？                              │  │
│       │   │                                                      │  │
│       │   │ 请提供准确、实用的回答...                             │  │
│       │   └──────────────────────────────────────────────────────┘  │
│       │                                                             │
│       ├── 调用 LLM（流式）                                          │
│       │   client.chat.completions.create(                           │
│       │       model="kimi-k2-0711-preview",                        │
│       │       messages=[{"role": "user", "content": prompt}],       │
│       │       stream=True                                           │
│       │   )                                                         │
│       │                                                             │
│       └── 逐字 yield 返回                                           │
│           yield "红"                                                │
│           yield "烧"                                                │
│           yield "肉"                                                │
│           yield "的"                                                │
│           ...                                                       │
└──────────────────────┬──────────────────────────────────────────────┘
                       │ yield 流式数据
                       ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    Web服务层 (web_service_handler.py)                │
│                                                                     │
│  Step 7: 包装成 SSE 格式并发送                                       │
│       │                                                             │
│       │  for chunk in generation_module.generate_adaptive_answer_stream():│
│       │      full_response += chunk                                 │
│       │      yield f'data: {json.dumps({"chunk": chunk})}\n\n'     │
│       │                                                             │
│       │  # 生成完成后                                               │
│       │  cache_manager.add_to_semantic_cache(query, full_response)  │
│       │  cache_manager.add_to_context(session_id, query, response)  │
│       │  yield 'data: [DONE]\n\n'                                   │
│       │                                                             │
│       ↓                                                             │
│  Response(generate(), mimetype='text/event-stream')                 │
└──────────────────────┬──────────────────────────────────────────────┘
                       │ HTTP 流式响应
                       ↓
┌─────────────────────────────────────────────────────────────────────┐
│                        前端 (Next.js)                                │
│                                                                     │
│  Step 8: 接收并显示                                                  │
│       │                                                             │
│       │  const reader = response.body.getReader()                   │
│       │  while (true) {                                             │
│       │      const { done, value } = await reader.read()            │
│       │      if (done) break                                        │
│       │      // 解析 data: {"chunk": "红"}                          │
│       │      // 更新聊天界面，逐字显示                                │
│       │  }                                                          │
│       │                                                             │
│       ↓                                                             │
│  用户看到回答逐字出现（像 ChatGPT 的打字效果）                         │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 向量数据库 vs 图数据库 的分工

在整个流程中，两个数据库各司其职：

### 向量数据库（Milvus）— "找相似"

```
用户问题 → Embedding → 向量 → 在 512 维空间中找最近的邻居

擅长场景："红烧肉怎么做"（语义模糊，需要理解"怎么做"≈"做法"）
工作原理：把文字变成数字，用数学距离衡量相似度
搜索结果：[0.92相似度] 红烧肉做法、[0.89相似度] 红烧肉食材...
```

### 图数据库（Neo4j）— "找关系"

```
用户问题 → 实体识别 → 图遍历 → 沿着关系边跳转

擅长场景："川菜和湘菜有什么关系"（需要多跳推理）
工作原理：从一个节点出发，沿着 REQUIRES/CONTAINS_STEP 等关系边行走
搜索结果：川菜 → 常用花椒 → 花椒也用于湘菜 → 两者都以辣闻名但风味不同...
```

### 两者协作方式

```
简单查询（如"红烧肉怎么做"）：
    → 路由器判断为 HYBRID_TRADITIONAL
    → 主要用 Milvus 向量检索（找相似文档）
    → 辅以图索引关键词匹配（精确找实体）
    → Round-robin 合并结果

复杂查询（如"为什么川菜喜欢用花椒"）：
    → 路由器判断为 GRAPH_RAG
    → 主要用 Neo4j 图遍历（多跳关系推理）
    → 从花椒节点出发 → 找到用花椒的菜谱 → 分析川菜特征

中等查询（如"推荐几道下饭菜"）：
    → 路由器判断为 COMBINED
    → Milvus 找语义相关的菜谱文档
    → Neo4j 找标签含"下饭菜"的菜谱
    → 合并两种结果
```

---

## 缓存的作用

缓存系统避免重复计算，加速响应：

```
用户第一次问："红烧肉怎么做？"
    → 缓存未命中
    → 走完整 RAG 流程（路由 → 检索 → LLM 生成）
    → 把 (问题, 回答) 存入语义缓存

用户第二次问："红烧肉怎么做？"（相同问题）
    → 缓存命中（余弦相似度 = 1.0 > 0.75）
    → 直接返回之前的回答
    → 跳过整个检索和生成过程
    → 响应速度快几倍

用户问："红烧肉的做法是什么？"（语义相似）
    → 缓存命中（余弦相似度 ≈ 0.85 > 0.75）
    → 返回之前的回答
    → 同样跳过检索和生成
```

---

## 时间线（大约耗时）

```
用户点击发送
    │
    ├── Step 1: 缓存检查                    ~50ms
    │   └── 缓存命中 → 直接返回              （总耗时 ~200ms）
    │
    ├── Step 3: LLM 分析查询意图            ~1-2s
    │
    ├── Step 4-5: 检索（并行执行）
    │   ├── 向量检索 (Milvus)               ~100ms
    │   ├── 图索引关键词匹配                 ~50ms
    │   └── 图遍历 (Neo4j)                  ~200ms
    │   └── 合并结果                         ~10ms
    │                                       合计 ~500ms
    │
    ├── Step 6: LLM 生成回答（流式）         ~5-15s
    │   └── 逐字返回，用户实时看到
    │
    └── Step 7-8: 缓存存储 + 前端渲染        ~10ms

总耗时（首次）：约 7-18 秒（主要取决于 LLM 生成速度）
总耗时（缓存命中）：约 200ms
```